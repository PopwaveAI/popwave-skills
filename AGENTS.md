# 项目规则 / pop 工作流

## 文件写入可靠性（实测教训，2026-09-09 固化）

实测发现：**`Edit`** **工具在本环境"返回成功但内容未写进磁盘"**，造成文档看似已改、实际没落盘；且用默认编码的 `Select-String` 回读又会被乱码哄骗，造成"以为写上了"的假象。已确认 `Write` 全量覆盖 + UTF-8 校验是可靠的。

因此，**本项目的所有文档写入，全部只用** **`Write`** **全量覆盖，完全禁止使用** **`Edit`** **工具。**

### 禁止 Edit，只用 Write

- **禁用** **`Edit`** **工具**：任何基于 old\_string 替换的修改方式都不准用，它在本环境不可靠。

- 所有改动（不管是新增、修改还是删除部分内容）一律**用** **`Write`** **重写整份文件的完整内容**。

- 新建文件也用 `Write`。

### Write 的正确用法

- **`Write`** **前必须先** **`Read`** **读取当前完整内容**（在旧内容上叠加改动，避免覆盖用户手动改过的部分；若只改某一小段，先读全文件再照抄其余部分）。

- **写完后必须用 PowerShell** **`-Encoding UTF8`** **回读校验**，检出本次改动独有的关键字符串，确认磁盘上真实存在才算完成。

- 禁止用默认编码的 `Select-String` 或只信工具返回的"成功"回执——它们都会被编码/回执骗到。

- 删除文件前先确认路径正确，删除后验证目标确实不存在。

- 修改文档后向用户汇报时，写清绝对路径 + 确认文件编码为 UTF-8，避免"我以为改了 X，其实 Y"或"打开乱码误以为没改"的错位。本目录下文档统一放 `新流程探索\sop文档\`。

### 验证命令（PowerShell，必须用 UTF-8 编码）

```powershell
$c = Get-Content -Path "文件绝对路径" -Raw -Encoding UTF8
$c.Contains("本次改动独有的关键字符串")   # True = 落盘成功；False = 写入失败，重写
```

