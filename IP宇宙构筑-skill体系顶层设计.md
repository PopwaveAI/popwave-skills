# IP 宇宙构筑 · skill 体系顶层设计

> 定位：这是"小说 IP 宇宙衍生管线"重构的地基文档。不是修补现有 skill，而是**从"构筑一个 IP 宇宙需要什么"反推**每个 skill 的定位、SOP、产出物。所有后续 skill 改造都以本文为唯一依据。
> 核心一句话：**先定义宇宙需要哪几类"真源"，再让每个 skill 只服务一个真源；派生层只消费真源，不各自重建。**
> 配套：`漫画专家管线梳理-IP宇宙衍生.md`（四层架构总纲）、`小说漫画专家重构方案.md`（分阶段落地）。
> **落地进度**：一阶段 L1 已落地——`character` 已改名升级为 `pop-visual-art-bible`（Art Bible · 美术设定集），pipeline Phase 2 路由到它，asset/style 互操作与下游 oc/cover/comic 消费协议均已指向美术设定集。详见 `小说漫画专家重构方案.md` 阶段一。

---

## 一、原点：构筑一个 IP 宇宙，需要什么

不先想 skill，先想"宇宙的成分"。一个能被持续衍生、跨形态变现的 IP 宇宙，由 **六类元件** 构成，每一类对应一组能力和一个产出物：

| 元件 | 它是"什么" | 对应能力 | 产出物 |
|:--|:--|:--|:--|
| **叙事芯** | 故事本身（剧情/对白/人物弧） | 写作 | 小说正文 |
| **世界事实** | 文本背后的客观设定（力量/地理/势力/人物关系/时间线） | 从文本提取 | 世界设定库 |
| **视觉事实** | 这个世界"看起来怎样"（画风/人物形象/场景形象/符号/色彩基调） | 消费世界事实→翻译成画面 | **视觉宇宙圣经** |
| **复现机制** | 怎么保证"处处画的是同一个宇宙"（定妆图/冻结提示词/seed） | 资产冻结+版本 | 复现资产库 |
| **衍生形态** | 把宇宙翻译成可消费作品（封面/立绘/条漫/漫剧/物料） | 生产 | 各形态成品 |
| **分发就绪** | 把成品送到平台的规格与话术 | 打包 | 投稿傻瓜包 |

外加一个贯穿的**总控**（管项目状态、阶段推进、路由调度）。

> 关键分界：**世界事实**是"这个世界有哪些客观设定"（文本里已有，提取即可）；**视觉事实**是"这个世界看起来怎样"（由世界事实翻译而来，需要专门生产）。这是两套不同的真相，必须分离、各有权威 owner。

---

## 二、现状根病诊断

现有视觉 skill 是**按"产物工序"分**的（`asset`/`style`/`character`/`oc`/`cover`/`comic`），导致两个致命错位：

1. **视觉真源被拆碎在三处、且无权威 owner。** 画风归 `style`、人物形象归 `character`、场景和符号归 `asset`——但"这个 IP 宇宙看起来整体是什么样"是一件需要统一裁决的事。现在没有任何一个 skill 是它的 owner，三处各画各的，跨形态必然漂移。
2. **`asset` 职责混。** 它既提"世界事实"（角色档案/场景资产），又产"视觉符号/IP视觉DNA"（视觉事实），横跨两套真相，职责不清。

**结论**：现有体系是"先有 skill 再拼宇宙"；重构后必须"**先定宇宙需要什么真源，再让每个 skill 只服务一个真源**"。

---

## 三、重构后的 skill 体系（按真源层 → 生产层 → 出口层）

### 真源层（唯一真源，派生层只消费，不重建）

#### ① 世界事实 owner（收敛 `pop-visual-asset` 的世界部分）

| 项 | 内容 |
|:--|:--|
| **定位** | 从小说文本提取并维护"这个世界的事实"，作为一切叙事的真源 |
| **SOP** | 读正文 → 提取人物/力量/地理/势力/时间线 → 建库 → 增量更新 |
| **产出物** | `素材/世界设定库/`（人物档案/力量体系/地理/势力/时间线，世界真源） |
| **消费方** | 视觉法则 owner（Bible）、衍生层各 skill |

#### ② 视觉法则 owner = Bible（新建/升级，重构核心）

| 项 | 内容 |
|:--|:--|
| **定位** | 消费世界事实，翻译并维护"这个宇宙看起来怎样"——**所有视觉真源的权威 owner** |
| **SOP** | 定画风基调 → 为每个实体（人物/场景/符号）建立视觉身份 → 跨实体仲裁一致性 → 冻结复现资产 → 管版本 |
| **产出物** | `素材/视觉宇宙圣经.md`（画风DNA + 人物形象 + 场景形象 + 符号库 + 色彩基调 + 复现资产 + 一致性规则 + 版本） |
| **消费方** | 生产层所有 skill（cover/oc/comic/comic-content） |

**为什么 Bible 独立**：它回答"视觉宇宙长什么样"这个跨所有衍生的根本问题，且需要 owner 做跨实体一致性裁决。这不是任何单一 skill 的子域（画风/人物/场景/符号都只是它的一个篇章），也不能塞给只做路由的 pipeline。

---

### 复用层 / 生产层（只消费 Bible，不重建视觉事实）

| Skill | 定位 | SOP 骨架 | 产出物 |
|:--|:--|:--|:--|
| `cover` | 封面/场景图生产 | 读 Bible → 选场景/符号/画风 → 出图 | 封面图、场景图 |
| `oc` | 立绘/OC 生产 | 读 Bible → 取人物形象+画风 → 出立绘 | 立绘、OC图 |
| `comic` | 序列叙事生产 | 读 Bible → 导演卡 → 出漫画页 | 条漫/页漫 |
| `comic-content` | 动态+物料生产 | 读 Bible → 出片/三件套 | 漫剧/放映/内容物料 |

> 生产层统一铁律：**只消费 Bible，禁止各自重建人物/场景/画风。** 身份改动必须回 Bible 升级版本。

---

### 出口层（分发就绪）

| Skill | 定位 | SOP 骨架 | 产出物 |
|:--|:--|:--|:--|
| 各生产 skill（内嵌） | 收尾生成自己的发布清单 | 成品 + 六项发布清单 | 投稿傻瓜包 |
| `pipeline`（出口） | 校验 + 汇总 | 校验每包带发布清单 → 汇总发布索引 | `投稿发布索引.md` |

---

### 总控

| Skill | 定位 | SOP 骨架 |
|:--|:--|:--|
| `pipeline` | 只路由不干活 | 读总控 → 检查 Bible 就绪门禁 → 路由到子 skill → 更新总控 |

---

## 四、现有 skill 归位调整对照

| 现有 skill | 重构后定位 | 理由 |
|:--|:--|:--|
| `pop-visual-asset` | 保留为 **世界事实提取**（角色档案/场景资产表/视觉符号库/IP视觉DNA） | 查原文"有什么"，不画图不裁决 |
| `pop-visual-style` | 降级为 **Bible 的画风子域输入**（提供画风DNA库/定标能力） | 画风只是 Bible 的一个篇章，不是独立真源 |
| `pop-visual-character` | **改名升级为 `pop-visual-art-bible`（Art Bible · 美术设定集）**，成为视觉法则权威 owner | 从"只设计人物身份"升级为"产出整部美术设定集"，人物只是其中一篇 |
| `pop-visual-art-bible`（即原 character） | 视觉法则权威 owner，汇总 asset/style 子域输入 + 仲裁 + 冻结，产出 `素材/美术设定集.md`（画风/人物/场景/符号/一致性五篇合一） | 重构的核心，由原 character 改名重构为一阶段产物 |
| `cover`/`oc`/`comic`/`comic-content` | 只消费美术设定集的生产层 | 不再各自重建视觉 |
| `pop-visual-pipeline` | 只路由 + 校验美术设定集就绪 | 职责不变，入口加美术设定集门禁 |

---

## 五、真源依赖关系

```
世界事实 owner ──→ 视觉法则 owner (art-bible) ──→ 生产层 (cover/oc/comic/comic-content) ──→ 出口层
   (提取文本设定)      (翻译成画面 · 仲裁)              (只消费美术设定集生产形态)              (发布清单+索引)
```

- **唯一真源原则**：世界设定库 = 世界真相唯一源；美术设定集 = 视觉真相唯一源。一切派生只消费这两本，禁止各自重建。
- **单向依赖**：生产层只依赖真源层，真源层不反向依赖生产层。
- **变更回流**：任何真源变更（新增角色/改画风）→ 回对应 owner 更新 → 升美术设定集版本 → 派生层感知后重产。

---

## 六、设计原则（本次重构的红线）

1. **真源唯一**：世界设定库 + 视觉宇宙圣经是仅有的两套真源，派生层只消费、不重建。
2. **先真源后生产**：任何形态生产前，必须先有已冻结的 Bible；缺失即中止，提示先跑真源层。
3. **Bible 是立法者不是拼接器**：它汇总 style/asset 的子域输入（人物/场景/符号由 art-bible 直接产出），但必须做跨实体一致性仲裁，产出"自洽的版本"，而非简单拼贴。
4. **单一职责**：每个 skill 只服务一个真源或一个生产动作，禁止横跨两套真相（`asset` 的教训）。
5. **pipeline 只路由**：所有实质产出由具体 skill 完成，pipeline 只做门禁检查与路由。

---

## 来源

- AI 生成标识办法与处罚：https://www.tianhebiaoshi.com/news/xinwenzixun1/205.html ；https://news.sohu.com/a/953598323_122551079