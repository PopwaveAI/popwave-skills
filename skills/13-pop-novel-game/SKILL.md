---
name: "13-pop-novel-game"
description: "当用户说'写文游 / 做互动游戏 / 小说改成游戏 / 做成AI文游 / 世界观改游戏 / 文字冒险 / 把设定变成可玩的'时启用。将写书过程中沉淀的世界观、人设、剧情设定等资料，转化为由AI驱动的互动文字游戏（AI文游）。"
pipeline:
  upstream: [pop-novel-bookstrap]
  downstream: []
---

# 13-pop-novel-game · 小说世界观AI文游化 v2.0.1

将写书过程中已沉淀的世界观、人设、剧情设定等资料，转化为**可游玩的 AI 文游（AI 驱动的文字游戏）**。

产出是让读者能走进你的小说世界里、亲自体验剧情的**互动文字游戏**（而非静态文档）。

---

## 速查表

| 阶段 | 核心动作 | 产出 | ❌ 门禁 |
|:-----|:---------|:-----|:--------|
| **Phase A — 资料解析** | 读取原始资料 → 提取静态层+角色层+剧情层 | 结构化世界观数据 JSON | 原始资料缺失→中止 |
| **Phase B — 文游设计** | 玩家身份 → 核心玩法 → 分支节点 → 数值系统 → AI约束 | AI文游指令文档 | 关键章节为空→回退 |
| **Phase C — 产出生成** | 生成指令文件 + HTML | 文游指令.md + AI文游.html | HTML 验证不通过→重做 |

### 与 pop-reader-making 的关系

```
pop-world-building（写作前：构建世界观）
        ↓
  [小说诞生]
        ↓
13-pop-novel-game（写作后/中：将世界观转化为 AI 文游）  ← 本 skill
        ↓
  [读者游玩体验]
        ↓
pop-reader-making（写完后：拆书分析）
```

---

## 执行流程 SOP

### 第一步：资料解析（Phase A）

| 项目 | 内容 |
|:-----|:------|
| 读什么 | 用户提供的世界设定、人设、剧情大纲等资料（格式不限） |
| 做什么 | 提取三层数据 |
| 产出什么 | 结构化世界观数据 JSON |
| ❌ 门禁 | 原始资料缺失或为空 → 中止 Phase A，提示用户补充。要求至少提供小说简介、世界观概要或角色列表之一 |
| 详细指令 | `方法/资料解析协议.md` |

#### 提取三层

```
静态层：地理/势力/规则体系/关键名词/专属设定
角色层：所有角色及关系网络、角色弧光
剧情层：核心冲突/关键事件/伏笔/分支可能性
```

---

### 第二步：文游设计（Phase B）

| 项目 | 内容 |
|:-----|:------|
| 读什么 | Phase A 产出的结构化 JSON |
| 做什么 | 设计游戏框架的 6 个维度 |
| 产出什么 | AI文游指令文档（草案） |
| ❌ 门禁 | 世界观/角色/游戏规则章节内容为空 → 回退到 Phase A 补充提取对应数据 |
| 详细指令 | `方法/文游设计协议.md` |

#### 设计六维

1. **玩家身份** — 作为原著中的谁？还是穿越者/旁观者？
2. **核心玩法** — 恋爱/冒险/阵营经营/探索/多线？
3. **分支节点** — 关键决策点 + 多结局路径（至少 3 个关键决策点）
4. **数值系统** — 好感度/修为/声望/黑化值等（克制：2~4 个核心数值）
5. **结局设计** — 默认至少 2 种不同结局
6. **AI约束规则** — AI 不能做什么、哪些设定不能打破

---

### 第三步：产出生成（Phase C）

| 项目 | 内容 |
|:-----|:------|
| 读什么 | Phase B 产出的文游指令草案 + 结构化 JSON |
| 做什么 | 生成两份核心产物 |
| 产出什么 | 见下表 |
| ❌ 门禁 | 指令关键章节为空 → 回退 Phase A |
| 门禁 | HTML 验证不通过 → 重新执行生成步骤，修复后再次验证 |
| 详细指令 | `方法/HTML产出规范.md` |

#### 产出文件

```
<小说名>-AI文游/
├── 互动/
│   └── AI文游.html            ← ★ 交互式HTML游玩界面（零外部依赖，双击即开）
├── 文游指令.md                ← ★ 核心产物！丢给Claude/GPT/DeepSeek即可开玩
├── 世界观设定集.md             ← 从原始资料整理的结构化世界摘要（参考用）
└── 角色档案.md                ← 从原始资料整理的角色档案（参考用）
```

---

## 文游指令设计规范

指令文件是**丢给 AI 模型让它当游戏主持人的核心文件**，必须包含：

```
# ════════════════════════════
# 《XXX》AI 文游：主持指令
# ════════════════════════════

## 一、你的角色
描述 AI 在游戏中扮演的角色（游戏主持人/DM/旁白 + 所有NPC）

## 二、世界观设定
- 时代背景
- 地理/势力分布
- 核心规则（如力量体系、社会规则、禁忌）
- 关键名词解释

## 三、角色设定
- 主角/可攻略角色/重要NPC的详细信息（性格、背景、关系）
- 玩家可以扮演的角色身份

## 四、游戏规则
- 开局流程（如何创建角色、初始设定）
- 核心玩法（每日流程、交互方式、数值系统）
- 数值规则（好感度/修为等如何增减、触发条件）
- 分支与结局（关键节点、结局条件）
- AI行为约束（禁止做哪些事、必须保持人设）
- 输出格式要求（剧情描述长度、选项数量等）
```

---

## HTML 设计要求

| 要求 | 说明 |
|:----|:------|
| **零外部依赖** | 不加载CDN、不跨域、无外部字体/图标库。仅 API 调用需要网络，`双击 .html` 即可打开 |
| **自包含** | 所有数据（含文游指令）以文本嵌入 HTML 的 JS 常量中 |
| **AI 实时驱动** | 每次玩家操作实时调 API 生成剧情，而非预设分支 |
| **世界观主题** | 视觉风格适配故事世界的调性（仙侠/科幻/都市/西幻……） |
| **交互性** | 玩家可点击选择推进剧情，输入框支持自由输入 |
| **世界百科** | 内置世界观、角色、势力的查阅面板 |
| **进度持久化** | 使用 localStorage 保存游玩进度（含对话历史和状态） |
| **验证规范** | 交付前必须验证：`</html>`闭合 / 文游指令完整嵌入 / API 路径正确 / 双击可用 |

### API 配置

HTML 生成时，API key **不得硬编码**，通过以下方式动态注入：

| 注入方式 | 说明 |
|:---------|:-----|
| **环境变量（推荐）** | 读取 `$env:DEEPSEEK_API_KEY`，生成时自动注入 HTML |
| **交互式提示** | 生成过程中显式提示用户输入 API key（不做默认填充，无占位符） |

### API 配置项

| 配置项 | 值 |
|:------|:---|
| Endpoint | `https://api.deepseek.com/v1/chat/completions` |
| Model | `deepseek-v4-flash` |
| Stream | `true` |
| Temperature | `0.85` |
| Max Tokens | `2048` |

### HTML 功能模块参考

```
AI文游.html
├── [数据层]
│   ├── API_KEY — 由环境变量或用户输入注入
│   ├── GAME_INSTRUCTION — 完整文游指令（system prompt），内部反引号须转义
│   └── WORDS_DATA — 世界观参考 JSON（供侧边栏展示）
│
├── [引擎层]
│   ├── callAI() — 调 DeepSeek API，stream:true，send messages → 流式返回 JSON
│   │   ├── AbortController — 每次创建新实例，支持取消进行中的请求
│   │   ├── 超时兜底 — 30 秒无响应自动断开 + 提示重试
│   │   └── 指数退避重试 — 网络错误最多重试 3 次（1s/2s/4s）
│   ├── cancelAI() — 取消当前 API 请求，恢复 UI 交互状态
│   ├── parseStreamResponse() — 解析完整响应
│   ├── applyGameResult() — 将 AI 响应结果同步到 gameState + 触发渲染
│   └── gameState — { messages[], stats{}, inventory[], flags{}, history[] }
│
├── [UI层]
│   ├── 主界面（叙事面板）
│   ├── 角色状态栏
│   ├── Typing Indicator（AI 思考中动画）
│   ├── 侧边栏 / 抽屉面板（世界百科/角色档案/剧情回顾/存档管理）
│   ├── 底部工具栏（复制文游指令 / 重置游戏）
│   └── [兜底机制] cleanDisplayText / JSON fallback / showRetryButton / try-catch
```

### API 交互协议

**请求**（POST `https://api.deepseek.com/v1/chat/completions`）：
```json
{
  "model": "deepseek-v4-flash",
  "messages": [
    {"role": "system", "content": "（完整文游指令）"},
    {"role": "assistant", "content": "上一轮AI的叙事和选项..."},
    {"role": "user", "content": "玩家本次的选择或自由输入"}
  ],
  "temperature": 0.85,
  "max_tokens": 2048,
  "stream": true
}
```

**SSE 流式解析流程**：
```
fetch(stream: true)
  │
  ▼
response.body.getReader()
  │
  ▼  逐块读取
for each chunk:
  ├─ 按 '\n' 分割行
  ├─ 取 'data: ' 前缀后的 JSON
  ├─ 检查 payload === '[DONE]' → 结束
  └─ choices[0].delta.content → 累积到 fullContent
       │
       ▼  实时渐进渲染（格式检测）
       ├─ 若以 { 或 " 开头（旧 JSON 格式）→ 不显示，等流结束
       ├─ 若检测到 ---DATA--- → 停止追加
       └─ 否则 → 立即追加到叙事面板（requestAnimationFrame 节流）
  │
  ▼  stream 结束
parseStreamResponse(fullContent) → { narrative, stats, choices, inventory, flags }
```

**响应格式**：
```
叙事文本（纯文字，300-500字，用 \n 表示换行）

---DATA---
{"stats":{"survival":60,"combat":8,"reputation":5,"shadow":3},"choices":["① 选项A","② 选项B","③ 选项C"],"inventory":["木棍","半块黑面包"],"flags":{"met_vivian":true}}
```

---

## 实战陷阱（踩坑记录，必须读）

### 陷阱 1：GAME_INSTRUCTION 中的反引号

**问题**：文游指令.md 里包含 markdown 代码块 ```，嵌入 HTML 时使用 JS 模板字面量，内部反引号没有转义 → JS 语法错误，所有功能失效。

**修复**：嵌入 GAME_INSTRUCTION 时，将所有内部反引号转义为 `\``。

```javascript
// ❌ 错误：内部反引号导致模板提前关闭
const GI = `# 指令内容...\`\`\`json{...}\`\`\`...`;

// ✅ 正确：内部反引号全部转义
const GI = `# 指令内容...\`\`\`json{...}\`\`\`...`;
```

### 陷阱 2：流式格式兼容

**问题**：AI 可能输出新格式（叙事文本 → `---DATA---` → JSON）或旧格式（纯 JSON）。如果流式阶段不检测格式，旧 JSON 的 `{"narrative":"你..."}` 会被实时追加到面板。

**修复**：流式循环中做 `isJsonFormat` 检测，JSON 格式等 stream 结束后一次性显示。

### 陷阱 3：重置游戏的 loading 状态锁

**问题**：如果 API 调用中（`isLoading = true`）点击重置，`callAI()` 被 `if (isLoading) return` 阻拦。

**修复**：`resetGame()` 开头调用 `cancelAI()` 解除 loading 锁——内部已执行 `isLoading = false; hideLoading();`，同时 abort 进行中的 API 请求。

### 陷阱 4：超时与重试

**问题**：API 可能因网络问题长时间无响应。

**修复**：
1. **超时定时器**（30 秒）— 连接阶段启动，数据到达后清除
2. **指数退避重试**（1s/2s/4s）— fetch 失败后自动重试，最多 3 次
3. **手动重试** — 3 次均失败后显示重试按钮

### 陷阱 5：localStorage 恢复时的 JSON 解析失败

**问题**：`localStorage` 数据可能因版本升级或手动篡改损坏，`JSON.parse` 直接抛异常导致页面白屏。

**修复**：`try-catch` 包裹恢复逻辑，解析失败直接开始新游戏。

---

## 纪律

1. **不准编造原始资料中没有的设定** — AI文游应基于作者的原始世界观，而非凭空添加
2. **Phase A 必须产出结构化 JSON** — 包含静态层 + 角色层 + 剧情层
3. **指令文件必须可独立使用** — 即使没有 HTML，丢给 AI 模型也必须能玩
4. **HTML 必须调 API 实时驱动** — 不准用预设场景查表模式
5. **HTML 交付前必须做完整性验证**
6. **HTML 的视觉风格必须匹配世界观调性**
7. **数值系统要克制** — 2~4 个核心数值足够
8. **分支节点至少设计 3 个关键决策点**
9. **默认提供至少 2 种不同结局**
10. **API key 安全** — 禁止硬编码

---

## ❌ 质量红线

| # | 红线 | 确认 |
|:-:|:-----|:----:|
| ❌1 | **不准硬编码 API key** — SKILL.md、方法文档、HTML 模板中不得出现任何真实的 API key | [ ] |
| ❌2 | **交付前必须做 HTML 完整性验证** — `</html>` 闭合、文游指令完整嵌入、SSE 解析正常、JSON fallback 正常、API 路径正确、双击可开 | [ ] |
| ❌3 | **Phase A 必须产出结构化 JSON** — 包含静态层 + 角色层 + 剧情层，不得以"已理解"替代 | [ ] |
| ❌4 | **不准编造原始资料中没有的设定** — 不得凭空添加角色、势力、规则等 | [ ] |
| ❌5 | **HTML 视觉风格必须匹配世界观调性** — 仙侠用仙侠风 UI，科幻用赛博风，不得风格错配 |
| ❌6 | **产出只留摘要** — 写入 JSON/文游指令/HTML 后对话中不粘贴完整产出。说"已写入 {路径}。摘要：{核心}。需展开告诉我。" | [ ] |

---

## ❌ 错误示例

### WRONG 1：在 SKILL.md 中硬编码 API key

```yaml
api:
  key: "sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"  # ❌ 真实 key 不应出现在任何源文件中
```
❌ 错误：一旦文件被提交到公开仓库，key 立即泄露。
✅ 正确：删除 frontmatter 中的 api 块，通过环境变量或交互式提示动态注入。

### WRONG 2：Phase A 不产出结构化 JSON

```
Agent：我看懂了这些设定 → 直接进入 Phase B 设计游戏
```
❌ 错误：Phase A 必须产出结构化 JSON，不得以"已理解"替代。
✅ 正确：先提取静态层/角色层/剧情层为 JSON，再进入 Phase B。

### WRONG 3：HTML 用了预设场景查表模式

```
Agent：预设了 10 个场景分支，玩家只能在这 10 个里选
```
❌ 错误：本 skill 要求每次玩家操作必须调 AI API 实时生成。
✅ 正确：每次玩家操作调 DeepSeek API 动态生成剧情，stream: true。

### WRONG 4：未转义 GAME_INSTRUCTION 中的反引号

```
Agent：直接将文游指令.md 的 markdown 原文嵌入 HTML 的 JS 模板字面量
```
❌ 错误：内部反引号未转义 → JS 语法错误 → 全文游不可用。
✅ 正确：嵌入前用 `.replace(/\`/g, '\\`')` 转义所有反引号。

### WRONG 5：输出目录覆盖问都不问

```
Agent：检测到 <小说名>-AI文游/ 已存在 → 直接覆盖
```
❌ 错误：覆盖已有文件应先询问用户。
✅ 正确：询问用户是否覆盖，还是创建带时间戳的备份目录（如 `<小说名>-AI文游_20260604/`）。

---

## 异常与边界条件

| # | 场景 | 处理方式 |
|:-:|:-----|:---------|
| 1 | 世界观/人设文件缺失或为空 | 中止 Phase A，明确提示缺少哪些文件，要求补充后再继续 |
| 2 | API 调用失败（网络错误 / 4xx / 5xx） | 指数退避重试最多 3 次（1s/2s/4s），3 次均失败显示手动重试按钮 |
| 3 | HTML 验证不通过（`</html>` 缺失等） | 重新执行 HTML 生成步骤，修复后再次验证，直至通过 |
| 4 | 用户未提供任何原始素材（空输入） | 要求至少提供小说简介、世界观概要或角色列表之一，否则无法启动 |
| 5 | 文游指令.md 的关键章节内容为空 | 回退到 Phase A 补充提取对应数据，确保指令完整后再进入 Phase C |
| 6 | 输出目录 `<小说名>-AI文游/` 已存在 | 询问用户是覆盖还是创建带时间戳的备份目录（如 `<小说名>-AI文游_20260604/`） |
| 7 | 用户要求移除所有 API 调用，使用纯预设内容 | 解释本 skill 的核心价值是 AI 实时驱动；如坚持，降级为静态分支游戏，但需标注"非 AI 驱动版" |
| 8 | 嵌入 HTML 的文游指令包含未转义反引号 | 自动转义所有内部反引号为 `\``，防止 JS 模板字面量语法错误 |

---

## 产出物验收清单

最终交付前，逐项确认：

- [ ] **文游指令.md** — 包含完整的角色设定、世界观规则、游戏机制、AI 行为约束、输出格式要求；丢给主流 AI 模型可直接开玩
- [ ] **AI文游.html** — 位于 `互动/` 目录下，零外部依赖，双击可打开；内置完整文游指令；所有功能正常工作
- [ ] **世界观设定集.md** — 从原始资料整理的结构化世界摘要
- [ ] **角色档案.md** — 从原始资料整理的角色档案
- [ ] **HTML 完整性验证通过** — `</html>` 闭合 / API endpoint 正确 / SSE 解析逻辑完整 / JSON fallback 兜底 / 存档恢复无白屏 / 取消与重试机制正常

---

## 方法/文档列表

- `方法/资料解析协议.md` — Phase A 资料提取标准
- `方法/文游设计协议.md` — Phase B 游戏框架设计指南
- `方法/HTML产出规范.md` — HTML 生成技术规范与验证清单

---

## 版本

v2.0.1 | 2026-06-04 | 完整变更记录 → [CHANGELOG.md](CHANGELOG.md)
