# {书名} — 项目总控台

> 人机共读 | 更新时间 {YYYY-MM-DD HH:MM}
> **上半屏 = 项目实际状态（文件系统扫描）· 下半屏 = 所属管线的理想模式**
> Agent 首次加载 / 会话恢复时从此文件获取进度锚点与目录路由。
> 对齐 PRD v5.0：管线7步 `creative → world → character → plot → chapter → prose → qa`

---

## 📚 知识库路径

> Agent 在需要查阅设定/剧情/文风参考时，从以下路径加载。路径为空 = 待补充，会阻塞相关 skill 的素材查询。
> **两类路径不可混用**：skill 公共库是所有项目共享的 pop-trope-library；用户私藏参考是单本书/单项目的精度数据。

| 类型 | 用途 | 绝对路径 | 状态 |
|:-----|:-----|:---------|:----:|
| **skill 公共库** | 设定库/剧情库/套路库/文风库 — pop-trope-library 的本地安装目录 | {pop-trope-library绝对路径} | {✅已解析 / ❌待补充} |
| **用户私藏参考** | 原书拆解数据、wiki骨架、文风DNA等 — 精度更高的原文分析（如有） | {用户指定的参考文件夹，可多个，用 ; 分隔} | {✅已填写 / ❌待补充 / — 本项目无需} |

### skill 公共库内容速览

> 初始化时自动扫描 pop-trope-library/设定库/ 下所有书目，标注是否有本书对标书。

| 模块 | 相对路径 | 本项目的对标书目 |
|:-----|:---------|:----------------|
| 设定库 | `设定库/` | {自动扫描：列出库中已有的书名；如有对标书则标注 ★} |
| 剧情库 | `剧情库/` | {同上} |
| 文风库 | `文风库/` | {同上} |
| 套路库 | `套路库/` | {列出抽象模式数量} |

### Agent 使用规则

- 首次加载总控时，检查「知识库路径」是否已填写 → 未填写则提示用户补充
- creative/world/plot 阶段启动前，必须先确认公共库路径可用（文件系统存在）
- 用户私藏参考路径 ≠ 空时，优先消费私藏数据（精度更高）; skill 公共库作为 fallback
- 路径写入后，Agent 不再询问「library 在哪」——直接读

---

# 📊 项目现状

## 首屏仪表盘

| 🏷️ | 项目阶段 | 📦 核心产出 |
|:----|:---------|:-----------|
| {状态} | 创意立项 | `全书立项PRD.md`（10块结构） |
| {状态} | 素材储备 | `素材储备池/{素材}.md`（剧情储备卡） |
| {状态} | 世界构筑 | `小说世界设定/L1-01~06.md` + `数值体系/*.md` + `起点快照.md` + `终点快照.md` + `动态升级表.md` |
| {状态} | 角色设计 | `状态/角色/{主角,配角}-角色卡.md` |
| {状态} | 剧情架构 | `剧情设计/卷/`（L4全书事件 + 每卷战略） + `剧情线/`（L3剧情线） + `幕/`（L2单元卡） |
| {状态} | 章节设计 | `章节设计包/chXXX-设计包.md`（事件链+情绪弧+钩子+调味空间） |
| {状态} | 正文渲染 | `正文/chXXX.md` |
| {状态} | 爽点质检 | QC 报告（不留盘） |

> 标记：✅ 已完成 · ⏳ 进行中 · ⬜ 未开始 · ⚠️ 跳过（有产出但非标准流程）

| 📈 项目统计 | |
|:------------|:---|
| **正文进度** | ch{起始}-ch{最新}（共 {N} 章 · 约 {N} 字） |
| **设计包** | ch{起始}-ch{最新}（{N} 个文件） |
| **素材储备池** | {N} 张（`素材储备池/`） |
| **文风DNA** | `写作资产/文风库/{书名}.md` |
| **当前幕** | 幕{N}·{名称}（ch{起始}-ch{结束}） |
| **下一章** | ch{编号} |

---

## 实际阶段执行

> 只列出文件系统中有产出的阶段。与管线理想顺序无关——反映真实执行历史。

| # | 阶段 | 一句话 | 状态 | 关键产出 |
|:-:|:-----|:------|:----:|:---------|
| {序号} | {阶段名} | {一句话说明} | {状态} | {产出文件列表} |

---

## 产出物清单

> 每个产出文件由哪些上游产物化合而成。状态由文件系统扫描自动标记。
> ✅ 存在 · ⬜ 待产出 · ❌ 缺失

| 产出文件 | 上游依赖 | 产出者 | 状态 |
|:---------|:---------|:-------|:----:|
| {文件名} | {上游1} + {上游2} + {上游3} | {skill名} | {状态} |
| 研究档案/研究档案索引.md | 种子展开法 5 阶段产出 | creative | {状态} |
| 研究档案/域DNA档案-{域名}.md | 种子展开法 Phase 4 深度域研究 | creative | {状态} |
| 研究档案/创意溯源记录.md | 种子展开法 Phase 4 深度域研究 | creative | {状态} |
| 研究档案/交叉授粉.md | 种子展开法 Phase 5 交叉授粉 | creative | {状态} |

---

## 目录结构

> 项目实际文件树。来自文件系统扫描——不展示管线理想目录，展示真实存在的目录。
> 标记：✅ 存在 · ⬜ 待产出 · ⚠️ 非标准路径（与管线理想目录不一致）

```
{项目名}/
│
├── 📄 项目总控.md                    ✅（本文件）
├── 📄 全书立项PRD.md                 {状态}  ← creative
│
├── 📁 素材储备池/                     ← creative
│   └── {素材}.md                     {状态}
│
├── 📁 研究档案/                        ← creative（下游 world/character/plot 消费）
│   ├── 研究档案索引.md                 {状态}  ← 导航+消费映射
│   ├── 种子展开图.md                   {状态}
│   ├── 域DNA档案-{域名}.md            {状态}  ← 每域一份
│   ├── 创意溯源记录.md                 {状态}
│   └── 交叉授粉.md                     {状态}
│
├── 📁 小说世界设定/                   ← world
│   ├── L1-01.md                      {状态}
│   ├── L1-02.md                      {状态}
│   ├── L1-03.md                      {状态}
│   ├── L1-04.md                      {状态}
│   ├── L1-05.md                      {状态}
│   ├── L1-06.md                      {状态}
│   ├── 起点快照.md                    {状态}
│   ├── 终点快照.md                    {状态}
│   ├── 动态升级表.md                  {状态}
│   └── 数值体系/                      {状态}
│       └── *.md                      {状态}
│
├── 📁 状态/                           ← character + prose
│   ├── 角色/
│   │   ├── 主角-角色卡.md             {状态}  ← character
│   │   └── 配角-角色卡.md             {状态}  ← character
│   └── state-log.yaml               {状态}  ← chapter（每章设计后更新，prose 读取）
│
├── 📁 剧情设计/                       ← plot（L4全书事件 / 每卷战略 / L3剧情线 / L2单元卡）
│   ├── 卷/                            ← L4全书事件 + 每卷战略
│   │   ├── L4-{编号}-{事件名}.md      {状态}
│   │   └── 卷{N}-卷纲.md              {状态}
│   ├── 剧情线/                        ← L3剧情线
│   │   └── L3-{编号}-{名称}.md        {状态}
│   └── 幕/                            ← L2单元卡
│       └── vol-XX/
│           └── L2-{编号}-{单元名}.md  {状态}
│
├── 📁 章节设计包/                     ← chapter
│   └── chXXX-设计包.md               {状态}
│
├── 📁 正文/                           ← prose
│   └── chXXX.md                      {状态}
│
└── 写作资产/                       ← world（从 library 获取）
    └── 文风库/
        └── {书名}.md                 {状态}
```

**统计**：应有 ~{N} 个文件 · 已有 ~{M} 个

---

## 当前进度锚点

- **state-log 章号**：ch{编号}（最后 event chapter: {编号}）
- **当前幕**：幕{N}·{名称}（ch{起始}-ch{结束}）
- **主角状态**：{一句话}
- **关键伏笔**：{伏笔摘要}

---

## ⚠️ 待处理 / 风险

> 当前阶段遗留问题、需要用户决策的点、可能阻塞后续管线的事项。每解决一条就删掉。

- {风险项}

---

# 🗺️ 所属管线：{专家名} {版本号}

> 本项目归属 **{专家名}** 管线。
> 以下为当前最新版管线的理想全流程。项目实际执行路径与理想管线有差异——见 §管线差异映射。

## 管线身份

| 属性 | 值 |
|:-----|:---|
| **专家类型** | {expert-writer / expert-deconstructor / ...} |
| **管线版本** | v{当前最新版本}（{日期}） |
| **管线顺序** | creative → world → character → plot → chapter → prose → qa |
| **本项目启动时版本** | v{启动版本}（{日期}）· {无差异 / ⚠️ 差 {N} 个版本} |

## 理想全流程

> 按管线合同顺序排列。每步标注调用的 skill 与核心产出。
> 对齐 PRD v5.0：7步管线，不可跳跃。

| # | 阶段 | Skill | 一句话 | 核心产出物 |
|:-:|:-----|:------|:------|:----------|
| T1 | creative | pop-writer-creative | 方向碰撞→立项PRD+储备卡 | `全书立项PRD.md` + `素材储备池/{素材}.md` + `研究档案/`（种子展开图+域DNA档案+创意溯源记录+交叉授粉图） |
| T2 | world | pop-writer-world | L1设定+数值体系+快照 | `小说世界设定/L1-01~06.md` + `数值体系/*.md` + `起点快照.md` + `终点快照.md` + `动态升级表.md` |
| T3 | character | pop-writer-character | 角色卡设计 | `状态/角色/{主角,配角}-角色卡.md` |
| T4 | plot | pop-writer-plot | L4全书事件+每卷战略+L3剧情线+L2单元卡 | `剧情设计/卷/L4-{编号}-{事件名}.md`（L4全书事件） + `卷{N}-卷纲.md`（每卷战略） + `剧情线/L3-{编号}-{名称}.md`（L3剧情线） + `幕/vol-XX/L2-{编号}-{单元名}.md`（L2单元卡） |
| T5 | chapter | pop-writer-chapter | 章设计包 | `章节设计包/chXXX-设计包.md` |
| T6 | prose | pop-writer-prose | 正文渲染+状态更新 | `正文/chXXX.md` + `状态/state-log.yaml` |
| T7 | qa | pop-writer-qa | 三层介入质检 | QC 报告（不留盘） |

## 理想目录路由

> 标准管线产出的文件落盘位置。Agent 按此路由查找上游依赖。
> 标注了每个目录对应哪个阶段。
> 对齐 PRD v5.0：无 `00-总控/` 前缀，总控文件 `项目总控.md` 在根目录。

```
{项目名}/
│
├── 项目总控.md                         ← 工程层（prose 每章更新）
├── 全书立项PRD.md                      ← T1 creative
│
├── 素材储备池/                          ← T1 creative
│   └── {素材}.md
│
├── 研究档案/                          ← T1 creative（world/character/plot 消费）
│   ├── 研究档案索引.md
│   ├── 种子展开图.md
│   ├── 域DNA档案-{域名}.md
│   ├── 创意溯源记录.md
│   └── 交叉授粉.md
│
├── 小说世界设定/                        ← T2 world
│   ├── L1-01.md
│   ├── L1-02.md
│   ├── L1-03.md
│   ├── L1-04.md
│   ├── L1-05.md
│   ├── L1-06.md
│   ├── 起点快照.md
│   ├── 终点快照.md
│   ├── 动态升级表.md
│   └── 数值体系/
│       └── *.md
│
├── 状态/                               ← T3 character + T6 prose
│   ├── 角色/
│   │   ├── 主角-角色卡.md              ← T3 character
│   │   └── 配角-角色卡.md              ← T3 character
│   └── state-log.yaml                ← T5 chapter（每章设计后更新，T6 prose 读取）
│
├── 剧情设计/                           ← T4 plot（L4全书事件 / 每卷战略 / L3剧情线 / L2单元卡）
│   ├── 卷/                            ← L4全书事件 + 每卷战略
│   │   ├── L4-{编号}-{事件名}.md
│   │   └── 卷{N}-卷纲.md
│   ├── 剧情线/                        ← L3剧情线
│   │   └── L3-{编号}-{名称}.md
│   └── 幕/                            ← L2单元卡
│       └── vol-XX/
│           └── L2-{编号}-{单元名}.md
│
├── 章节设计包/                         ← T5 chapter
│   └── chXXX-设计包.md
│
├── 正文/                               ← T6 prose
│   └── chXXX.md
│
└── 写作资产/                           ← T2 world（从 library 获取）
    └── 文风库/
        └── {书名}.md
```

## 管线差异映射

> 理想管线 vs 本项目实际。差异不代表错误——可能是主动跳过、替代方案覆盖、或尚未执行。

| 理想阶段 | 本项目状态 | 差异说明 | 影响评估 |
|:---------|:----------|:---------|:---------|
| {阶段名} | {✅/⏳/⚠️跳过/⬜} | {差异描述} | {低/中/高风险 — 原因} |

## 下一步

**基于现状锚点**：{当前进度描述}

**按管线建议**：
1. {动作1}
2. {动作2}
3. ...

---

## 执行顺序日志

> 按实际完成的先后顺序记录。用于管线连续性检测——状态标记可以全 ✅，但顺序日志会暴露断裂。

| # | 阶段 | 完成时间 | 备注 |
|:-:|:-----|:---------|:------|
| 1 | {阶段名} | {YYYY-MM-DD HH:MM} | |
| 2 | ... | ... | |

---

## 已锁定的硬约束

> 以下不可随意修改。改前需评估连锁影响。

- **{约束类别}**：{具体内容}

---

> **模板使用说明（Agent 读取，不写入生成文件）**
>
> **初始化时**（step-0-init / step-1-think 首次发现总控不存在）：
> 1. 用 `{书名}` 替换标题
> 2. 写 `更新时间` 为当前时间戳
> 3. 写 `## 管线身份` 中的版本号为当前 expert-writer SKILL.md 的 version 字段
> 4. 写 `本项目启动时版本` = 当前版本（新项目）或从已有总控继承（存量项目）
> 5. `📊 项目现状` 全部字段通过文件系统扫描填充
> 6. `🗺️ 所属管线` 从 pipeline-manifest.md 填充阶段列表（7步：creative → world → character → plot → chapter → prose → qa）
>
> **每次阶段完成后**（step-3-reflect）：
> 1. 更新 `📊 项目现状` → 首屏仪表盘 / 实际阶段执行 / 产出物清单 / 进度锚点
> 2. 追加 `执行顺序日志` 一行
> 3. 扫描文件系统更新 `目录结构`
>
> **版本漂移检测**（step-1-think 会话恢复时）：
> 1. 比对 `管线身份.管线版本` vs 当前最新版本
> 2. 如有差异 → 刷新 `🗺️ 所属管线` 全部字段，重新生成差异映射
> 3. `📊 项目现状` 不动（文件系统扫描结果不受版本影响）
>
> **跨专家复用**：
> 此模板的上半屏（`📊 项目现状`）骨架通用。下半屏（`🗺️ 所属管线`）由各专家 skill 自定义——写作专家填 T1-T7，拆书专家填 D1-D7。
