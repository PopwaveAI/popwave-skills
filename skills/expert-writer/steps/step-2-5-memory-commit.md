# Step 2.5 - 结果写入

在执行 skill 完成后，只做必要写入，不扩写历史。

## 正文完成

- 正文保存到 `正文/chXXX.md` 或用户指定路径。
- 将本章真实发生的变化追加到 `活记忆/活记忆.yaml`。
- 如果出现新设定或设定变更，标注“待 arc/plot 冻结”，不要在正文阶段私自改总设定。

## 幕纲完成

- 幕纲保存到 `library/单元剧情卡/` 或用户指定路径。
- 章节运行日志保存到 `卷纲/运行/`。
- 设定新增项写入设定账本草案区。

## 复盘完成

- arc 输出的剧情线、设定账本、活记忆压缩结果，按其指定位置写入。
- 不保留历史过程文档。
