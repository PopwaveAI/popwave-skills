## v8.1.0 | 2026-09-21

### 规则改为全量：主角与重要角色的名字，每一处出现都加「」

老板拍板。原来那条「一个人名最多 4 处、全章最多 12 处」作废，改成**名单里的名字每出现一次就加一次**。

**依据（旧日书 20 章真实正文，49430 字，名单取自章纲：沈念、凌霄、老张、苏瑶）**：

| 配方 | 平均降幅 | 加权降幅 | 命中 | 标记密度 |
| :-- | --: | --: | --: | --: |
| 一人名 ≤4 处／全章 ≤12 处 | −0.1312 | −0.1317 | 15/20 | — |
| 一人名 ≤8 处／全章 ≤30 处 | −0.1827 | ≈−0.184 | 17/20 | — |
| 只标主角全量 | −0.2204 | −0.2239 | 19/20 | 26~114/万字 |
| **主要角色全量** | **−0.2512** | **−0.2523** | **19/20** | 26~166/万字 |

- 全量把三章「加到 13 处都不动」的救回来了：ch019 −0.012 → −0.256、ch011 −0.006 → −0.099、ch018 −0.271 → −0.424。
- 20 章只剩 ch008 仍不动（该章重要角色名总共只出现 8 次，全量也只有 8 处）。
- 逐章最深：ch007 −0.5307、ch004 −0.5288、ch018 −0.4243。

**另一个理由来自老板的观感要求**：只加几处会让同一章里同一个名字一处加了、一处没加，读起来奇怪。全量章内一致。

**已知代价（写进 SKILL.md）**：全量约每万字 82 处，是真人网文里拿「」标人名最激进那一部（每万字 2.6 处）的约 32 倍。密度与降幅正相关，这条绕不开。

### 改了什么

- `SKILL.md`：加引号一节把「篇数」那条换成「全量」，并补两条已知代价；导语由「给重要人名加中文引号」改为「给主角与重要角色的名字加中文引号，每一处都加」。
- `skill.json`：version 8.0.1 → 8.1.0；description 由「在正文的重要人名上加中文引号」改为「在正文的主角与重要角色名上加中文引号」。

### 没改

除那一条规则外，走法、回执、红线、不做清单均未动。skill 仍然只有一条动作，包内仍只有 `SKILL.md`、`skill.json`、`CHANGELOG.md` 三个文件。

## v8.0.1 | 2026-09-21

### 更正 v8.0.0「已知代价」里的一句归因：那块红利不是「标点与字形规范化」，是「引号全量转换」

v8.0.0 写「脚本的标点与字形规范化是实测里最大的一块红利（同一章 0.8691 → 0.6405）」。老板要求把脚本与加引号合起来实测，确认整体正向再决定拿不拿回来。五章四臂实测后，那句归因要更正。**结论：不拿回来。**

**实测**（五章各 1392 到 1540 字，各 2 块按字数加权）：

| 臂 | 平均降幅 | 有效章数 |
| :-- | --: | --: |
| A 只加人名引号 | −0.1145 | 3/5 |
| B 只跑新脚本（v7.1.0，不转引号） | **+0.0003** | **0/5** |
| C 两个一起 | −0.1151 | 3/5 |
| D 旧脚本（v7.0.3，含全量转引号） | −0.0207 | 2/5 |

C 与 A 的逐章差是 −0.0010／0.0000／−0.0002／0.0000／−0.0015，即**合起来没有任何额外增益，还略差一点**。

**为什么。** 逐字量了脚本改多少：新脚本在这五章上只改 0 到 14 个字符，全部是「提示冒号改逗号」（第 4 章一个字都没改）；旧脚本改 0 到 74 个字符，其中绝大多数是引号转换（“”→「」、‘’→『』）。所以当年那块红利的主体是**引号全量转换**，不是笼统的「标点与字形规范化」；而引号那一层已被更强的人名加引号取代（A −0.1145 对 D −0.0207）。

**结论。** 不拿回来。它在这类稿子上几乎不触发，拿回只带回 817 行代码、34.5KB、32 个步骤与相应维护成本，增益为零。skill 维持单条动作。

### 改了什么

- `CHANGELOG.md`：本条更正。
- `skill.json`：version 8.0.0 → 8.0.1。
- `SKILL.md`：**未改动**。

## v8.0.0 | 2026-09-21

### 砍到只剩一条：给重要人名加中文引号

老板拍板。skill 现在只有一条动作：agent 读完正文，在主角与重要角色的名字上加直角引号「」。脚本、检查项、语感五条、资源全部去掉。

**为什么砍。** v7.0.0 号称「砍到只剩标点脚本」，但脚本实际还有 **817 行 / 34.5KB / 32 个编号步骤**，资源另有 **约 8 万字**（`examples-advanced.md` 39169 字节、`banned-words.md` 26651 字节）。老板复核时问「脚本为什么这么大，不应该大幅简化了吗」，结论是干脆只留有效的那一条动作。

**保留哪一条。** 只剩「加引号」。依据是本轮实测：整章 2077 字，原文 0.8078，只给名字与术语加「」得 0.6047（−0.203）；七份稿子里五份响应，降 0.04 到 0.42。

**跟着去掉的**：

| 去掉的 | 原来的作用 | 依据 |
| :-- | :-- | :-- |
| `scripts/deai_gate.py`（817 行） | 标点与字形规范化 ＋ 加引号 | 只剩一条；加引号改由 agent 做，脚本认不出主角、也判断不了合适位置 |
| `scripts/deai_profiles.json` | `--profile` 的档案 | v7.0.2 起已无调用方 |
| `resources/banned-words.md`、`structures.md`、`examples.md`、`examples-advanced.md` | 语感层的判据与示例 | 语感层已撤 |
| 破式五层法（v7.2.0 刚写回的那五条） | 语感改写口径 | 老板核对了，不是他记忆里那套，原话「算了不折腾了」 |

**已知代价，写清备查。** 脚本的标点与字形规范化是实测里最大的一块红利（同一章：原文 0.8691 → 只跑脚本 0.6405）。去掉它之后只剩加引号这一层，命中率约一半到六成（七份稿子里两份完全不响应，标满 32 到 34 处只降 0.02）。要拿回来，从 git 历史取 `f86a13b` 之前的 `scripts/deai_gate.py`。

### 改了什么

- `SKILL.md`：全文重写为 48 行，只剩「做什么／加引号的做法／不做／走法／回执／红线」六节。
- `skill.json`：version 7.2.0 → 8.0.0（能力范围变了，走大版本）；description 由「规范化中文正文的标点与字形、给主角与重要角色加引号…默认只动标点与引号」改为「在正文的重要人名上加中文引号…」。
- 删除：`scripts/deai_gate.py`、`scripts/deai_profiles.json`、`resources/banned-words.md`、`resources/structures.md`、`resources/examples.md`、`resources/examples-advanced.md`。

## v7.2.0 | 2026-09-21

### 加引号改由 agent 执行；语感五条写回（破式五层法）

**1. 加引号这件事从脚本移到 agent。** 脚本认不出谁是主角，也判断不了哪一处人名该加（照《十日终焉》的做法，加在人物被当作话题、称谓、对象的位置，不是每个主语都加）。走法第 4 步改为 agent 读正文加引号；脚本的 `--names` 保留为**省事档**（不想逐处挑位置时用），效果一致，差别只在位置是机器挑的还是人挑的。

**2. 语感五条找到了，是「破式五层法」。** 出处 `@user_741dc82b/libai`（李白.Skill:润色专家 v2.0），v4.0.0 整包替换时进入本 skill（commit 924cc7a），v7.0.0 随语感层一起被撤掉。本版写回 SKILL.md，含 L1 破句式／L2 破词汇／L3 破结构／L4 破思维／L5 破语气五层与各自手段。

**3. 默认只自检、不自动改写。** 依据是本包自己的实测：字词层 ±0.0000、整段语感改写 +0.029；把语感改动叠在标点红利用上，同一段从 0.3559 回升到 0.7504（另一处记载：只跑脚本 0.6405，脚本加语感改写 0.7671）；且执行一次要十分钟。用户明确要求「按五条重写」时才从 L1 往 L5 推进，每层留改前改后对照。

### 改了什么

- `SKILL.md`：新增「加引号」与「语感五条：破式五层法」两节；走法由六步改八步（第 4 步 agent 加引号，第 5 步脚本收尾，第 6 步自检语感）；红线重写为 8 条，第 1 条明确「只有加引号这一步允许动正文，且只许插「」两个字符」；资源表把 `banned-words.md`、`structures.md`、`examples*.md` 重新挂上用途，不再是「无调用方」。
- `skill.json`：version 7.1.0 → 7.2.0；description 末句由「不改用词与句式」改为「默认只动标点与引号」（按五条重写时会动到用词，原文表述已不成立）。
- `scripts/deai_gate.py`：**未改动**。v7.1.0 的 `--names`／`--name-marks`／`--name-max` 保留，定位由「唯一做法」降为「省事档」。

## v7.1.0 | 2026-09-21

### 引号口径改向：不再全量转直角引号，改成给主角与重要角色加引号

**实测依据**（老板在场逐轮实测，七份验证记录在 popwave 仓 `docs/PRD/储备需求/PRD-desktop-0020-humanize-rewrite/`）：

1. **全量转直角引号这条站不住。** 扫 87 部真人网文语料：拿「」当对话引号用的 **0 部**；拿「」做着重号（对话仍用“”）的 32 部；全库引号总量里直角只占 10.10%。也就是说「对话全变直角」是真人不会写的形态。
2. **改成只给术语与名字加「」，效果与全量转引号相当。** 整章 2077 字：原文 0.8078 → 只给术语加「」得 0.6047（−0.203），全量转引号得 0.6116（−0.196），而前者只动 6 处。
3. **只测人名这一个动作、七份稿子**：五份响应（降 0.04 到 0.42），两份标满 32 到 34 处仍只降 0.02。「这篇吃不吃这一手」事前看不出来。
4. **密度**：标 2 处（约 9.6/万字）完全无效，标 8 处起效；只标出现最多的那**一个**人名（18 处集中在一个人名上）效果最好，比标满三个人名 36 处还多降 0.04。
5. **分句不作为方向**：保守分句三档（切 >40 / >28 / >20 字的句子）五章实测降分章数 0/5；重度档把平均句长从 23.6 压到 15.8、句数涨 49%，仍不降分。

### 改了什么

- `scripts/deai_gate.py`：撤掉「弯引号 → 直角引号」的逐行状态机（原 14b）；新增 `add_name_marks()`，给 `--names` 给出的名字均匀挑几处包上「」（新 14c）。新增三个参数：`--names`、`--name-marks`（默认 4）、`--name-max`（默认 12）。
- `SKILL.md`：走法由五步改六步，新增第 3 步「取名单」（从章纲或角色卡读出主角与重要角色姓名，只取姓名，不读正文）；新增「五原则」一节；「标点与字形」一节补记引号口径的例外；红线增至 8 条。
- `skill.json`：version 7.0.3 → 7.1.0；description 补「给主角与重要角色加引号」。

### 没改

`--fix` 的其余标点与字形规则、检查项与阈值、报告格式未动。**不给 `--names` 时脚本不做任何加引号动作**（实测：弯引号 11 个原样保留，直角引号 0 个）。

### 已知代价

名字落在对话引号内部会读成「……「某某」……」。先接受；要改只需把入引号换成直角单引号『』，一处代码。

### 「五原则」的来源

本版写进 SKILL.md 的五原则取自本包自己的历史，不是新拟：前三条是脚本 `CHECKS` 里既有的「长句串动作／同义短语重复／情绪直说」（v6.0.0 由八条规则收成三条），后两条取自 v6.2.0 的质量校验第 2 道与 v6.0.0 的定位。**若与老板记忆里的那套不同，以老板给的为准，改 SKILL.md 那一节即可。**

## v7.0.3 | 2026-09-21

**跨包引用与画风 DNA 库文件名收口。**

- 跨包引用统一改 `<包名 包根>/...` 写法：原来写的 `skills/<包名>/...`、`../其它包/...`、裸 `pop-xxx/references/...` 在用户机器上都解析不到。
- `文风DNA-library.json` 这个文件名在本仓不存在，真实文件是 `style-dna-library.json`，全部改回真实名。

无内容语义变更，只改路径写法。

## v7.0.2 | 2026-09-21

**脚本路径与参数表执死。**

### 为什么

消耗异常排查（小说项目 111，run `0e88608d`）实测：模型先试 `deai_gate.py --prof`，报 `error: unrecognized arguments: --prof`；再反复搜脚本位置。两处都不是脚本逻辑问题，是技能文档没给出包根解析方式与参数表。`--profile`／`--prof` 已于 2026-09-20 移除，但包内仍留着无调用方的 `deai_profiles.json`，模型看到它就猜了 `--prof`。

### 改了什么

- 走法第 3 步的脚本路径改为相对**本包根目录**展开：`python "<包根>\scripts\deai_gate.py" <文件> --fix`。包根 = 系统提示里 `--- skill: /pop-ai-reduce-lite (<包根>) ---` 括号内的路径（命令行等价于 `--skill`）。原文写的是 `skills/pop-ai-reduce-lite/scripts/deai_gate.py`，任何用户机器上都不存在。
- 新增「脚本位置与参数」一节，列出参数表：位置参数 `<文件>`、`--fix`、`--json`、`-h/--help`，并明示 `--profile`／`--prof`／`-r` 不存在，不要试。
- 资源表新增一行，标注 `scripts/deai_profiles.json` 已无调用方，不得当参数依据。
- `scripts/deai_gate.py`：找不到文件时，报错附带脚本实际查找的完整绝对路径与用法，并指向 `--help`。退出码仍为 2。

### 没改

`--fix` 的标点与字形规范化规则、检查项与阈值、报告格式均未动。

## v7.0.1 | 2026-09-20

改 description：补触发条件、删内部实现说明（原 73 字 → 79 字）。不涉及规程与产出变更。

# CHANGELOG — pop-ai-reduce-lite

## v7.0.0 | 2026-09-20

### 砍到只剩标点脚本：语感层整体撤掉

**实测依据（同一章 `9-18-项目a` 正文）**：

| 稿件 | AIGC 值 | 首段 |
| :-- | :-- | :-- |
| 原文 | 0.8691 | 0.8492 |
| 旧 skill 产出（脚本 ＋ 语感改写） | 0.7671 | 0.7519 |
| **只跑脚本** | **0.6405** | **0.3350（人工带）** |

语感改写把红利吃掉 0.1266。加上它实测对朱雀分数无贡献（字词层 ±0.0000），且执行一次要十分钟（模型整章改写加多轮工具调用），收益为负、成本极高，故整体删除。

**删除**：三条语感规则、质量校验三道（命中数／内容守恒／盲评）、走法里的改写步骤、相关红线与资源引用。

**保留**：`deai_gate.py` 的 `--fix`（标点与字形规范化）。这一步不经过模型，耗时约 1 秒。

**顺带查出**：旧 skill 产出的 99% 是显示口径问题，不是脚本没跑。该稿 `softmax_confidence` 为 0.9999（应用界面显示的就是它），而它的真实 AIGC 值是 0.7671。`detect.ts` 取错字段这件事仍未修，属产品侧需求。

**skill.json**：version 6.2.0 → 7.0.0。

## v6.2.0 | 2026-09-20

### 新增质量校验：接受分数小回升，但质量必须被验证

**定位明确**：语感改写只服务阅读质量，对朱雀分数无贡献（实测字词层 ±0.0000，整段语感改写 +0.029），这条代价已知且接受，写进 SKILL.md 让读者不再误判。

**新增「质量校验」一节**，三道校验，任一条不过就回退语感改动、只交脚本结果：

1. 命中数必须降：改写前后各跑一次脚本，长句串动作／同义短语重复／情绪直说三项命中数都不高于改前，否则那条规则没生效。
2. 内容守恒：字数波动 ±10% 以内，人名地名道具名数字一个不丢，不新增设定与回忆。
3. 盲评：改前稿与改后稿交独立评审，不给顺序提示，让它选阅读体验更好的一份并说明理由；选改前稿就整段回退。

**回升上限**：允许分数回升，上限 0.05；超过则语感改动整段回退，只保留脚本的标点结果。

**走法**加一步「先留改前稿」，为校验与留档提供对照；红线增至 8 条，新增「质量校验没过就回退，不许把没验过的稿子交出去」。

**skill.json**：version 6.1.1 → 6.2.0。

## v6.1.1 | 2026-09-20

### 修走法顺序：脚本挪到改写之后

原走法是「先跑脚本、再改写」，这是把红利盖掉的顺序——模型改写时会重新打出弯引号、冒号、破折号。改为：读输入 → 改写 → 跑脚本收尾 → 写回留档 → 回报，并注明顺序不能反。

**skill.json**：version 6.1.0 → 6.1.1。

## v6.1.0 | 2026-09-20

### 标点规范化按语文逻辑重做，实测降幅 −0.2286

**背景**：多轮单变量实测确认红利全在标点层——引号字形（−0.10 到 −0.20）、破折号→逗号（−0.0483）、冒号→逗号（−0.0349）、括号→逗号（−0.0166）；字词层（书面连词口语化、其→他、分号、叹号）**全部为 ±0.0000**，即朱雀不看用词。粗暴全量替换可拿到 −0.2435，但会产生列举冒号被改成逗号这类语病。

**改动**：`deai_gate.py` 新增 `fix_punct_norm`，按语文规范限定边界（591 → 770 行）。引号一律转直角引号；破折号只改解释说明与成对插入语，**保留**声音延长、被打断、列举与总结前用法；冒号**只改对话提示语**，保留列举与总说冒号；括号**只改 2 到 8 字的短注释与同位语**，保留长补充说明。删掉把半角 `--` 转成 `——` 那一项（实测是负功）。

**实测**：`9-18项目a` 正文 0.8691 → **0.6405（−0.2286）**，首段进入「人工」带（0.3350）。合规化相比粗暴全换（0.6256）只损失 0.015，换掉的是肉眼可见的语病。

**另一发现（撤回旧结论）**：`旧日书` 亦为 AI 书，非人书。此前以它为「人书对照」得出的「人书也压不到 0.5」不成立，已撤回。手头暂无真人写作样本。

**skill.json**：version 6.0.0 → 6.1.0。

## v6.0.0 | 2026-09-20

### 大幅简化：按「降分 ＋ 少量语感 ＋ 快」重定位

**定位变更（老板拍板）**：去AI味现阶段不追求真正跑通，只要三件——标点与字形归位（主力）、少量语感改写、速度快。原来的八条规则与整套检查面按此收缩。

**SKILL.md 重写**：八条规则收到三条（长句串动作、同义短语重复、情绪直说）；走法由七步收到五步；删掉强度分级、前后对照、脚本预扫步骤，以及三条铁律的展开。

**脚本精简（1648 → 591 行）**：删掉 338 条套话与软信号词库及其检查；删掉只报数的统计项（句长CV／段长CV／TTR／顿号密度／成语连排／意象连排／抽象标签／语气句／标点节奏CV／语气词密度）；删掉 `--profile` 参数与 profile 装载、递归 `-r` 与聚合 summary。报告只留「位置 ＋ 命中 ＋ 建议」。`--fix` 的行为逐字节不变。

**skill.json**：version 5.2.0 → 6.0.0。

## v5.2.0 | 2026-09-20

### 砍掉 doc profile：脚本 1648 → 1393 行，配置 747 → 328 行

**改动**：`deai_gate.py` 的 `doc` profile 整块移除。它的调用方（seed／stage／plot／outline／review／research／decon／dna-style／critic-panel）在 v5.1.0 已全部摘除，此前只剩脚本自己的文档里还提它，属死代码。删掉 `DOC_NAMES`、`DEFAULT_DOC_CFG`，以及各函数里的 doc 分支与参数、CLI 取值；`deai_profiles.json` 的 `doc` 段一并删除。文件头版本改为 v3.5.0，自称由「去AI味门禁」改为「正文检查与标点规范化」。

**body 路径行为不变**：改前改后对 3 份真实章节跑 4 种命令，stdout／stderr 逐字节一致，`--fix` 后的文本 SHA256 一致，80 项检查的名称、顺序、取值、判定、阈值全部相同。仅 4 处与 doc 相关的文案随之消失（不带 `--fix` 的提示行、空目录错误提示、`--help`、`--profile doc` 由可用变为被拒绝）。

**skill.json**：version 5.1.2 → 5.2.0。

## v5.1.2 | 2026-09-20

### 随包文件清理：只留运行与维护必需的内容

**改动**：内部维护记录（来源标注、改动项清单、外部依据）从本包移出，另存到不随包发布的归档目录 `temp/pop-ai-reduce-lite-内部记录-20260920/`；本包内只留改动与用法说明。涉及文件：`CHANGELOG.md`（本文件）、`scripts/deai_gate.py` 的注释与提示文案、`scripts/deai_profiles.json` 的说明、`prd/` 两份档案、`resources/banned-words.md`、`resources/examples.md`、`SKILL.md` 的两条红线。

**脚本行为未变**：改动只落在注释与文案。代码 token 逐条比对完全一致；同一份样例修复后字节相同，80 项检查的名称、取值、阈值、判定、退出码全部一致。

**skill.json**：version 5.1.1 → 5.1.2。

## v5.1.1 | 2026-09-20

### 标点那一轮收进脚本，文档不再列改动项

**改动**：`SKILL.md` 的「标点规范化」一节删掉逐条清单，改成一句「跑一次脚本，动哪些项由脚本定，本文件不逐条列，也不在别处解释」。走法第 4、5 步、红线、以及「一次交付做完两件事」里点到具体标点项的地方一并撤下，交给脚本。新增一条：这一轮的口径不写进回执，也不讲给作者听。`skill.json` 的描述同步去掉同类字句。

**skill.json**：version 5.1.0 → 5.1.1。

## v5.1.0 | 2026-09-20

### 三套写作链路摘除去AI味环节，本 skill 收成唯一宿主

> **背景（老板拍板）**：snow／lantern／emergent 三套专家链路此前各自带一套「存档质量检查」，由 `deai_gate.py` 执行，并要求每个 skill 存档后回报一行确认。去AI味收口到本 skill，链路上不再有这一环。

**改动**：

- **脚本换宿主**：`deai_gate.py` 与 `deai_profiles.json` 由 `pop-snow-pipeline/scripts/` 移到 `pop-ai-reduce-lite/scripts/`，原目录删除。脚本按自身目录读配置，搬迁后功能不变，已冒烟验证。
- **三套链路摘除**：`pop-snow-*`、`pop-lantern-*`、`pop-emergent-*`、`pop-write`、`pop-review`、`pop-outline` 共 15 份 SKILL.md，删掉「存档质量检查」整节、速查表里的脚本行、以及「存档后执行检查」与「回报确认行」的全部要求。写作质量要求（不写套话、不写 AI 腔）保留，只把措辞改成直述。另清理 `pop-critic-panel`、`pop-decon`、`pop-decon-dimension`、`pop-dna-style` 里各一条同类条目。
- **新增「一次交付做完两件事」**：语感改写与标点规范化必须在同一次交付完成，只做一件算半成品。
- **skill.json**：version 5.0.0 → 5.1.0；description 同步；slashCommands 去掉一个把判定口径写在用户可见菜单里的触发词。

## v5.0.0 | 2026-09-20

### 主干重写：废早期的外部包流程，换三条铁律 ＋ 八条规则 ＋ 标点规范化

> **根因（老板拍板）**：v4.0.0 采用外部社区包流程的判断被实测推翻——那套「往口语化、往啰嗦改」的方向在真实正文上要么不达预期要么反向，且必然牺牲质量；旧流程还贵，8 步脚本流水线每跑一次要多次调模型。

**改动**：

- **SKILL.md 主干重写**：废掉外部包原文（步骤 0-4、四步排雷法、破式五层法、A/B/C 三模式、风格注入规则），换成三条铁律 ＋ 八条规则 ＋ 一组标点规范化机械项。
- **规则来源收口**：八条规则只写进 SKILL.md 一处；标点规范化交给 `deai_gate.py --fix`（机械项，零 LLM 成本），不再由规则 JSON 驱动模型自算。
- **保留**：三条铁律（立场守恒／血肉不丢／篇幅容差）、抽象对偶比喻（降为附则）、`banned-words.md`、`examples*.md`、`structures.md`。
- **旧资源已物理删除**：8 步流水线脚本、`zh_rules.json`、`zh_rules_archived.json`、`synonyms.json`。原稿另存归档。
- **标点口径统一到一处**：`deai_gate.py` 的同类处理收敛为单一方向，相关判据文案同步对齐，改动面最小。
- **三种调用场景写死**：管线内（write 之后，写回 ＋ 留档 ＋ 回执）、独立调用（功能区／外部工具，给文本）、只查不改。
- **skill.json**：displayName 与 description 全部改写为新定位；version 4.1.0 → 5.0.0。

**实测依据**：本轮改动据以成立的实测记录不随包发布，存于 `temp/pop-ai-reduce-lite-内部记录-20260920/`。

**待办（未完成，勿当作已做）**：

- 调用点收口：`pop-decon`／`pop-dna-style`／`pop-critic-panel`／`pop-snow-*`／`pop-lantern-*`／`pop-emergent-*`／`pop-outline`／`pop-review` 里各自散落的规则表述与词库来源，改指本 skill 与 `deai_gate` 正本。
- `prd/PRD.md` 与 `prd/进度与待探索.md` 的设计预期等四节，按 v4.1.0 的约定填实。
- 回归：阈值调校所需的样例回归尚未跑。改 `deai_profiles.json` 之前必须先跑。

## v4.1.0 | 2026-09-13

### 新增 `prd/` 骨架（设计档案）

`pop-shared-skill-create` v9.0.0 把 `prd/`（设计预期／解决思路／进度／后续方向）立为 skill 标配，本 skill 先落两份骨架：`prd/PRD.md`、`prd/进度与待探索.md`。

**内容状态**：设计预期、解决思路、边界、关键取舍四节标「待补（2026-09-13）」，等下一次改到这个 skill 时填实。**不编设计预期**——编出来的比没有更坏，下一轮会拿它当依据。

## v4.0.0 | 2026-09-02

### 整包换用外部社区包的实现

> **根因**：对比评测后老板拍板——外部包的检测与改写能力全面强于自研 lite 的表层规则降噪。与其在自研浅层上补短板，不如整包采用成熟实现。维持「非 snow 默认流程」定位不动。

**改动**：

- **实现整包替换**：本目录清空旧实现，铺入外部包全部内容（SKILL.md / resources 规则库 / scripts Python 检测改写引擎）。
- **去掉两级闸门**：删除原「表层降噪→表层后询问→路由深度技法」链路，改为一次到位。
- **skill.json 重写**：id 与中文触发词保留；version 3.0.0 → 4.0.0。
- **脚本运行时**：由 Node(.mjs) 切换为该包的 Python 引擎；依赖 Python ≥3.8。
- **旧实现已备份**：`temp/backup_pop-ai-reduce-lite_20260902_170039/`（project-source + runtime-copy 双份）。

---

## v3.1.0 | 2026-08-31

### 去AI味
- 「交互闭环」改为「在同一流程内完成」，去空洞名词
- 同步 skill.json（version）

---

## v3.0.0 | 2026-08-24

### steps 单件全合入 SKILL.md 单文件精炼

> **根因**：实测 step 文件在当前 harness 从未被加载/Read（子agent注入链断在骨架层）。参考 write/pipeline 改造模式合入主文档。

**改动**：
- **steps/ 目录删除**：pipeline-execute.md 全文合入 SKILL.md「四步执行管线」节（Step 1-4 执行细节+词表+示例全保留）
- **执行模式明确**：主agent直执——单章4步改写+脚本验证+表层后询问用户是同一交互闭环，无自然子agent适配点
- **内容精炼**：字数保留率回查规则收敛进红线第 5 条（补上step文件中"重点查Step 1/Step 4过度删除"的指向）；速查表三条全部并入「输出与验证」节正文（去独立表）；加载门禁节随链式加载架构废除删除；表层后询问模板压缩（提醒两点+选项保留）
- skill.json version 2.4.0→3.0.0

---

> 历史版本条目已归档：`temp/backup_pop-ai-reduce-lite_20260902_170039/project-source/CHANGELOG.md`
> 本轮撤下的内部维护记录：`temp/pop-ai-reduce-lite-内部记录-20260920/`
