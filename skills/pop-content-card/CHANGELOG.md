# CHANGELOG — pop-content-card

## v4.0.1 | 2026-08-31

### 去AI味 + 文档瘦身
- SKILL.md 版本节版本历史解耦，仅留当前版本 + 指向 CHANGELOG
- 同步 skill.json（version）

---

## 4.0.0 - 2026-08-24

### steps 两件全合入 SKILL.md 单文件精炼

> **根因**：实测 step 文件在当前 harness 从未被加载/Read（子agent注入链断在骨架层）。参考 write/pipeline 改造模式合入主文档。

**改动**：
- **steps/ 目录删除**：step1-content / step2-visual 两件全合入 SKILL.md「怎么操作」节
- **执行模式明确**：主agent直执——素材内容生产与模板填充是连续创作链（68字段JSON直接喂Step 2，中间卡8项校验门禁），无自然子agent适配点
- **内容精炼**：68字段JSON模板从逐字段代码块压缩为分区块占位符表（68字段名与四层标注全保留，校验门禁引用字段名不失真）；风格池表去重（step2重复表删除，仅留SKILL.md一处）；step链式加载门禁节废除
- skill.json version 3.3.0→4.0.0

## 3.3.0 - 2026-08-13

- skill.json 的 description 改为面向用户介绍、tags 改为可调用专家标签、版本号同步至 3.3.0。

## 3.2.0 - 2026-07-24

- 三层×三维度对齐：每层显式回答"定位/什么是好的/如何写出好的"，且"好"的标准从三可（可截图×可套用×可回看）贯穿传导，不衰减。
- **SKILL.md**：新增「三层×三维度对齐矩阵」总纲，定义质量传导链：三可 → 68字段×8校验 → 版式呈现。
- **Layer 1 内容方法论**：开头加定位声明（质量源头）；自检清单8项各加「对应三可」列，标注每项检查服务哪个可。
- **Layer 2 信息架构**：step1开头加定位声明（把三可传导为JSON）+校验清单8项加〔对应三可〕标注；step2开头加定位声明（把JSON传导为0.5秒可感知的视觉）+完成检查8项加〔对应三可〕标注。
- **Layer 3 视觉模板**：visual-density开头加定位声明+「什么是好看的模板」（让三可被0.5秒感知）+模块组合表加「服务三可」列；master-template.html文件头注释加定位声明+模块→三可映射（可截图→cover-hook+quote-stack / 可套用→formula-card+contrast+ending-card / 可回看→scene-paper+ending-grid+signoff）。

## 3.1.0 - 2026-07-24

- 三层对齐重构：内容方法论、信息架构、视觉模板从各自维度调好并对齐。
- **Layer 1 内容方法论**：修复stale"9页"引用×2处；新增第七节"产出格式与字段清单"（60内容字段映射HTML占位符）；新增第八节"产出后自检门禁"（8项检查，任一不通过禁止进入模板渲染）。
- **Layer 2 信息架构**：step1从Markdown输出改为JSON输出（68字段完整模板+8项校验门禁）；step2加填充前字段检查+填充后残留检查。
- **Layer 3 视觉模板**：素材句加感官通道标签（触觉/物件/动作/信息，data-tag+CSS差异化）；正误对照加✓/✗视觉标识+好例渐变背景+坏例斜纹底纹；封面加标题装饰光晕；公式卡加渐变背景+底部装饰线；场景纸加引号装饰+渐变底色+顶部装饰条。
- 新增 `templates/html/example.html`：用真实内容（破镜重圆主题）填充的完整参照，68字段全部替换0残留。

## 3.0.0 - 2026-07-24

- 9页→5页重构：从"满页堆量"转向"少而精"。
- content-method.md 从第一性原理重写：定义"好素材卡 = 可截图 × 可套用 × 可回看"。
- 4种素材类型差异化5页结构：桥段/字句/人设/写法各有专属页面分配。
- 素材句三层结构标准：物件/动作 + 感官细节 + 情绪暗示。
- 封面钩子句标准：具体物件/动作 + 情绪张力。
- 名场面必须包含"选择"和"代价"。
- 母版从9页缩减为5页：封面→公式+素材句→正误对照→名场面→收束地图。
- 删除9页结构中的账本/压力轴/微卡等冗余模块，聚焦核心内容。
- SKILL.md 新增素材类型与5页结构对照表。
- step1/step2/visual-density 全部同步5页结构。

## 2.0.0 - 2026-07-24

- 引入 html-anything 锁死版式池理念：版式结构锁死、色板锁死、铁律锁死。
- 配色系统重构：从注释切换改为 body class 切换，6套锁死色板（暖纸/墨韵/青碧/赤金/暮紫/极夜）。
- 新增暮紫调（奇幻/修仙）和极夜调（暗黑/无限流）两套深色风格。
- 极夜调深色适配：页码、对照卡、工具栏等硬编码颜色覆盖。
- 母版头部加入4条铁律注释，防止 agent 自由发挥破坏设计。
- SKILL.md 升级：风格池速查表 + 铁律表（含违反后果列）。
- step2-visual.md 升级：配色选择从"改 :root 变量"改为"切 body class"。

## 1.0.0 - 2026-07-24

- 从 maodemi-content-studio v1.0.6 通用化改造。
- 去个人IP：账号名/署名/收尾语/配色/头像/贴纸全部参数化为占位符，用户按需填入。
- 内化素材生产方法论：不再依赖外部"知夏"skill，自包含素材分类+数量选择决策+9页文案组织。
- 新建通用HTML母版 `templates/html/master-template.html`：覆盖全部模块类型（公式卡/引用栈/账本/时间轴/正误对照/场景纸/结局卡/清单网格/素材地图），配色用CSS变量参数化，提供4套预设方案。
- 保留满页密度规范、页脚安全区、短句不断行等通用经验。
- SKILL.md 按Popwave Skill设计规范重写，≤100行，SOP骨架+4条红线+全文件速查表。
