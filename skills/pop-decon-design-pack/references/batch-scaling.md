# 设计包批量处理：30章合并批处理策略

> 适用场景：拆书管线 Phase 1 逐章提取。v6.2.0 起双模式统一采用 **30章合并批处理**：每批读入 30 章原文，一次 API 调用产出 30 张白描卡/设计包，降低 API 调用成本约 30%。
>
> ⚠️ **上下文预算硬约束（v6.2.0+）**：30章合并的输入 ~100-150K 字符（约 30-45K tokens），需使用 DeepSeek 长上下文模型（deepseek-v4-flash）。每批输出 30 张卡，fast 模式 ~10K 字符，precision 模式 ~30-60K 字符。**批大小超过 30 章将导致后段输出质量下降**，详见 `references/slim-card-format-spec.md`。

## 双模式策略选择

| 模式 | 处理方式 | 187章耗时 | 压缩比 | 适用场景 |
|:-----|:---------|:---------|:------:|:---------|
| **precision** | `slim_card_batch.py --mode precision` 30章合并 | ~10-15 分钟 | 80-100% | 精品拆书/prose-render直接消费 |
| **fast** | `slim_card_batch.py --mode fast` 30章合并 | ~3-4 分钟 | ~11% | 大规模拆书/快速验证/全书骨架 |

**选择规则**：章数 > 100 且不需要 prose-render 直接消费 → fast；否则 precision。

## 统一处理方式（双模式）

两个模式均使用同一个脚本 `scripts/slim_card_batch.py`，仅 `--mode` 不同：

```bash
# fast（瘦身白描卡，4段式）
python scripts/slim_card_batch.py --input "{书名}.txt" --output "写作资产/白描卡/" --mode fast --batch-size 30

# precision（v4设计包，3层+1区）
python scripts/slim_card_batch.py --input "{书名}.txt" --output "写作资产/设计包v4/" --mode precision --batch-size 30
```

### 批次数计算

```python
import math
total_chapters = 187
batch_size = 30  # 默认30章/批
batches = math.ceil(total_chapters / batch_size)
# 187 / 30 = 7 批（最后一批可能不足30章，直接按实际章数处理）
```

### 批次对齐规则

- 前 N 个完整批次：30 章/批（ch001-030, ch031-060, ...）
- 最后一批可能不足 30 章（ch181-187），直接按实际章数处理
- 不跨卷对齐（卷边界由 Phase 2 识别）

### 脚本内部流程

1. **读取全文**：自动编码检测（gbk→gb18030→utf-8→utf-8-sig→big5）
2. **按章分割**：正则 `第(\d+)章` 识别章节边界
3. **分批**：每 30 章为一批，合并原文为单一 prompt（`===== 第N章 标题 =====` 分隔）
4. **并发调用**：每批 1 次 API，`--workers` 并行批数（默认 3）
5. **解析产出**：按 `# chXXX「标题」`（fast）/ `# 设计包 — chXXX「标题」`（precision）标记拆分，逐章写入独立文件
6. **汇总报告**：输出缺失章节清单，供重跑

## 命名归一化（precision mode）

脚本按标记拆分自动写入 `chXXX-设计包.md`，但若模型输出标记漂移，可能出现命名异常。全部批次完成后，必须执行一次目录扫描和统一：

```bash
# 1. 扫描所有异常命名变体
ls 写作资产/设计包v4/ | grep -vE '^ch[0-9]{3}-设计包\.md$'

# 2a. 处理下划线变体（ch036_v3设计包.md）
mv ch036_v3设计包.md ch036-设计包.md

# 2b. 处理 -v3 后缀变体（ch171-v3设计包.md）
mv ch171-v3设计包.md ch171-设计包.md

# 3. 最终验证
for i in $(seq -w 1 $TOTAL); do
  if [ ! -f "ch${i}-设计包.md" ]; then
    echo "MISSING: ch${i}"
  fi
done
```

**⚠️ 常见错误命名变体：**

| 变体 | 示例 | 根因 | 预防 |
|:-----|:-----|:-----|:-----|
| 下划线分隔 | `ch036_v3设计包.md` | 模型未按标记拆分 | 脚本解析或后处理归一化 |
| -v3 后缀 | `ch171-v3设计包.md` | 模型输出格式漂移 | 标准化断言 |
| 多章合并为单一文件 | `ch136-140-设计包.md` | 模型将多章合并输出 | 脚本按标记拆分，缺失则重跑 |

## 质量注意事项

| 风险 | 对策 |
|:-----|:------|
| 30章合并后段质量下降 | 批大小 ≤30；超长卷拆分批；抽检后10% |
| 模型输出遗漏章节 | 脚本解析比对，missing 章节重跑 |
| 命名漂移 | 脚本按 `# chXXX` 标记拆分 + 后处理归一化 |
| 并发过高致失败 | `--workers` 默认 3；失败降并发重跑 |
| 上下文预算不够 | 30章 ~100-150K 字符，超长章节可降批到 20-25 |

## 性能参考（实测数据）

### fast mode（30章合并）

| 量级 | 批数 | 每批章数 | 并发批 | 总耗时 | 压缩比 | 实测验证 |
|:-----|:----:|:-------:|:------:|:------:|:------:|:---------|
| 10 章 | 1 | 30 | 1 | ~20s | ~11% | ✅ |
| 30 章 | 1 | 30 | 1 | ~60s | ~11% | ✅ 深渊主宰 |
| 187 章 | 7 | 30 | 3 | ~3-4 分钟 | ~11% | ✅ 深渊主宰第一卷 |
| 678 章（全书） | 23 | 30 | 3 | ~12 分钟 | ~8% | 预估 |

### precision mode（30章合并）

| 量级 | 批数 | 每批章数 | 并发批 | 总耗时 | 压缩比 | 实测验证 |
|:-----|:----:|:-------:|:------:|:------:|:------:|:---------|
| 30 章 | 1 | 30 | 1 | ~3-4 分钟 | 80-100% | 预估 |
| 100 章 | 4 | 30 | 3 | ~10-15 分钟 | 80-100% | 预估 |
| 187 章 | 7 | 30 | 3 | ~15-20 分钟 | 80-100% | 预估 |

**成本对比（v6.1.0 vs v6.2.0）**：

| 维度 | 旧版（单章调用） | 新版（30章合并） | 节省 |
|:-----|:----------------|:----------------|:-----|
| 187章 API 调用次数 | 187 次 | 7 次 | ~96% |
| 单批 overhead（system prompt+指令） | 187 次重复 | 7 次重复 | ~96% |
| 估算 API 成本 | 基准 | 约 70% | ~30% |
| 187章耗时 | ~3 分钟（10并发） | ~3-4 分钟（3并发批） | 持平 |

## 质量门禁实测数据（30章合并后）

| # | 检查项 | 实测结果 | 说明 |
|:-:|:-------|:---------|:------|
| 1 | **覆盖率** | 30/30 = 100% | 30章合并解析无遗漏 |
| 2 | **命名一致性** | 100% ✅ | 脚本按标记拆分自动命名 |
| 3 | **首行格式** | 100% ✅ | 脚本解析保留 `# chXXX「标题」` 首行 |
| 4 | **结构完整性** | 抽检后段通过 | 30章批内后段无质量断崖 |

**结论**: 30章合并脚本在批内覆盖率/命名一致性上有程序化保证；内容级质量（首行格式/小节命名）需抽检后 10% 章节确认。