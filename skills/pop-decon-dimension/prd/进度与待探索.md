# 进度与待探索 · pop-decon-dimension

## 当前进度

**已完成（带凭据）**

- v3.7.0（2026-09-22）：Step 0 读源包（合并章节索引替代 `_temp/chapters/` 幽灵路径）、Step 4 批次完成标志、红线 8→7。见 `CHANGELOG.md` v3.7.0。
- 补齐本 `prd/`（此前 CHANGELOG v3.6.0 声明「新增 prd/ 骨架」但目录不存在，属历史遗漏）。

**进行中**

- 无。

**已知缺口**

- `references/维度路由清单.md` 是双份维护（入口 `pop-decon` 也有一份同名文件），需人工同步，暂无单一事实源。
- L1 门禁 7 项检查目前是 agent 自检（写进批次档案），尚未下沉为脚本机械核对。`tool-split-chapters` 只做分章/切批，不做门禁计数。

## 后续方向

- **近期**：把 L1 七项门禁的机械部分（证据条数计数、ch 锚点覆盖核对、量化 🔒 缺失扫描）下沉脚本，减轻 agent 自检负担。算做完 = 一批批次档案能被脚本自动判定 passed/failed，与人工判定一致率 ≥95%。
- **中期**：`focusDimensions` 落到 L2 产出顺序与深度的具体规则（哪些模块优先出样板、非重点模块如何降深不降质）。算做完 = 七个标签各有一条明确的产出顺序/深度规则，且不裁剪 L1。
- **长期**：试拆（<20 章）专用质量规则。算做完 = 少于 20 章的范围有一套经评审的产出与验收标准，且不放松锚点池门槛。

## 悬而未决

- 试拆质量规则的设计口径，等产品侧评审（集成计划把它列为首要待评审项）。等谁拍：产品侧。
- 批次完成标志的落盘路径与命名，等桌面侧契约冻结。等谁拍：产品侧（另一个 agent）。

## 踩过的坑

- 版本描述曾错位：SKILL.md 写 v3.6.0 为「去 AI 味检查接线」，实际去 AI 味是 v3.5.0，v3.6.0 是 prd/ 骨架。v3.7.0 已覆盖修正。
- 红线曾 8 条（超规范 ≤7），且第一条不是读取协议。v3.7.0 已收敛为 7 条并补读取协议为首条。
