---
name: pop-decon-volume
description: "Phase 2 of deconstruction: 提取L2剧情单元卡 + 归纳卷纲（含溯源燃料台）。物理结构降级为参考坐标系。"
pipeline:
  upstream: [pop-decon-design-pack, wiki-skeleton]
  downstream: [pop-decon-setting]
  note: "wiki-skeleton 为可选上游。当 pop-decon Step 2.7 产出 _temp/wiki-skeleton.md 时，Phase 2 消费其卷结构作为全局脚手架。"
---

# pop-decon-volume · 叙事结构提取 v4.0.0

> **定位**：Phase 2 of deconstruction. 提取 L2 剧情单元卡、归纳卷纲（含溯源燃料台）。
> 物理结构（卷/幕/段）降级为参考坐标系，不再作为产出层级。
> **格式变更**：v4.0.0 起从「L2 单元卡 / L3 剧情线 / L4 全书事件」三级产出改为「L2 单元卡 / 卷纲（含溯源燃料台）」双轨产出。
> **方向**：逆 `plot` + `pop-writer-chapter`。只聚类原文已呈现的结构，从不「设计」剧情。

---

## 层级定义（L2 单元卡 vs 卷纲）

**决定层级归属的核心判据不是物理篇幅，而是叙事结构的抽象层级。**

| 层级 | 功能 | 典型规模 | 核心边界信号 | 物理坐标（参考） |
|:-----|:-----|:--------|:------------|:----------------|
| **L2 剧情单元** | 一个有始有终的行动弧，内部含嵌套子线 | 5-30章 | ①穿越入口/出口 ②地点切换 ③力量跃迁 ④叙事基调转变 ⑤事件类型突变 ⑥POV 变化 ⑦**过渡/收束章**：当一章的功能是「战后复盘+权力碾压展示」而非「推进新冲突」，它通常是前单元的终点而非后单元的起点 | 对应原书的「幕」或「跨幕弧线」 |
| **卷纲** | 从 L2 单元卡逆向归纳的卷级结构，含溯源燃料台 | 对应原书的一卷或多卷 | ①多个 L2 单元在同一物理卷内连续 ②卷末有叙事弧收束或全局质变 ③可从 L2 卡的嵌套子线「延伸」标记识别跨卷方向 | 对应原书的「卷」 |

**物理结构降级说明：**
- 原书的「卷/幕/段」不再作为产出层级，仅作为 L2 单元卡的「物理坐标」参考字段
- 一个 L2 单元可能跨越原书的多个幕，也可能小于一个幕
- 卷纲从 L2 单元卡逆向归纳，不直接从原文物理卷结构推导

**实际操作原则：**
- 当你在设计包中看到"主角升级了" → 这是 L2 单元内事件
- 当主角的**战力定位**从"城市级冒险者"变成"区域级战力" → 可能是 L2 单元边界
- 当多个 L2 单元在同一物理卷内连续，且卷末有叙事弧收束 → 归纳为一卷的卷纲
- **溯源燃料台**只在卷纲级产出，用于记录「原文段落→猜测来源→置信度」

**scope 声明（❗必须执行）：**
所有产出文件必须在首行之后添加以下声明：
```markdown
> ⚠️ **基于前X章数据推断**：本章只读到了全书的一部分。
> 所有后续结论（卷纲结构/跨卷主题线/套路偏好）均基于此有限窗口，
> 不代表全书终态。如需更新，可在后续章节补充后修正。
```
其中 X = 实际读到的章节数。

---

## 前置条件

- Phase 1 已完成：`写作资产/设计包v4/chXXX-设计包.md`（或 v2 格式）存在
- `_temp/chapter-index.json` / `plot-data.json` 就位
- **（可选）** `_temp/wiki-skeleton.md` — Wiki 全局骨架，为 L2 单元边界识别提供全局脚手架
> 文件不全 → 退回 Phase 1。
> Wiki 骨架缺失 → 不阻塞，照常基于设计包聚类。

---

## 步骤速查

| 步骤 | 操作 | 读什么 | 产出 | 门禁 |
|:----:|:-----|:-------|:-----|:-----|
| 1a | **L2 边界预扫描** | 设计包标题/元数据（每章前20行：章名、核心行动、章末钩子） | `步骤1产出/L2-unit-segmentation.md` — 边界信号表 + 提案分段 | 边界必须有跨章证据；提案覆盖全部章节范围 |
| 1b | **L2 剧情单元生产**（并行） | 步骤1a的边界提案 + 各批次设计包全文 | `设计/L2单元卡/L2-{编号}-{名称}.md` | 每卡结构分析表完整；嵌套子线逐条产出；**批产量 ≤3 卡/轮** |
| 2 | **卷纲归纳**（含溯源燃料台） | 所有已产出的 L2 单元卡 | `设计/卷纲/卷N-卷纲.md` | 从 L2 卡逆向归纳卷级结构；溯源燃料台4类溯源完整；幕序列含L2卡映射 |
| 3 | **跨卷主题线追踪** | 所有卷纲 + L2 单元卡 | `设计/跨卷主题线.md` | YAML 极简格式；跨卷范围明确；承载 L2 卡编号 |
| 4 | **套路聚合** | 扫描 `写作资产/套路库/` | `设计/卷纲/` 套路分析段 | 套路库为空时跳过 |
| 5 | **L2卡+卷纲入库**（到 pop-trope-library） | L2 单元卡 + 卷纲 | `pop-trope-library/剧情库/{书}/L2/...` + `.../卷纲/...` | 使用 heredoc 复制文件（避免 read_file 行号污染）；多卷书副卷加 `-v{卷号}` 后缀；更新 00-索引.md |

---

## 产出目录树

```markdown
设计/
├── L2单元卡/
│   ├── L2-001-{单元名}.md         ← Markdown，L2 剧情单元卡（物理坐标+结构分析+嵌套子线+可迁移要点）
│   ├── L2-002-{单元名}.md
│   └── ...
├── 卷纲/
│   ├── 卷1-卷纲.md                ← Markdown，卷纲（本卷定位+舞台+幕序列+溯源燃料台+事件种子池+核心高潮点+篇幅预算）
│   ├── 卷2-卷纲.md
│   └── ...
└── 跨卷主题线.md                  ← YAML 极简格式，跨卷主题线追踪

写作资产/
└── 剧情库/                        ← Step 5 产出（可迁移单元/卷纲入库）
    ├── {标签}/
    │   ├── {书}-L2-{编号}-{名称}.md   ← 可迁移 L2 单元卡
    │   └── {书}-卷纲-{卷号}.md        ← 可迁移卷纲
    └── ...
```

**命名规范（所有子agent严格执行）：**
- L2 单元卡文件：`# L2-{编号} · {单元名}（chXXX-chYYY）` — 文件名为 `L2-{编号}-{单元名}.md`
- 卷纲文件：`# 卷{N} · {卷名}` — 文件名为 `卷N-卷纲.md`
- 跨卷主题线文件：`设计/跨卷主题线.md`
- 剧情库文件：`{书}-L2-{编号}-{名称}.md` / `{书}-卷纲-{卷号}.md`
- 编号从 001 开始，按章节顺序递增

**delegate_task 时的格式锁定：**
委派子agent 时，必须在 context 中明确给出 heading 格式，例如：
```
产出文件首行必须严格为：「# L2-001 · 少林蛰伏与因果了结（ch001-ch011）」
禁止使用「单元1:」「unit 1」「chapter 1」等变体
```

### 🧠 递送上下文增强策略
当委托 L2 单元卡生产时，**子 agent 需要比 heading 格式更多的引导**。经验表明，以下上下文显著提升产出质量：

1. **提供预扫描的边界信号**：在 context 中写明「chN→chM 的边界信号是事件类型突变/场景切换/力量跃迁 X」—— 子 agent 有了边界锚点后不会自行位移
2. **提供提案的阶段递进骨架**：例如「本单元的阶段预估：铺垫(chA)→推进①(chB-C)→触发(chD)→高潮(chE)→余波(chF)」—— 骨架降低子 agent 从零构建结构分析的成本
3. **说明本单元叙事类型**（建置型/对抗型/探索型/关系型/复合型）—— 不同单元类型对应不同的阶段模式偏好
4. **指明设计包格式（表格 vs 段落式）** —— 段落式格式会缺少 scene/POV 字段，子 agent 提前知道就不会静默消费（见红线❌10b）

**反模式（已踩过的坑）：**
- 不给边界信号，子 agent 会自行发明边界 → 返工
- 不给阶段骨架，子 agent 会产出过简的结构分析 → 违反红线❌7
- 不给章节 title 列表，子 agent 从设计包索引章节可能出错 → 章节编号漂移
- **L2 卡用中文文件名，子 agent 的 `read_file` 可能失败** — 当子 agent 按 context 中的路径调用 `read_file('.../设计/L2单元卡/L2-004-巫医崛起·医神之路.md')` 时，中文文件名可能导致解析失败。**对策**：在 context 末尾加一行「读取 L2 卡前先用 `search_files(pattern='L2-*.md', target='files', path='{项目根}/设计/L2单元卡/')` 获取实际文件路径，再用 `read_file` 读取 resolved path。不要直接用 context 中给出的文件名路径调用 read_file。」

### ⚠️ read_file→write_file 行号污染陷阱

当向 library（pop-trope-library/剧情库/）复制 L2 卡/卷纲时，**不要使用 `read_file` + `write_file` 通路**：`read_file` 返回的内容带行号前缀（如 `1|content\n2|content`），直接 `write_file` 会将行号写入库文件。

**对策（三选一，按推荐顺序）：**
- **A. terminal heredoc** — `cat > {目标路径} << 'EOF' \n{内容}\nEOF` （最可靠，无编码问题）
- **B. `search_files` + `write_file`** — 先用 `search_files(pattern='*.md', target='files')` 获取绝对路径，再用 `write_file` 写。写完后用 `terminal("head -3 {目标路径}")` 验证无行号前缀
- **C. Python 后处理** — 如果用 `write_file` 写入了带行号的内容，用 `re.sub(r'^\d+\|', '', content, flags=re.MULTILINE)` 修复

**验证命令**：`head -3 {目标路径}` — 首行应为 `# L2-...` 而非 `1|# L2-...`

当分解至最后一卷的最后一个 L2 单元时（卷末单元），在标准 L2 卡内容之外追加以下区块：

1. **跨卷未解钩子汇编**：按类别（战争线/势力线/角色线/世界观·神性线）分类整理所有开放伏笔，标注每条钩子的状态（展开/潜伏/演进中）
2. **卷末身份跃迁核心解读**：主角称号/地位的对比（起点→终点），解析递进逻辑
3. **卷末状态总结表**：所有子线闭合 vs 延伸状态清单，标注「跨卷敞口」
4. **下一卷预测锚点**：基于当前窗口的延伸子线方向，标注哪些主题线明确延续到下一卷

### 🧠 多卷项目目录组织

当项目包含多卷设计包（如 vol1 + vol2），产出的目录组织应遵循以下规则：

```
设计/
├── L2单元卡/          ← 当前精度窗口的**主卷**（最近作业的卷）
├── 卷纲/              ← 同上
├── 跨卷主题线.md      ← 跨卷追踪（由主卷视角识别）
├── 卷1/               ← 其他卷（非主卷）
│   ├── L2单元卡/
│   └── 卷纲/
└── 数值体系反拆/      ← 跨卷共享
```

- 当前正在作业的卷产出放在 `设计/` 根目录，避免路径过深
- 历史卷或非主卷放在 `设计/卷{N}/` 下
- 跨卷主题线优先放在主卷的 `设计/` 根目录下（因为跨卷追踪由主卷视角识别）
- 如果后续切换作业卷，对应目录提升到 `设计/` 根目录，原根目录内容移入 `设计/卷{N}/`

### 🧹 旧格式废弃与归档

当旧格式文件（act-*.yaml / volume-*.md / 卷X.md）需要清理时，按以下流程操作：

1. **创建废弃目录**：在 `vol{N}/废弃文件/` 下创建子文件夹（按文件类型分类：`幕纲旧YAML/`、`旧卷架构/`、`旧剧情单元/`、`旧卷{N}幕纲/`、`旧卷{N}架构/`）
2. **移动文件**：将旧格式文件移入对应子文件夹（使用 `mv`，保留历史版本）
3. **清理空目录**：移除原空目录（`rmdir`）
4. **验证**：确认 `设计/` 下只剩新格式产出，废弃文件在 `vol{N}/废弃文件/` 下可回溯

**⚠️ 注意**：即使已为旧文件添加过 `⛔ 废弃文件` 头注，迁移时仍直接 mv 即可（头注随文件保留）。不需要先移除头注再移动。

---

**⚠️ 结构分析格式漂移陷阱**：当用 delegate_task 并行产出多个 L2 单元卡时，子 agent 倾向于将结构分析表（阶段/功能/章节/事件brief/情感设计/节奏）简化为「阶段一览」或「事件流水账」。此漂移源于子 agent 天然压缩冗余信息的倾向。

**对策（二选一，优先A）：**
- **A. 指令锁定** — 在 delegate_task context 中写明：「结构分析必须按 L2 模板逐阶段产出，每阶段一行含全部字段（阶段/功能/章节/事件brief/情感设计/节奏）。不得将多阶段合并为「阶段一览」或「事件流水账」。嵌套子线必须逐条产出，不得省略。」
- **B. 后处理补充** — 子 agent 先产出单元元数据+结构摘要（节省 token），主 agent 收到后用一轮委托逐阶段补充结构分析+嵌套子线。适用于≥20章的宽单元。

---

## ❌ 质量红线

| # | 红线 |
|:-:|:-----|
| ❌1 | **Phase 1 未完成就聚类** — 设计包不存在 → 退回 |
| ❌2 | **L2 单元边界无跨章证据** — 必须有事件类型突变/场景切换/力量跃迁等跨章证据 → 退回补充 |
| ❌4 | **前N章只做N章** — **每文件首行之后必须**标注「基于前X章推断，后续将修正」 |
| ❌6 | **套路聚合不编造** — 仅从设计包 L2 爽点层读取数据，不得凭空归纳 |
| ❌7 | **L2 单元卡缺结构分析** — 每个 L2 单元卡必须包含结构分析表（阶段/功能/章节/事件brief/情感设计/节奏）+ 嵌套子线表 → 缺任意一项退回 |
| ❌9 | **产出格式为 Markdown** — 严禁输出 YAML 格式（跨卷主题线.md 除外，其为 YAML 极简格式）。所有产出为 Markdown |
| ❌10 | **设计包是唯一数据源，不是原始小说** — 子 agent 必须从 `写作资产/设计包v4/chXXX-设计包.md` 读取数据，不得绕过设计包直接从原始 TXT 提取。委派时在 context 中给出**绝对路径**防止相对路径解析失败 |
| ❌10b | **⛔ 设计包格式异常不标记** — 如果设计包事件链不是标准表格格式，子 agent 必须先识别格式变体并记录格式异常再作业。已知格式变体（均缺标准 scene/POV 表头字段）：①**段落式 E1/E2 标记**（事件以段落 + 原文证据形式呈现，如 `ch006-设计包.md`）②**叙事层格式**（以「叙事层（Narrative Layer）」为区块头，事件以表格但无标准 scene/POV，如 `ch022-设计包.md`）③**事件N 标记**（以「事件N：」段落形式呈现，如 `ch021-设计包.md`）。子 agent 必须记录「⚠️ 本批设计包为 {变体名} 格式，缺标准 scene/POV 字段」再作业。不得静默消费格式异常的设计包 |
| ❌12 | **命名不规范** — L2 文件必须 `# L2-{编号} · {名称}（chXXX-chYYY）`，卷纲文件必须 `# 卷{N} · {卷名}`。首行格式不匹配 → 退回。参见[产出目录树](#产出目录树) |
| ❌13 | **delegate_task 未锁定格式** — 委派子agent时必须在其 context 中给出精确的 heading 格式范例。子agent 间命名不一致 → 追责到调度 agent |
| ❌14 | **L2 单元边界从单章设计包直接判断** — 单元边界必须有跨章证据（至少 2 章的设计包对比），不得从单章设计包的事件直接判断单元边界。单章判断 → 退回 |
| ❌15 | **剧情单元写原文叙事** — 事件链每行必须是结构描述（"内鬼提议全体自裁以自证"），不能是原文叙事（"王平站起来说"）。原文叙事=污染下游复用 |
| ❌16 | **剧情单元写方法论** — 不写"这个单元好在哪""读者为什么会爽"。只写事件链+结构解析+可迁移要点 |
| ❌17 | **Wiki 数据覆盖精度数据** — 当 `_temp/wiki-skeleton.md` 存在时，不得用 Wiki 的卷边界直接替代精度数据的聚类结果。Wiki 是参考，设计包是权威。冲突时以精度数据为准，标注冲突点 |
| ❌18 | **卷纲无溯源燃料台** — 每个卷纲必须包含溯源燃料台（4类溯源：剧情溯源/设定溯源/创意溯源/质感溯源），格式为 `| 原文段落 | 猜测来源 | 置信度 |` → 缺任一类型退回 |

---

## 产出格式规范

### L2 剧情单元卡 — L2-*.md

详见模板 `templates/L2-剧情单元卡.tpl.md`

必须包含以下区块：

| 区块 | 必须 | 内容 |
|:-----|:----:|:-----|
| 元数据 | ✅ | 来源书/章节范围/嵌套子线数/单元类型 |
| 物理坐标 | ✅ | 时间/地点/人物/力量 |
| 一句话单元 | ✅ | 用一句话概括本单元的叙事弧 |
| 结构分析 | ✅ | 表格：阶段/功能/章节/事件brief/情感设计/节奏 |
| 主题表达 | ✅ | 本单元在讲什么，主题与结构的对应 |
| 嵌套子线 | ✅ | 表格：子线/类型（闭合或延伸）/在本单元内的推进/与其他子线的咬合 |
| 可迁移要点 | ✅ | 结构模式/情感设计/节奏控制/嵌套策略等可复用模式 |
| 单元边界信号 | ✅ | 起点信号/终点信号（事件类型突变/场景切换/角色状态/心态状态/叙事功能） |

### 卷纲 — 卷N-卷纲.md

详见模板 `templates/卷纲-拆书版.tpl.md`

| 区块 | 必须 | 内容 |
|:-----|:----:|:-----|
| 本卷定位 | ✅ | 卷定位/主爽点/读者期待/剧情brief（3-5句话） |
| 舞台 | ✅ | 登场人物7列表（人物/定位/反应模式/说话方式/与主角关系/起点状态/终点状态）+ 地图 + 时间 |
| 溯源燃料台 | ✅ | 4类溯源：剧情溯源/设定溯源/创意溯源/质感溯源，每类格式为 `| 原文段落 | 猜测来源 | 置信度 |` |
| 幕序列 | ✅ | 表格：幕/章节范围/功能位/主爽点/对应L2卡/下幕衔接 |
| 事件种子池 | ✅ | 主线种子/支线·暗线种子/世界观·设定种子 |
| 核心高潮点 | ✅ | chXXX — 一句话描述 + 爽感类型 |
| 篇幅预算 | ✅ | 总章数/各幕分配/总字数估算 |

**溯源燃料台说明：**
- 只在卷纲级产出，不在 L2 单元卡级产出
- 格式统一为：`| 原文段落 | 猜测来源 | 置信度 |`
- 4类溯源：
  - **剧情溯源**：原文中具体的剧情桥段，猜测其来源（如某作品的类似桥段、某种叙事套路）
  - **设定溯源**：原文中的世界观/设定元素，猜测其来源
  - **创意溯源**：原文中的创意点子/反转设计，猜测其灵感来源
  - **质感溯源**：原文的文风/质感/氛围，猜测其文学来源
- 置信度标注：低/中/高

---

## 落盘检查点

| 文件 | 检查项 |
|:----|:-------|
| `设计/L2单元卡/L2-*.md` | 首行格式正确（# L2-{编号} · {名称}（chXXX-chYYY））；有scope声明；结构分析表完整（阶段/功能/章节/事件brief/情感设计/节奏）；嵌套子线逐条产出；单元边界信号有跨章证据 |
| `设计/卷纲/卷N-卷纲.md` | 首行格式正确（# 卷{N} · {卷名}）；有scope声明；溯源燃料台4类溯源完整；幕序列含L2卡映射；舞台人物7列表完整 |
| `设计/跨卷主题线.md` | YAML极简格式；每条主题线含名称/类型/跨卷范围/承载L2/演化/置信度 |
| `写作资产/剧情库/{标签}/{书}-L2-*.md` | 区分原书切片与可迁移单元；可迁移要点完整 |
| `写作资产/剧情库/{标签}/{书}-卷纲-*.md` | 溯源燃料台完整；幕序列含L2卡映射 |

---

## 版本

v4.0.0 | 2026-07-01 | **架构重构**：从「L2 单元卡 / L3 剧情线 / L4 全书事件」三级产出改为「L2 单元卡 / 卷纲（含溯源燃料台）」双轨产出。删除 L3 剧情线、L4 全书事件的全部层级定义、模板、步骤和红线。新增卷纲（拆书版）模板，含溯源燃料台（4类溯源：剧情/设定/创意/质感）。步骤从 8 步精简为 5 步。Step 2 从 L3 剧情线追踪改为卷纲归纳。Step 3 从 L4 全书事件识别改为跨卷主题线追踪（YAML极简格式）。Step 5 从 L2/L3 双轨入库改为 L2卡+卷纲双轨入库。L2 模板删除「不可迁移部分」区块。删除红线 ❌3/❌5/❌8/❌11，新增红线 ❌18（卷纲无溯源燃料台）。

v3.0.4 | 2026-06-25 | **新增反模式：L2 卡中文文件名阻塞 read_file**。在「递送上下文增强策略 → 反模式」中新增第4条：子 agent 的 `read_file` 对中文文件名解析可能失败，对策是先用 `search_files` 获取实际路径再读取。

v3.0.4 | 2026-06-25 | **新增 read_file→write_file 行号污染陷阱**（入库时用 heredoc 而非 read_file/write_file）。更新库结构为按书分类（与 pop-trope-library 对齐）。

v3.0.3 | 2026-06-25 | 新增「L3 生产特有的格式漂移陷阱」「多卷项目目录组织」「旧格式废弃与归档」流程。

v3.0.1 | 2026-06-24 | **工作流精细化**：步骤速查从 5 步扩展为 8 步。新增「递送上下文增强策略」「L3 两阶段生产模式」「卷末单元特殊处理」节。

v3.0.0 | 2026-06-23 | **架构重构**：从「卷纲/幕纲」改为「L2 剧情单元卡 + L3 剧情线 + L4 全书事件」三级产出。

v2.3.0 | 2026-06-22 | **Wiki 骨架集成**：新增 `wiki-skeleton` 可选上游。

v2.2.1 | 2026-06-19 | 新增❌14章级切片格式漂移陷阱。

v2.2.0 | 2026-06-19 | 新增红线❌10b：设计包格式异常标记要求。

v2.0.1 | 2026-06-16 | 新增红线❌10：子 agent 必须从设计包文件读取数据。

v2.0.0 | 2026-06-16 | 从YAML→Markdown 迁移。
