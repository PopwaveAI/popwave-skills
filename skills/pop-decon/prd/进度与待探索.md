# 进度与待探索 · pop-decon

## 当前进度

**已完成（带凭据）**

- v26.4.0（2026-09-22）：Step 0 新增分章接线，Step 1 升级为读配置优先，新增 `templates/拆解配置.tpl.json`。见 `CHANGELOG.md` v26.4.0。
- 补齐本 `prd/`（此前 CHANGELOG v26.3.1 声明「新增 prd/ 骨架」但目录不存在，属历史遗漏）。

**进行中**

- 无。

**已知缺口**

- `references/delegation-orchestration.md` 与 `references/small-book-phase2-strategy.md` 仍写「每批不超过 8 章」旧口径，与 dimension 的「~30章/批，上限40」矛盾。等 dimension 侧统一后一并修，入口侧两个文件是孤儿（不在速查表）。
- 拆解配置的 `validate-scope` 预检尚未在入口流程里强制（当前是「建议预检」），等桌面侧契约冻结后定是否强制。

## 后续方向

- **近期**：统一 `delegation-orchestration.md` / `small-book-phase2-strategy.md` 的切批口径到 30章/批，或删除孤儿文件。算做完 = 两个文件与 dimension 口径一致，或从速查表与目录移除且全仓无引用。
- **中期**：入口支持多资产（多 TXT = 多卷）场景的分章与范围选择。算做完 = 宿主传入多资产时，入口能正确路由并生成跨卷连续 `globalNo` 的合并索引。
- **长期**：入口与桌面端「独立交互」完全对齐（配置字段、事件回传格式）。算做完 = 桌面端 PRD 里的七项契约在入口侧全部有对应实现。

## 悬而未决

- `focusDimensions` 七标签的最终取值与「深度」语义，等桌面侧 PRD 定稿。等谁拍：产品侧（另一个 agent）。
- 分章脚本与桌面 `bookImportService` 是否收敛为单一实现。等谁拍：产品侧。

## 踩过的坑

- 版本号曾三处不一致（SKILL.md 头部 v26.1.0、文末 v26.3.0、skill.json v26.3.0），v26.4.0 已统一。
- CHANGELOG 曾出现两条 v26.3.0，已把 09-13 一条修正为 v26.3.1。
