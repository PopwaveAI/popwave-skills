---
name: pop-novel-asset
description: "当用户说'提取角色档案/提取场景资产/提取视觉符号/小说素材提取/视觉资产准备'或需要在做OC/漫画/封面前先从原文提取素材时启用。从小说原文系统性提取结构化视觉资产（角色档案/场景资产表/视觉符号库/IP视觉DNA），供 pop-novel-oc、pop-novel-comic、pop-novel-cover 消费。兼容写作专家项目和独立小说项目，支持增量更新。"
---

# pop-novel-asset

> 小说视觉资产提取器。从原文提取结构化素材，供视觉 skill 群消费。v1.0.0

## 做什么

输入：小说原文（txt 文件或多章 txt 目录）+ 可选的角色名/章节范围/提取需求。
输出：`素材/视觉资产/` 目录下的标准化资产文件。

核心价值：**只提取不生成，只理解不画图**。把非结构化的小说原文，转化为结构化的视觉资产库，让下游 skill（OC/漫画/封面）不用各自从原文提取、重复劳动。

与拆书 skill 的本质区别：拆书拆的是**怎么写的**（技法），asset 提的是**有什么**（素材）。消费者不同，提取层不同。

## 两种项目空间

| 探测信号 | 项目类型 | 原文路径 | 已有设定 |
|:---------|:---------|:---------|:---------|
| `项目总控.html` | 写作专家·起点 | `正文/ch*.txt` | `设计/角色库/` + `设计/全书设定/` |
| `project-state.md` | 写作专家·番茄 | `正文/ch*.txt` | `设计/角色库/` + `设计/` |
| `原料/小说原文/` | 独立小说项目 | 单个完整 txt | 无 |
| 用户指定路径 | 临时模式 | 指定文件 | 无 |

> 写作专家项目已有角色库.md 时，asset 读取作为辅助参考，但仍需从正文采样原文佐证和行号索引。

## 怎么运作

### Step 0: 项目空间探测 + 原文定位 + 产出规划 → `steps/step0-detect.md`

- 探测项目类型（四种信号）
- 定位小说原文（多章合并 or 单文件）
- IP 背景判定（同人/改编/原创）
- 根据用户需求规划产出组合（角色档案/场景资产表/视觉符号库/IP视觉DNA）
- 增量更新检查：已有资产文件 → 检测原文新增章节 → 规划增量范围

### Step 1: 资产提取 → `steps/step1-extract.md`

按 Step 0 规划执行提取，每种产出独立操作：

**角色档案**：5轮关键词搜索 → 上下文采样 → 10维度档案 → 落盘
**场景资产表**：原文通读/章节扫描 → 关键场景拆解 → 可定格帧清单 → 落盘
**视觉符号库**：全文意象扫描 → 标志色/器物/符号提取 → 落盘
**IP视觉DNA**（同人时）：源IP搜索 → 画风/经典形象/世界观符号提取 → 落盘

### 🚪 门禁：资产确认

向用户呈现提取结果摘要，确认准确性和完整性后落盘。

### 消费路由

资产就绪后，根据用户意图路由到消费 skill：

| 用户意图 | 路由到 | 读取哪些资产 |
|:---------|:-------|:------------|
| "做OC/立绘" | `pop-novel-oc` | 角色档案 |
| "做漫画" | `pop-novel-comic` | 角色档案 + 场景资产表 |
| "做封面/场景图" | `pop-novel-cover` | 场景资产表 + 视觉符号库 |
| "只提取" | 停止 | 资产已就绪供后续使用 |

## 增量更新机制

当写作专家项目写到新章节后，用户可要求增量更新已有资产：

1. 读取已有资产文件的"采样范围"标记（如"截至 ch015"）
2. 检测原文新增章节（ch016-ch020）
3. 只对新增章节执行提取
4. 合并到已有资产文件（追加新维度内容 + 更新采样范围标记）

> 增量更新不覆盖已有内容，只追加新发现的信息。已有角色档案的维度二（外貌演变）追加新阶段，维度五（成长弧线）追加新阶段。

## 产出文件结构

```
{项目}/素材/视觉资产/
├── [角色名]角色档案.md          # 10维度 · 原文采样+行号索引
├── 场景资产表.md                # 可定格帧清单
├── 视觉符号库.md                # 核心意象/标志色/器物/阵营符号
├── IP视觉DNA.md                 # 同人时才有
└── 资产索引.md                  # 所有资产的目录和状态
```

## ❌ 铁律

| # | 铁律 | 违反后果 |
|:-:|:-----|:---------|
| ❌1 | **只提取不生成** — asset skill 禁止调用任何图片/视频生成 API，禁止做视觉设计方案。只从原文提取素材，生成交给消费 skill | 职责越界，和消费 skill 重复 |
| ❌2 | **原文采样必须有行号索引** — 每条提取的信息必须标注原文行号，便于用户查验和消费 skill 定位场景 | 无法溯源，消费 skill 无法定位原文 |
| ❌3 | **增量更新不覆盖** — 增量更新只追加新内容，不删除或覆盖已有内容。已有内容如有错误，在门禁环节由用户确认后修改 | 丢失已有采样数据 |
| ❌4 | **资产必须落盘到 素材/视觉资产/** — 所有产出文件写入 `素材/视觉资产/` 目录，不散落到项目根目录或其他目录 | 消费 skill 找不到资产文件 |

## 速查表

| 我要 | 读什么 | 什么时候 |
|:-----|:------|:---------|
| 探测项目空间+规划产出 | `steps/step0-detect.md` | Step 0 |
| 执行资产提取 | `steps/step1-extract.md` | Step 1 |
| 角色档案10维度方法论 | `references/asset-extract-guide.md` §一 | 提取角色档案时 |
| 场景帧提取方法论 | `references/asset-extract-guide.md` §二 | 提取场景资产表时 |
| 视觉符号库方法论 | `references/asset-extract-guide.md` §三 | 提取视觉符号库时 |
| IP视觉DNA提取方法 | `references/asset-extract-guide.md` §四 | 同人小说时 |

> **环境**：Python 3.8+。纯文本处理 skill，不需要 API Key。使用 Grep + Read 工具从原文采样。
