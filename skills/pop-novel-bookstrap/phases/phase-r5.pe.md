# Phase r5：逆向提取 — 卷大纲确认

## 前置条件
Phase r1 + Phase r4 完成。

## 执行步骤

分情况处理：

| 情况 | 处理 |
|------|------|
| 已有完整卷大纲 | 确认当前进度 → 标记续写起点 |
| 只有粗略规划 | 从事件日志提取卷级事件链 → 整理为剧情脉络.md |
| 完全无大纲 | 从事件日志反向推导卷级结构 → 产出剧情脉络.md |

### 产出
- `设计/全书架构.md` + `设计/卷/volume-XX.md`（剧情脉络）
- `状态/`角色卡快照 + `entity-snapshot.yaml` event_log（伏笔追踪）

### 用户确认
大纲方向用户必须认可。

## 产出
- `设计/全书架构.md` + `设计/卷/volume-XX.md`（剧情脉络）
- `状态/`角色卡快照段（伏笔追踪）
