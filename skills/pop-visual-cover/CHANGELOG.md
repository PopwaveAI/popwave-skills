# CHANGELOG

## v2.1.5 | 2026-09-21

**跨包引用与画风 DNA 库文件名收口。**

- 跨包引用统一改 `<包名 包根>/...` 写法：原来写的 `skills/<包名>/...`、`../其它包/...`、裸 `pop-xxx/references/...` 在用户机器上都解析不到。
- `文风DNA-library.json` 这个文件名在本仓不存在，真实文件是 `style-dna-library.json`，全部改回真实名。

无内容语义变更，只改路径写法。

## v2.1.4 | 2026-09-21

**路径写法去个人化：不再写死用户名与盘符。**

- 运行脚本：`generate_comic_page.py` 的输出目录与定妆图根目录改走环境变量 + 中性默认（原来写的是开发机盘符与个人用户目录）；`build_3test.py` 删掉写死的 DNA 库绝对路径，改 `--dna-lib` + 环境变量 + cwd 候选，找不到时报用法；`tool-download-webnovel` 三个脚本的默认下载目录由开发机盘符改为用户主目录下的 Downloads/novel。
- 相对路径原按包根解析、会把产物写进 skill 包内，改按当前工作目录解析。
- 引用修正：`文风DNA-library.json` 这个文件名在本仓不存在，改回真实文件名 `style-dna-library.json`。

无脚本对外契约变更（除新增可选参数与默认值去个人化）。

## v2.1.3 | 2026-09-21

**脚本与跨包路径改按包根解析。**

把脚本调用与跨包引用的写法从「发布源相对路径」改为「本包根相对路径」。本包根就是运行时系统提示里 `--- skill: /pop-visual-cover (<包根>) ---` 括号内的路径，命令行等价于 `--skill`，逐台机器不同。原来写的 `scripts/x.py`、`../pop-visual-shared/scripts/x.py`、`skills/pop-xxx/...` 在用户机器上解析不到，模型只能全盘搜脚本。

改动：SKILL.md 增「脚本调用约定」段；跨包引用改为 `<包名 包根>` 形式；本包脚本调用补 `<包根>` 前缀。无脚本逻辑变更。

## v2.1.2 | 2026-09-20

改 description：补触发条件、删内部实现说明（原 51 字 → 63 字）。不涉及规程与产出变更。

## v2.1.1 | 2026-09-14

**统一命名与编号（全局改动的一部分）。** 依老板口径"三条线是同一写作思路下不同复杂度"：相同定位的文件同名、引用逻辑一致，复杂度差异只体现在填多少。

### 改动

- 统一命名与编号（老板拍板「三条线是同一写作思路下不同复杂度」）：设计库档名对齐为 `世界画像.md`／`世界法则.md`／`世界格局.md`（原 世界全书／力量体系／势力格局）；全书级主表统一为 `剧情库/00-主线结构表.md`（原 00-全书卷进度表／00-上升线表）；三线 phase 编号归一为 1-8 单层号（原 2a-2g／4a-4e／1.5 作废）；三线 pipeline 建目录补设计库 5 件主档＋3 件可选空档；三个 pipeline 新增「存量项目标准化改造」一节。

### 同批全局改动

| 项 | 处理 |
|:--|:--|
| 设计库档名 | `世界全书`→`世界画像`、`力量体系`→`世界法则`、`势力格局`→`世界格局`（`力量体系模板库` 等库名不动） |
| 全书主表 | `00-全书卷进度表`／`00-上升线表` → 统一 `剧情库/00-主线结构表.md`；两个模板文件同步改名 |
| 编号 | 三线 phase 归一为 1-8（1 立项／2 设定／3 剧情骨架／4 排期编排／5 章纲／6 正文／7 验收／8 回看），旧号作废 |
| 建目录 | 三线统一 11 个一级目录；设计库建 5 件主档＋3 件可选空档；人物库建 `剧档案/` |
| 存量改造 | 三个 pipeline 新增「存量项目标准化改造」一节（差异报告→确认→执行→迁移报告） |
## v2.1.0 — 2026-09-13

### 新增 `prd/` 骨架（设计档案）

`pop-shared-skill-create` v9.0.0 把 `prd/`（设计预期／解决思路／进度／后续方向）立为 skill 标配，本 skill 先落两份骨架：`prd/PRD.md`、`prd/进度与待探索.md`。

**内容状态**：设计预期、解决思路、边界、关键取舍四节标「待补（2026-09-13）」，等下一次改到这个 skill 时填实。**不编设计预期**——编出来的比没有更坏，下一轮会拿它当依据。


## v2.0.0 — 2026-08-24

### steps 四件全合入 SKILL.md 单文件精炼

> **根因**：实测 step 文件在当前 harness 从未被加载/Read（子agent注入链断在骨架层）。参考 write/pipeline 改造模式合入主文档。

**改动**：
- **steps/ 目录删除**：step0-research / step0-scene-understand / step1-design / step2-generate 四件全合入 SKILL.md 对应 SOP 节（Step 0 意图分析+搜图选图 / Step 0-Scene 原文理解管线 / Step 1 设计方案 / Step 2 生成资产）
- **执行模式明确**：门禁A（选图对齐/理解确认）与门禁B（方案对齐）用户多轮交互主 agent 直执；Pinterest 搜图+图片分析、原文解构与上下文补全（WebSearch）等只读类工作可派子 agent 回报结果、主 agent 整合落盘；翻译与生成是工具调用主 agent 直执
- **内容精炼**：门禁A/B 呈现模板与落盘格式、参考点选项清单、参考点→设计范围/提示词策略双表、放开吸收公式+四条关键原则、V3 结构化公式+11条翻译规则、Seedance 公式、反向强化技巧、data URI 注意事项、安全转换五规则、上下文补全三路径、叙事瞬间选取四依据、降级机制五档、自检清单全保留；SKILL.md 原 Step 0 摘要（资产优先/美术设定集唯一真源/cover 轻量档位）并入 Step 0-Scene；删两段长翻译示例（规则已编码同一逻辑）；迭代模式快速路径"进入 steps/step2-generate.md"改指 Step 2
- skill.json version 1.8.0→2.0.0

---

## v1.8.0 | 2026-08-13

### 元数据同步

- skill.json 的 description 改为面向用户介绍、tags 改为可调用专家标签、版本号同步至 v1.8.0。

## v1.7.0 | 2026-08-09

### 落盘三态对齐（候选→测试/封面，记录→_过程/提示词记录）

按 `pop-visual-pipeline/references/落盘规范.md`：封面/场景候选图从 `素材/视觉/` 迁到 `测试/封面/`，确认后复制到 `成品/封面/`（加 `-final`）；设计记录 `素材/视觉设计方案.md` 迁到 `_过程/提示词记录.md`。`step0-research/step0-scene-understand/step1-design/step2-generate` 与 `SKILL.md` ❌4 同步。

## v1.6.0 | 2026-08-05

### 消费链路对齐：cover 意图 = 轻量基建

老板审视全链路发现——cover 消费链路未声明档位，需明确 cover 意图只需基建到身份卡即可派生，不强制双角度定妆：

- `SKILL.md` Step 0-Scene 新增档位说明：Pipeline 语境下 cover 只需身份卡（轻量档），**不强制双角度定妆**；如需角色参考图用 character 单张定妆图
- 版本同步：SKILL.md / skill.json 至 v1.6.0

---

> 历史版本条目已归档：`_archive/changelog-history/pop-visual-cover/CHANGELOG.md`（全量保留，本文件仅保留最近3个版本）
