# 进度与待探索 · tool-split-chapters

## 当前进度

**已完成（带凭据）**

- 分章脚本 `scripts/split_book.py` 四子命令（split/batch/validate-scope/estimate），本机实测通过：干净样本 8 章 2 卷分章正确，缺失章检测 `quality.missing=[6]`，防盗标记 `flags=["antileech"]`，试拆判定退出码 2。
- 三份契约 schema 落 `references/契约说明.md`。

**进行中**

- 无。

**已知缺口**

- 分章正则对「正文中独立成行且以第X章/卷开头的短行」会误判（`split-test` 里"第一卷内容结束""第四章正文"两行触发了误判）。已在 SKILL.md 红线 5 声明需人工抽检边界，未做自动修复。
- 多资产（多 TXT = 多卷）合并尚未实现，当前 `split` 只接受单 TXT，卷靠卷标题识别。
- `crawl-order` 与 `toc` 两种 splitMethod 未实现，当前只 `title-regex`。

## 后续方向

- **近期**：抽检真实网文样本（≥5 本不同来源），核对分章边界。算做完 = 5 本样本的分章结果人工核对无误，误判率低于每百章 1 处。
- **中期**：多资产合并（`index` 子命令，把多个单卷索引合并成合并章节索引）。算做完 = 输入 3 个 TXT 能产出跨卷连续 `globalNo` 的合并索引，且指纹去重生效。
- **长期**：`crawl-order` 分章（消费 `tool-download-webnovel` 的目录），替代 title-regex 作为爬取产物的首选。算做完 = 爬取产物用目录切分时零正则误判。

## 悬而未决

- 分章脚本与桌面端 `bookImportService` 存在双实现风险，等桌面侧定「分章归一只实现一次」后再决定是否收敛。等谁拍：产品侧（另一个 agent）。
- token 估算系数 [0.6, 1.0] 是经验值，等一次真实拆解拿到实际用量后校准。等谁拍：拆解侧实测数据。

## 踩过的坑

- `split` 早期把「序章/尾声」归入卷标题正则，导致尾声被开成新卷。已改为章级特殊标记，不改卷号。
- `write_out` 早期直接对 str 取 `.parent`，报 `'str' object has no attribute 'parent'`。已统一转 `Path`。
- 外部 JSON（桌面端/用户写的配置）可能带 UTF-8 BOM，`json.loads` 会抛 `Unexpected UTF-8 BOM`。统一用 `utf-8-sig` 读。
