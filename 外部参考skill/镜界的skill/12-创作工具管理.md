# 创作工具管理

先按任务选择附件，只读取本次需要的操作指南：

| 任务 | Reference |
| --- | --- |
| 保存长期写作偏好、禁忌，管理当前小说的 AgentRules | `references/agent-rules.md` |
| AgentSkill 的查找、安装与日常管理，包括附件 | `references/agent-skills.md` |
| 从模糊需求设计新 AgentSkill，优化触发边界、提示词或附件结构 | `references/skill-design.md` |
| 管理智能体，配置职责与 system prompt | `references/subagents.md` |
| 编排和维护多步骤 Workflow 节点图 | `references/workflows.md` |

复制 `<skill_resources>` 中的 `reference-list` 调用列出附件，再用 `reference-get` 读取选中文件；`name` 使用「创作工具管理」，`path` 使用目录返回值。已读取且仍在上下文中的附件无需重读，不预读全部附件。

小说角色、功法、世界观规则和故事流程属于故事资料，不按 AI 工具配置处理。规则作用于当前小说，AgentSkill 和 Workflow 可跨小说复用；载体或作用范围不明确时先确认。
