# 小说漫画专家重构方案（分阶段执行）

> 定位：把"小说文本 → 多形态衍生 → 多平台分发变现"的重构落地成**可逐步执行的分阶段方案**。因为工程太大，不一次性推翻，而是按阶段推进，**每阶段独立可交付、可验收、可回滚**。
> 指导思想（已定调）：**四层架构**（源层→视觉设定集 Bible→派生层→出口层）+ **发布清单内嵌各派生 skill**（不新增独立 publish skill）。
> 配套：`漫画专家管线梳理-IP宇宙衍生.md`（四层架构总纲）、`P0粮种评估报告.md`（变现优先级）。

---

## 一、重构目标（一句话）

把现状"两套平行 pipeline、无统一出口、基建是散点"重构为一条 **"小说 IP 宇宙衍生管线"**：基建一次做对并汇总成**一部视觉设定集 Bible**，派生层全部消费设定集、收尾自带**发布清单**，出口只做校验汇总。最终对标"写完小说→投番茄/起点"，实现"**基于小说衍生 IP 宇宙 → 一键投稿多平台**"。

---

## 二、现状盘点（gap 诊断）

> 基于当前 skill 实际版本清点，标出"已具备"与"重构缺口"。

| Skill | 版本 | 已具备 | 重构缺口 |
|:--|:--|:--|:--|
| `pop-visual-pipeline` | v2.0.0 | Phase 0-6 路由调度、意图闸口前置、基建就绪门禁 | **缺 Phase 7 出口（校验+汇总发布索引）** |
| `pop-visual-asset` | v1.1.0 | 角色档案/场景资产表/视觉符号库/IP视觉DNA | —（继续作为美术设定集"世界观/场景/符号篇"来源） |
| `pop-visual-style` | v1.8.0 | 画风DNA库、定标、稳定复现 | —（继续作为美术设定集"画风篇"来源） |
| `pop-visual-character` | **→ art-bible v2.0.0** | **已改名升级为 `pop-visual-art-bible`**，产出美术设定集（五篇合一），不再单独产出身份卡 | —（一阶段已落地） |
| `pop-visual-oc` | v1.9.0 | 立绘OC、六层信息、系列化 | **缺"纯立绘交付形态"分支**；**缺立绘包发布清单收尾** |
| `pop-visual-cover` | v1.6.0 | 封面/场景图 | **缺封面包/场景包发布清单收尾** |
| `pop-visual-comic` | v7.20.0 | 页漫连载、`分享/` 分页图+长图、水印 | **缺小红书多图条漫导出**；**缺条漫包发布清单收尾** |
| `pop-comic-content` | v0.6.0 | 三件套+出片（漫剧/放映视频） | **缺漫剧包/放映视频包/内容包发布清单收尾** |
| `pop-visual-shared` | — | generate/batch_test/pinterest_search | **缺统一发布清单模板（六项字段底座）** |

---

## 三、分阶段实施路线（5 阶段）

> 原则：**先立规范底座，再打变现入口，最后收口验证**。每阶段改动尽量"增量叠加、不破坏现有"，可独立验收后再进下一阶段。

### 阶段一：地基双底座（Art Bible 设定集 + 发布清单模板）— ✅ 已落地

**目标**：把 L1 基建统一成"一部美术设定集"，把 L3 需要的"发布清单六项模板"立起来。这是全部派生层的公共底座，先做对，后面各 skill 直接引用。

**改动范围**：
- `pop-visual-character` → **改名升级为 `pop-visual-art-bible`（Art Bible · 美术设定集）**：从"只设计人物身份"升级为"产出整部美术设定集"，消费 asset（查原文）+ style（定画风），产出 `素材/美术设定集.md`（画风/人物/场景/符号/一致性五篇合一，文+图），做跨实体一致性仲裁，冻结为派生层唯一真源。✅ 已完成
- `pop-visual-pipeline`：Phase 0-2 路由到 art-bible（Phase 2 产出美术设定集），派生层（Phase 3-6）只消费美术设定集；总控模板 phase2 改为"美术设定集"。✅ 已完成
- `pop-visual-asset` / `pop-visual-style`：互操作引用指向 art-bible（asset 优先路由到 art-bible，style 画风基线先被 art-bible 消费）。✅ 已完成
- 下游 `oc`/`cover`/`comic` 消费协议：从"读角色视觉身份卡"改为"读美术设定集·人物篇"，断链引用全部修复。✅ 已完成
- `pop-visual-shared`：新增 `templates/发布清单.tpl.md`（六项固定字段：平台规格/标题文案/话题标签/AI标注/投流建议/发布动作清单）+ 各派生 skill 引用协议。⏳ 待做

**产出/验收**：✅ 已跑通"基建(asset/style/art-bible) → 美术设定集冻结 → 派生层只读美术设定集且不重建人物/场景/画风"的链路改造；skill 文档与顶层设计均已同步。

**依赖**：无（纯规范底座，不加新功能）。

**建议**：一阶段 L1 部分已落地（art-bible 升级 + pipeline 路由 + asset/style 互操作 + 下游消费协议）。发布清单模板（`pop-visual-shared` 新增）属 L3 底座，可并入阶段二随首个派生包一并补齐。

---

### 阶段二：P0-01 立绘包（oc 纯立绘 + 内嵌发布清单）

**目标**：打通最快变现入口（设圈约稿/头像），产出即带"一键照做的立绘投稿包"。

**改动范围**：
- `pop-visual-oc`：新增**纯立绘交付形态**分支（去文字/装饰/文化元素，纯角色本体，供设圈约稿/头像/展示，对标定妆照但走展示精度）。
- `pop-visual-oc`：收尾**生成立绘包** `投稿包-立绘-{角色}/`（成品纯立绘图 + 人设简介卡 + 报价档位表 + 发布清单.md）。
- 发布清单六项：米画师/工坊/小红书规格、标题×3、话题标签、AI 标注、投流建议（可选）、发布动作。

**产出/验收**：跑通一个角色 → 纯立绘 + 发布清单 → 用户照单即可在米画师/小红书投稿。

**依赖**：阶段一（发布清单模板）。

---

### 阶段三：P0-02 条漫包（comic 小红书多图导出 + 内嵌发布清单）

**目标**：打通"人物→漫画→社媒获客"（小红书/B站图文分发），产出即带条漫投稿包。

**改动范围**：
- `pop-visual-comic`：新增**小红书多图条漫导出**（复用已有 `分享/page01~N.png` 分页成品图，按小红书多图规格整理上传包，不走整条长图单发）。
- `pop-visual-comic`：收尾**生成条漫包** `投稿包-条漫-{书}/`（多图成品 + 标题×3 + 话题标签 + AI 标注 + 薯条投流建议 + 发布动作）。
- 发布清单六项：小红书/ B 站版式规格、标题/文案、631 标签、AI 标注、投流建议、发布动作。

**产出/验收**：跑通一章 → 多图条漫包 → 用户照单即可在小红书发布。

**依赖**：阶段一（发布清单模板）。

---

### 阶段四：P0-03 漫剧·放映·封面·内容包（comic-content + cover 内嵌发布清单）

**目标**：把 IP 放大终点（漫剧/放映视频）与反哺订阅（封面/场景）的包补齐，各派生 skill 收尾都自带发布清单。

**改动范围**：
- `pop-comic-content`：出片/三件套完成后收尾生成**漫剧包**（抖音/快手/B 站规格+投流建议）+ **放映视频包**（抖音/视频号/小红书，小说推文话术）+ **内容包**（复用已产出口播/种草/推广文案）。
- `pop-visual-cover`：封面收尾生成**封面包**（番茄/起点投稿尺寸）+ 场景收尾生成**场景包**（小红书/ B 站展示规格）。

**产出/验收**：跑通漫剧包 + 放映视频包 + 封面包各一，均带六项发布清单。

**依赖**：阶段一（发布清单模板）。

---

### 阶段五：出口层 Phase 7（L3 校验 + 汇总发布索引）

**目标**：把整条链收口，最终对标"写完小说→一键投稿"。

**改动范围**：
- `pop-visual-pipeline`：新增 **Phase 7 出口**，校验每个产出是否携带发布清单（缺失即打回对应 skill 补），并汇总 `投稿发布索引.md`（全部傻瓜包一览：投哪/干嘛用/发布动作）。
- 全链路闭环：基建 → 派生 → 各投稿傻瓜包 → 发布索引。

**产出/验收**：全链路一键投稿傻瓜包跑通——一个项目从小说文本到各平台就绪的五类包全部产出 + 发布索引。

**依赖**：阶段二/三/四（各包就绪后出口才可校验汇总）。

---

## 四、阶段依赖与价值顺序

```
阶段一 地基双底座（设定集Bible + 发布清单模板）   ← 无依赖，纯规范底座
   │
   ├──→ 阶段二 P0-01 立绘包（oc）       【最快变现，先做】
   ├──→ 阶段三 P0-02 条漫包（comic）    【社媒获客】
   └──→ 阶段四 P0-03 漫剧·放映·封面·内容包 【IP放大+反哺】
              │
  阶段二/三/四 全部就绪后 ──→ 阶段五 出口层 Phase 7（校验+汇总发布索引）
```

- **价值顺序**：变现快（立绘）→ 获客（条漫）→ IP 放大 + 反哺（漫剧/封面）→ 收口验证（出口）。
- **阶段二/三/四可并行**，但都依赖阶段一的发布清单模板，故阶段一必须最先做。

---

## 五、每阶段验收门禁（防带病推进）

| 阶段 | 门禁（不满足不进下一阶段） |
|:--|:--|
| 一 | 设定集冻结为 `✅ 已认可`；派生层只读设定集不重建；发布清单模板落盘可被各 skill 引用 |
| 二 | 立绘包含纯立绘成品 + 六项发布清单；照单可投米画师/小红书 |
| 三 | 条漫包含小红书多图成品 + 六项发布清单；照单可投小红书 |
| 四 | 漫剧包/放映包/封面包各含成品 + 六项发布清单；照单可投对应平台 |
| 五 | 出口校验无缺失包；发布索引就绪；全链路一键投稿跑通 |

> 统一红线：**发布清单六项缺一不可**（平台规格/标题文案/话题标签/AI标注/投流建议/发布动作）。AI 生成标注为强制项（2025-09-01 标识办法生效，未标注限流降权）。

---

## 六、完成后效果对比

| 维度 | 重构前 | 重构后 |
|:--|:--|:--|
| 基建 | asset/style/character 散点各自为政 | **一部视觉设定集 Bible**，派生层只消费它 |
| 出口 | 无统一标准，拿到产出不知投哪 | 每个产出自带**发布清单**，照单即投 |
| publish | 无 | **不新增独立 skill**，发布清单内嵌各派生 skill |
| 闭环 | 断在"画完" | 小说→各平台就绪的傻瓜包 + 发布索引 |

---

## 来源

- AI 生成标识办法与处罚：https://www.tianhebiaoshi.com/news/xinwenzixun1/205.html ；https://news.sohu.com/a/953598323_122551079