# pop-comic-content 改动清单（上）· SKILL.md

> **修改前**＝`skills/pop-comic-content/SKILL.md`（原版 288 行）
> **修改后**＝`SKILL-改后.md`
> 判据三问：**谁**（主语在不在）、**做什么**（动作在不在）、**凭什么判断**（依据或正反例在不在）。
> 本轮做两件事：一、把压缩单元展开成完整句子；二、把语域收到书面。
> 病类缩写：缺主＝缺主语｜缺谓＝缺谓语｜缺动＝缺动作词｜硬切＝硬切短句｜符号＝符号串当句｜口语＝语域降档｜生造＝生造词｜指代＝指代不明｜压失＝压缩到失准｜比喻＝装饰性比喻。
> 表内单元格以 `｜` 表示，与正文表格列区分。下半见 `改动清单-下.md`。

## 一、前言与做什么（原第 3 至 23 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 1 | 漫画 → 内容物料 skill。**模型读图自理解**漫画页（图片为主…）→ 一键产出三件套： | 本技能负责把漫画转为内容物料。以模型读图的方式理解漫画页，以图片为主…随后一键产出三件套： | 补主语；→→转为/随后 | 缺主、符号 |
| 2 | **（可选）出片**：把确认后的口播脚本 + 漫画页图组装成竖版短视频（ | **（可选）出片**：把确认后的口播脚本与漫画页图组装成竖版短视频（ | +→与 | 符号 |
| 3 | ，**不烧字幕**——成品图已内嵌文字， | ，**不烧字幕**，因成品图已内嵌文字， | 破折号→逗号，补"因" | 硬切、缺谓 |
| 4 | **图片优先用 `分享/` 成品图**（有字/水印），无则回退 `output/` 生图。 | **图片优先用 `分享/` 成品图**（有字、有水印），无则回退 `output/` 生图。 | /→、 | 符号 |
| 5 | 把已成稿的漫画页（page1~N.png + 页面配置.json + OC/封面）转化为 | 把已成稿的漫画页（page1~N.png、页面配置.json、OC/封面）转化为 | +→、 | 符号 |
| 6 | `页面配置.json`/`导演卡.md` 的文字只做锚点与保真兜底。禁止不读图凭记忆编造漫画内容。 | `页面配置.json` 与 `导演卡.md` 的文字只做锚点与保真兜底。不得跳过读图，凭记忆编造漫画内容。 | /→与；禁止不读图→不得跳过读图 | 符号 |
| 7 | （素材收集 + 读图理解）是只读扫描/读图分析，可派子agent执行并回报（素材清单/逐页白描表/卖点识别），主agent落盘；…主agent直执。 | （素材收集 + 读图理解）是只读扫描与读图分析，可派子 agent 执行并回报素材清单、逐页白描表与卖点识别，由主 agent 写入；…由主 agent 直接执行。 | /→与、；子agent→子 agent；落盘→写入；直执→直接执行 | 符号、生造 |
| 8 | 输入：漫画页图片（page1~N.png）+ `页面配置.json`（文字锚点）+ 可选 `导演卡.md`（剧情因果链）+ OC/封面图。 | 输入：漫画页图片（page1~N.png）、`页面配置.json`（文字锚点）、可选 `导演卡.md`（剧情因果链）与 OC/封面图。 | +→、 | 符号 |
| 9 | 输出：一套内容物料（三份可投放文本）+ 可选一支竖版短视频。 | 输出：一套内容物料（三份可投放文本），以及可选的竖版短视频。 | +→，以及 | 符号、缺谓 |

## 二、模型说明表（原第 29 至 36 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 10 | 读图理解（画面+剧情）｜模型多模态读图｜主 agent（或子agent）逐页 Read 漫画页，产出逐页画面白描 + 剧情因果锚点 | 读图理解（画面与剧情）｜模型多模态读图｜主 agent（或子 agent）逐页 Read 漫画页，产出逐页画面白描与剧情因果锚点 | +→与；子agent→子 agent | 符号 |
| 11 | TTS 配音（可选）｜火山灿灿 2.0（`scripts/tts_generate.py`）｜key 固化在 `scripts/.env`（`VOLC_ARK_API_KEY`），自动读取 | TTS 配音（可选）｜火山灿灿 2.0（`scripts/tts_generate.py`）｜key 固化在 `scripts/.env`（`VOLC_ARK_API_KEY`），脚本自动读取 | 补主语"脚本" | 缺主 |
| 12 | 出片渲染（可选）｜FFmpeg 图片序列+xfade（`scripts/build_comic_video.py`）｜整页顺序播放+封面信息页+切页点对齐口播段+不烧字幕，确定性渲染不落千张 PNG | 出片渲染（可选）｜FFmpeg 图片序列加 xfade（`scripts/build_comic_video.py`）｜整页顺序播放，加入封面信息页，切页点对齐口播段，不烧字幕；确定性渲染，不生成上千张 PNG 中间帧 | +→加/，；不落千张→不生成上千张…中间帧 | 符号、口语 |
| 13 | 混音（可选）｜`scripts/mix_audio.py`｜ffmpeg adelay+amix 按时间轴定位混入，BGM 0.15 不压人声 | 混音（可选）｜`scripts/mix_audio.py`｜以 ffmpeg adelay 与 amix 按时间轴定位混入，BGM 音量 0.15，不压人声 | 补"以"；+→与；BGM 0.15→BGM 音量 0.15 | 缺谓、符号 |
| 14 | 导出归档｜主 agent 写文件｜三件套+视频写入 `{项目}/内容/` 与 `{项目}/视频/` | 导出归档｜主 agent 写文件｜三件套与视频写入 `{项目}/内容/` 与 `{项目}/视频/` | +→与 | 符号 |
| 15 | > 出片是**确定性渲染**（脚本+FFmpeg），不走 AI 视频（Seedance/GenerateVideo）。 | > 出片是**确定性渲染**（脚本与 FFmpeg），不采用 AI 视频（Seedance/GenerateVideo）。 | +→与；走→采用 | 符号、口语 |
| 16 | `## 怎么操作（SOP 全内联）` | `## 操作步骤（规程全文内联）` | SOP→规程 | 生造（SOP） |

## 三、Step 0 收集素材（原第 42 至 54 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 17 | 1. **扫描章目录**，确认素材源： | 1. **扫描章目录**，确认素材来源： | 素材源→素材来源 | 缺字、压失 |
| 18 | 成品图（含对白/旁白/水印，**优先**——投放市场的真实样子，读图与出片首选）；`output/page1~N.png` 生图（无字/无水印，**回退替补**——分享缺页时用对应页补齐，标注「成品图缺失，用生图回退」） | 成品图（含对白、旁白与水印，**优先**，因为这是投放市场的真实样子，读图与出片首选）；`output/page1~N.png` 生图（无字、无水印，**回退替补**，在分享缺页时以对应页补齐，并标注「成品图缺失，用生图回退」） | /→、；破折号→逗号，补"因为/在…时/并" | 符号、硬切 |
| 19 | `导演卡.md`（可选：剧情因果链/高光名场面/旁白文案，卖点识别的保真锚点） | `导演卡.md`（可选：剧情因果链、高光名场面、旁白文案，卖点识别的保真锚点） | /→、 | 符号 |
| 20 | OC/封面图（可选，种草/推广的视觉素材，通常在 `成品/` 或 `漫画/assets/characters/`） | OC/封面图（可选，种草与推广的视觉素材，通常在 `成品/` 或 `漫画/assets/characters/`） | /→与 | 符号 |
| 21 | 用 PIL 记录每页图片数量与尺寸（供视觉描述/画布规划），**记录每页来源（分享/output）**。 | 用 PIL 记录每页图片数量与尺寸（供视觉描述与画布规划），**记录每页来源（分享或 output）**。 | /→与/或 | 符号 |
| 22 | 产出 `素材清单.md`：每页图片路径 + 来源 + 尺寸 + 文字锚点 + 导演卡摘要 + OC/封面路径。 | 产出 `素材清单.md`：每页图片路径、来源、尺寸、文字锚点、导演卡摘要与 OC/封面路径。 | +→、 | 符号 |
| 23 | **缺件兜底**：缺 `页面配置.json` → 跳过文字锚点纯读图（仍可走，锚点会弱）；缺 `导演卡.md` → 卖点识别只靠读图+页面配置；`分享/` 与 `output/` 都缺页 → 如实报告缺图，不编造。 | **缺件兜底**：缺 `页面配置.json` 时，跳过文字锚点而只做读图，仍可执行，但锚点会偏弱；缺 `导演卡.md` 时，卖点识别只依靠读图与页面配置；`分享/` 与 `output/` 都缺页时，如实报告缺图，不得编造。 | →→时；纯读图→只做读图；仍可走→仍可执行；靠→依靠；不编造→不得编造 | 符号、口语 |
| 24 | **完成判定**：成品图优先已检查｜缺页回退已标注｜文字锚点已提取｜素材清单已写入（含来源标注）。 | **完成判定**：成品图优先已检查；缺页回退已标注；文字锚点已提取；素材清单已写入，且含来源标注。 | ｜→； | 符号 |

## 四、Step 1 读图理解（原第 64 至 71 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 25 | **展示模板**列：按画面特征映射 `T1`推近（特写）/`T2`拉远（全景）/…+ 聚焦点，供 Step 2 口播脚本引用 | **展示模板**列：按画面特征映射 `T1` 推近（特写）、`T2` 拉远（全景）…与聚焦点，供 Step 2 口播脚本引用 | /→、；+→与 | 符号 |
| 26 | 2. 单页被安全审查拦截 → 用该页 `页面配置.json` 文字锚点 + `导演卡.md` 因果链补全画面理解，标注 `[读图被拦截，配置文字兜底]`；**禁止因读图失败中断管线** | 2. 单页被安全审查拦截时，用该页 `页面配置.json` 文字锚点与 `导演卡.md` 因果链补全画面理解，并标注 `[读图被拦截，配置文字兜底]`。**不得因读图失败中断管线** | →→时；+→与；禁止→不得 | 符号 |
| 27 | 4. **全章卖点识别**（口碑内容唯一素材源…）：剧情主线（一句话）｜高光/名场面（哪几页最抓眼球：视觉奇观/情绪爆发/剧情转折）｜观众爽点/悬念（为什么想看下去）｜可安利卖点（人设/画风/世界观/剧情冲突）。 | 4. **全章卖点识别**（口碑内容的唯一素材来源…）：剧情主线，一句话概括；高光与名场面，写明哪几页最抓眼球，例如视觉奇观、情绪爆发、剧情转折；观众爽点与悬念，即观众为什么想看下去；可安利卖点，包括人设、画风、世界观、剧情冲突。 | 唯一素材源→唯一素材来源；｜→；；/→、；补"概括/写明/即/包括" | 符号、缺谓 |
| 28 | 5. 产出 `画面理解.md`（逐页五要素表 + 全章卖点识别）。 | 5. 产出 `画面理解.md`（逐页五要素表与全章卖点识别）。 | +→与 | 符号 |
| 29 | **完成判定**：所有页已处理（含拦截页兜底）｜每页五要素齐全｜卖点识别完成｜已落盘。 | **完成判定**：所有页已处理，含拦截页兜底；每页五要素齐全；卖点识别完成；已写入。 | ｜→；；落盘→写入 | 符号、口语 |

## 五、Step 2 内容创作（原第 75 至 120 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 30 | **素材驱动 6 步创作法**（先想清楚再写，借 brand 骨架，"价值点"换成漫画"剧情卖点"）： | **素材驱动的六步创作法**（先想清楚再写，沿用 brand 骨架，把"价值点"换成漫画的"剧情卖点"）： | 6 步→六步；借→沿用；补"把/的" | 缺谓、压失 |
| 31 | 1. **提炼剧情真相**：这部漫画/这一章最打动人的是什么（从画面理解的卖点识别提炼） | 1. **提炼剧情真相**：找出这部漫画或这一章最打动人之处，从画面理解中的卖点识别提炼。 | 补"找出/之处"；/→或 | 缺主、符号 |
| 32 | 2. **立定立意**：这套内容想让观众带走的一句话（北极星） | 2. **立定立意**：确定这套内容想让观众带走的一句话，即北极星。 | 补"确定"；括号→即 | 缺谓 |
| 33 | 3. **选择角度**：从剧情卖点选切入（强钩子/悬念/情感/视觉奇观） | 3. **选择角度**：从剧情卖点中选择切入角度，如强钩子、悬念、情感、视觉奇观。 | 选切入→选择切入角度；/→、；补"如" | 缺谓、符号 |
| 34 | 4. **铺设结构**：三件套功能位——口播=推进+情绪线；种草=感受+安利；推广=钩子+互动 | 4. **铺设结构**：明确三件套的功能位，口播负责推进与情绪线，种草负责感受与安利，推广负责钩子与互动。 | 破折号→逗号；=→负责；+→与 | 符号、缺主 |
| 35 | 5. **逐镜写作**：三件套逐条写，卖点全部来自画面理解 | 5. **逐镜写作**：三件套逐条写作，卖点全部来自画面理解。 | 写→写作 | 缺谓 |
| 36 | 6. **闸门自检**：删掉画面描述只看文字，验证能讲清/有钩子/无虚构/像真人说话 | 6. **闸门自检**：删掉画面描述，只看文字，验证能否讲清、是否有钩子、是否无虚构、是否像真人说话。 | 补"否/是否"；/→、 | 缺谓、符号 |
| 37 | **template**：该页展示模板（`T1`推近/`T2`拉远/…），按情绪线选 | **template**：该页展示模板（`T1` 推近、`T2` 拉远…），按情绪线选择 | /→、；选→选择 | 符号 |
| 38 | **focus**（仅 T1/T3/T4 需要）：聚焦点或扫过方向（如 `T1@镜中白狗`、`T3@左→右`）；T2/T5 填 `—` | **focus**（仅 T1/T3/T4 需要）：聚焦点或扫过方向，如 `T1@镜中白狗`、`T3@左→右`；T2/T5 填 `—`。 | 括号→逗号；补句号 | 符号 |
| 39 | **写作总原则**：①卖点全部来自画面理解（铁律❌3，禁虚构）②平台语法差异化（口播口语化叙述/种草感性安利/推广短平快钩子…）③钩子优先，开头不平铺直叙④情绪线贯穿（如"痛醒→陌生→…"递进）⑤每页至少 1 句…总时长 ~60s（约 240-300 字）…⑥平台写法细节参照… | **写作总原则**：一、卖点全部来自画面理解（铁律❌3，不得虚构）；二、平台语法差异化，口播用口语化叙述，种草用感性安利，推广用短平快钩子…；三、钩子优先，开头不平铺直叙；四、情绪线贯穿，例如按"痛醒、陌生…"递进；五、每页至少一句…总时长约 60 秒（约 240 至 300 字）…；六、平台写法细节参照… | ①→一；禁→不得；/→、/用；~→约；→→、 | 符号、口语 |
| 40 | **🚪 确认闸门**：产出三件套后**停下来等老板确认**（铁律❌4）。确认后才进 Step 3/4/5（若要视频）+ Step 6 归档；只要文本不出片则跳过 Step 3/4/5 直接归档。 | **🚪 确认闸门**：产出三件套后**须停下来等老板确认**（铁律❌4）。确认后方可进入 Step 3/4/5（如需视频）与 Step 6 归档；只要文本、不出片时，跳过 Step 3/4/5，直接归档。 | 补"须/方可"；+→与；若要→如需；则→时 | 缺谓、符号 |
| 41 | **完成判定**：口播有钩子+每页≥1句绑 page+情绪线递进+~60s+每句有 template/focus｜种草五要素齐｜推广五要素齐｜三件套卖点无虚构｜**已等老板确认**（未确认禁止进入出片/归档）。 | **完成判定**：口播有钩子，每页至少一句并绑定 page，情绪线递进，时长约 60 秒，每句都有 template 与 focus；种草五要素齐全；推广五要素齐全；三件套卖点无虚构；**已等老板确认**（未确认不得进入出片或归档）。 | +→，；≥1→至少一；~→约；｜→；；齐→齐全；禁止→不得；/→或 | 符号、口语 |

## 六、Step 3 TTS 配音（原第 124 至 143 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 42 | .env 则复制 `scripts/.env.example` 填入。key 获取：`https://console.volcengine.com/voice` → 开通豆包语音合成大模型 → API Key 管理复制 X-Api-Key）。 | .env 时复制 `scripts/.env.example` 并填入。key 获取方式：访问 `https://console.volcengine.com/voice`，开通豆包语音合成大模型，然后在 API Key 管理页复制 X-Api-Key）。 | 则→时；补"并"；获取→获取方式；→→访问/然后 | 符号、缺谓 |
| 43 | seed-audio-1.0 是自然语言驱动的音频大模型，括号指令调节念法且**不会被念出**（ASR 验证过）。 | seed-audio-1.0 是自然语言驱动的音频大模型，括号指令用于调节念法，且**不会被念出**，此点经 ASR 验证。 | 调节念法→用于调节念法；ASR 验证过→此点经 ASR 验证 | 缺谓 |
| 44 | 2. **整段一次合成**（含封面钩子句 + 正文全部，避免逐句拼接割裂/语速不均）： | 2. **整段一次合成**（含封面钩子句与正文全部内容，避免逐句拼接造成割裂或语速不均）： | +→与；补"内容/造成"；/→或 | 符号、缺谓 |
| 45 | key 读取优先级：`--api-key` > 环境变量 `VOLC_ARK_API_KEY` > `scripts/.env`，三者皆缺脚本报错并提示配置 | key 读取优先级依次为 `--api-key`、环境变量 `VOLC_ARK_API_KEY`、`scripts/.env`；三者皆缺时脚本报错并提示配置 | >→依次为/、；补"时" | 符号 |
| 46 | **统一 `--speech-rate +5`**（v0.11 终稿：+8 偏快且抹平情绪，+5 从容让情绪指令有发挥空间）；必要时 `--loudness-rate/--pitch-rate` 微调 | **统一使用 `--speech-rate +5`**（v0.11 终稿：+8 偏快且抹平情绪，+5 更从容，让情绪指令有发挥空间）；必要时用 `--loudness-rate`、`--pitch-rate` 微调 | 补"使用/用"；从容→更从容；/→、 | 缺谓、符号 |
| 47 | 正文各段依次）。对齐必须用纯文本——情绪指令不进语音、不占时长。 | 正文各段依次）。对齐必须用纯文本，因情绪指令不进语音、不占时长。 | 破折号→逗号，补"因" | 硬切、缺谓 |
| 48 | **失败处理**：整段合成失败重试整段；反复失败换 `--speaker` 备用音色，或对情绪指令做口语化微调。 | **失败处理**：整段合成失败时重试整段；反复失败时更换 `--speaker` 备用音色，或对情绪指令做口语化微调。 | 补"时"；换→更换 | 缺谓 |
| 49 | **完成判定**：整段一次合成无割裂｜对齐.json 含全部句（封面 seq0+正文）的 seq/start/end/duration｜语速 +5 情绪指令生效（有起伏、无指令被念出）｜总时长与画面偏差可接受。 | **完成判定**：整段一次合成，无割裂；对齐.json 含全部句（封面 seq0 与正文）的 seq/start/end/duration；语速 +5，情绪指令生效，有起伏且无指令被念出；总时长与画面的偏差可接受。 | ｜→；；+→与；补"的" | 符号 |

## 七、Step 4 出片渲染（原第 149 至 185 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 50 | 页面形态｜**整页顺序播放**（漫画是分格叙事，禁止 Ken Burns 缩放） | 页面形态｜**整页顺序播放**（漫画是分格叙事，不得使用 Ken Burns 缩放） | 禁止→不得使用 | 口语 |
| 51 | 封面信息页｜**开头加章节封面/信息页**（长图顶部标题区，含书名+章节号） | 封面信息页｜**开头加章节封面与信息页**（长图顶部标题区，含书名与章节号） | /→与；+→与 | 符号 |
| 52 | 字幕｜**不烧字幕**（成品图已内嵌文字，避免重复/重叠） | 字幕｜**不烧字幕**（成品图已内嵌文字，避免重复或重叠） | /→或 | 符号 |
| 53 | 切页点｜**对齐口播段边界**（每页末句 end，xfade 过渡中心=切页点） | 切页点｜**对齐口播段边界**（每页末句 end，xfade 过渡中心即切页点） | =→即 | 符号 |
| 54 | **输入**：老板确认的口播脚本｜漫画页（按素材清单来源记录取图…）｜封面/信息页（`分享/长图-*.png` 顶部标题区 或 `成品/封面/*-final.jpg`…）｜`对齐.json`（每句 start/end）｜`分镜标注.json`（…页→seq 映射）。 | **输入**：老板确认的口播脚本；漫画页，按素材清单的来源记录取图…；封面或信息页，取 `分享/长图-*.png` 顶部标题区或 `成品/封面/*-final.jpg`…；`对齐.json`（每句 start 与 end）；`分镜标注.json`（…页与 seq 的映射）。 | ｜→；；/→或；start/end→start 与 end；页→seq→页与 seq | 符号、缺谓 |
| 55 | 1. **准备封面信息页**：优先 `分享/长图-*.png` 顶部标题区——按竖版宽高比裁顶部 `h = w/0.5625`，LANCZOS 缩放到 1080×1920 存 `cover.png`；无长图用 `成品/封面/*-final.jpg`（已竖版，直接缩放）。 | 1. **准备封面信息页**：优先取 `分享/长图-*.png` 顶部标题区，按竖版宽高比裁顶部 `h = w/0.5625`，用 LANCZOS 缩放到 1080×1920，存为 `cover.png`；无长图时用 `成品/封面/*-final.jpg`（已竖版，直接缩放）。 | 补"取/用/为/时"；破折号→逗号 | 缺谓、硬切 |
| 56 | 2. **切页点计算**（脚本内已实现）：每页口播窗口 = 该页首句 start → 末句 end；平滑处理消除句间 gap/重叠（`start = max(start, 前句end)`）保证切页点连续。 | 2. **切页点计算**（脚本内已实现）：每页口播窗口为该页首句 start 至末句 end；经平滑处理消除句间间隙与重叠（`start = max(start, 前句end)`），保证切页点连续。 | =→为；→→至；gap→间隙；/→与；补"经/，" | 符号、缺谓 |
| 57 | `--page-seq`：页文件名→口播段 seq 列表（0-based）；缺省一页一段 | `--page-seq`：页文件名到口播段 seq 列表的映射（0-based）；缺省为一页一段 | →→到…的映射；缺省→缺省为 | 符号、缺谓 |
| 58 | 每输入 `-loop 1 -t dur -i img`；每输入 filter：背景 scale 1080:1920 + 模糊 + 压暗，前景等比缩放居中 overlay（分享页 820×1150 → 宽 1080 高 `FG_H=1515` 垂直居中） | 每个输入以 `-loop 1 -t dur -i img` 处理；每个输入的 filter 为：背景 scale 1080:1920，加模糊与压暗，前景等比缩放居中 overlay（分享页 820×1150，缩放为宽 1080、高 `FG_H=1515`，垂直居中） | 补"个/以…处理/的…为"；+→加/与；→→，缩放为 | 符号、缺谓 |
| 59 | 串联 `xfade`：`transition=fade duration=TRANS`，`offset = 上一页末句end - TRANS/2`；`TRANS=0.4`（比 0.5 利落） | 以 `xfade` 串联：`transition=fade duration=TRANS`，`offset = 上一页末句end - TRANS/2`；`TRANS=0.4`（比 0.5 利落） | 串联→以…串联 | 缺谓 |
| 60 | 产出 `第{N}章-v{版本}.mp4`（无音轨中间候选）+ 自动混入整段配音 → `第{N}章-配音-v{版本}-final.mp4`（命名遵循落盘规范 §3.1b） | 产出 `第{N}章-v{版本}.mp4`（无音轨中间候选），并自动混入整段配音，得到 `第{N}章-配音-v{版本}-final.mp4`（命名遵循落盘规范 §3.1b） | +→，并/得到；→→， | 符号、硬切 |
| 61 | 抽封面/各切页点/各页中段帧**逐张 Read 审查**：封面标题完整｜无黑边变形｜每页取景完整｜切页点前后已切到对应页｜无水印残留侵占｜无字幕与画面文字重叠。发现问题回改再出，直到通过。 | 抽取封面、各切页点与各页中段帧，**逐张 Read 审查**：封面标题完整；无黑边变形；每页取景完整；切页点前后已切到对应页；无水印残留侵占；无字幕与画面文字重叠。发现问题后回改重出，直到通过。 | 抽→抽取；/→、；｜→；；补"后/…重出" | 符号、缺谓 |
| 62 | **完成判定**：开头封面信息页（书名/章节号清晰无黑边）｜整页顺序播放无 Ken Burns｜切页点对齐口播段边界｜无字幕与画面文字重叠｜1080×1920 / 30fps / 时长与配音一致（probe 校验）。 | **完成判定**：开头封面信息页（书名与章节号清晰、无黑边）；整页顺序播放，无 Ken Burns；切页点对齐口播段边界；无字幕与画面文字重叠；1080×1920、30fps、时长与配音一致（probe 校验）。 | /→与/、；｜→； | 符号 |
| 63 | > 出片是确定性渲染（FFmpeg 图片序列+xfade），不走 AI 视频。 | > 出片是确定性渲染（FFmpeg 图片序列加 xfade），不采用 AI 视频。 | +→加；走→采用 | 符号、口语 |

## 八、Step 5 混音、Step 6 归档、铁律与前置（原第 189 至 281 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 64 | 1. **BGM 选曲**：按漫画**赛道 × 主要情绪段**从 `references/bgm-guide.md` 的「赛道 × 情绪 → 参考曲目」表选定（…紧张/高潮段配 `fever pitch`…），BGM 放 `{项目}/视频/bgm/bgm-*.mp3`。老板已指定 BGM 则直接用，不重新选。 | 1. **BGM 选曲**：按漫画的**赛道与主要情绪段**，从 `references/bgm-guide.md` 的「赛道 × 情绪 → 参考曲目」表选定（…紧张与高潮段配 `fever pitch`…），BGM 放 `{项目}/视频/bgm/bgm-*.mp3`。老板已指定 BGM 时直接使用，不重新选。 | 赛道 × →赛道与；/→与；则→时；用→使用 | 符号、缺谓 |
| 65 | 有 BGM 传 `--bgm`（音量 0.15 循环铺满全程、不盖人声）；无 BGM 省略，脚本自动只混配音 | 有 BGM 时传 `--bgm`（音量 0.15，循环铺满全程，不盖人声）；无 BGM 时省略，脚本自动只混配音 | 补"时"；、→， | 缺谓、符号 |
| 66 | 3. **probe 校验**：分辨率 1080×1920｜时长正确｜含 AAC 音轨。 | 3. **probe 校验**：分辨率 1080×1920；时长正确；含 AAC 音轨。 | ｜→； | 符号 |
| 67 | **完成判定**：BGM（若有）匹配赛道×情绪且 0.15 不压人声｜口播清晰、落在对应画面时间点｜probe 三项通过｜最终视频已交付。只要文本三件套本步可跳过。 | **完成判定**：BGM（若有）匹配赛道与情绪，且音量 0.15 不压人声；口播清晰，落在对应画面时间点；probe 三项通过；最终视频已交付。只要文本三件套时，本步可跳过。 | ×→与；补"音量/时"；｜→； | 符号、缺谓 |
| 68 | 1. **确认产出完整**：三件套 + 素材清单 + 画面理解均已存在且内容完整。 | 1. **确认产出完整**：三件套、素材清单与画面理解均已存在且内容完整。 | +→、与 | 符号 |
| 69 | 2. **写入目标目录**：文本三件套 → `{项目}/内容/`（确认后即内容成品）；视频（若出片）→ `{项目}/视频/`（…）。命名遵循落盘规范 §3.1b。 | 2. **写入目标目录**：文本三件套写入 `{项目}/内容/`（确认后即内容成品）；视频（若出片）写入 `{项目}/视频/`（…）。命名遵循落盘规范 §3.1b。 | →→写入 | 符号、缺动 |
| 70 | **完成判定**：三件套+素材清单+画面理解全部在目标目录｜（若出片）final 视频已归档｜内容索引.md 已写入｜已向老板汇报。 | **完成判定**：三件套、素材清单与画面理解全部在目标目录；若出片，final 视频已归档；内容索引.md 已写入；已向老板汇报。 | +→、与；｜→；；补"若/，" | 符号 |
| 71 | > 落盘遵循 `pop-visual-pipeline/references/落盘规范.md` §3.1b §五 §六：文本三件套确认后在 `内容/`（内容成品），视频成品版本化+`-final`。 | > 写入遵循 `pop-visual-pipeline/references/落盘规范.md` §3.1b §五 §六：文本三件套确认后放在 `内容/`（内容成品），视频成品版本化并加 `-final`。 | 落盘（动词）→写入；在→放在；+→并加 | 口语（黑话）、符号 |
| 72 | ❌1 **先读图后产出** — …禁止跳过读图直接凭项目记忆编造内容 | ❌1 **先读图后产出** — …不得跳过读图直接凭项目记忆编造内容 | 禁止→不得 | 口语 |
| 73 | ❌2 **读图为主、配置文字兜底** — …`页面配置.json`/`导演卡.md` 文字做锚点；…禁止因读图失败中断 | ❌2 **读图为主、配置文字兜底** — …`页面配置.json` 与 `导演卡.md` 的文字做锚点；…不得因读图失败中断 | /→与；禁止→不得 | 符号 |
| 74 | ❌3 …禁止虚构书中不存在的剧情或卖点 | ❌3 …不得虚构书中不存在的剧情或卖点 | 禁止→不得 | 口语 |
| 75 | ❌4 …确认后才进配音/出片/归档；禁止一次性直接跑完全流程｜方向跑偏，三件套白做，返工三份 | ❌4 …确认后才进配音、出片与归档；不得一次性直接跑完全流程｜方向跑偏，三件套作废，需返工三份 | /→、与；禁止→不得；白做→作废，补"需" | 符号、口语 |
| 76 | ❌5 **出片走确定性渲染，不走 AI 视频** — 用脚本+FFmpeg 合成（整页顺序播放），禁止 Seedance/GenerateVideo 按秒生成的 AI 视频 | ❌5 **出片采用确定性渲染，不采用 AI 视频** — 用脚本与 FFmpeg 合成（整页顺序播放），不得使用 Seedance/GenerateVideo 按秒生成的 AI 视频 | 走→采用；+→与；禁止→不得使用 | 口语、符号 |
| 77 | ❌6 **产出落盘版本化+final** — …禁止裸名 `成品.mp4`/`成品-配音.mp4`；`preview/frames/audio/` 是过程（见落盘规范 §3.1b §六） | ❌6 **产出写入版本化并加 final** — …不得使用裸名 `成品.mp4` 或 `成品-配音.mp4`；`preview/frames/audio/` 是过程（见落盘规范 §3.1b §六） | 落盘→写入；+→并加；禁止裸名/→不得使用裸名…或 | 口语、符号 |
| 78 | 3. 出片需：Python 3.8+ + Pillow + imageio-ffmpeg（完整版 ffmpeg） | 3. 出片需：Python 3.8+、Pillow、imageio-ffmpeg（完整版 ffmpeg） | +→、 | 符号 |

## 本清单（上）小结

- 覆盖：`SKILL.md` 全文，共 **78 个句子单元**，全部落在规程句与说明文字上。
- **改得最集中的三类**：一、**符号串当句**（`+`、`/`、`｜`、`→`、`=`、`~`、`×`、`=`、破折号硬切），原句拿关系符号代替连接词；二、**缺主语缺谓语**（"素材源""自动读取""选切入""调节念法"一类压缩单元）；三、**语域降档与黑话**（落盘、直执、SOP、不落千张、白做），逐处收为书面。
- **黑话替换**：`SOP→规程`、`落盘（动词）→写入`、`直执→直接执行`、`子agent→子 agent`。
- **未动一处**：路径、脚本名、命令、字段名、枚举值、阈值、文件名模板、代码块，一处未变；条款一条未删、未新增；表格列与段落顺序未动。
- 未改动登记见 `改动清单-下.md` 末节（覆盖两个文件的共用保留项）。
