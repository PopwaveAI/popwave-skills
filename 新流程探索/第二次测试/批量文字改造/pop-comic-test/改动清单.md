# pop-comic-test 改动清单

> **修改前**＝`skills/pop-comic-test/SKILL.md`（原版 63 行）
> **修改后**＝`SKILL-改后.md`
> 判据三问：**谁**（主语在不在）、**做什么**（动作在不在）、**凭什么判断**（依据或正反例在不在）。
> 本轮做两件事：一、把压缩单元展开成完整句子；二、把语域收到书面。
> 病类缩写：缺主＝缺主语｜缺谓＝缺谓语｜缺动＝缺动作词｜硬切＝硬切短句｜符号＝符号串当句｜口语＝语域降档｜生造＝生造词｜指代＝指代不明｜压失＝压缩到失准｜比喻＝装饰性比喻。

## 一、前言与边界（原第 3 至 15 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 1 | 画风三组测试。用固定三组模板（场景/角色/多格剧情）逐画风验证 DNA 执行力… | 本技能是画风三组测试。用固定三组模板（场景、角色、多格剧情）逐个画风验证 DNA 执行力… | 补主语；逐画风→逐个画风；/→、 | 缺主、符号 |
| 2 | `## 这个 Skill 做什么` | `## 职责范围` | 口语式标题收为书面 | 口语 |
| 3 | **T1 场景向**：环境为主，验证画风能否撑起全景/氛围 | **T1 场景向**：环境为主，验证画风能否撑起全景与氛围 | /→与 | 符号 |
| 4 | **T2 角色立绘向**：角色为主，验证画风能否撑起角色/主观特征 | **T2 角色立绘向**：角色为主，验证画风能否撑起角色与主观特征 | /→与 | 符号 |
| 5 | 一个画风光"适合场景"不够…三组全过 = 画风稳定；部分过 = 画风有偏科 | 一个画风仅"适合场景"不够…三组全过即画风稳定；部分过即画风有偏科 | 光→仅；=→即 | 符号、口语 |

## 二、Step 1 至 Step 4（原第 19 至 38 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 6 | `### Step 1: 选画风范围 → 读 DNA 库` | `### Step 1: 选画风范围，读取 DNA 库` | →→，读取 | 符号、硬切 |
| 7 | 每次取该画风的 `dna` + `constraint` 字段注入三组模板 | 每次取该画风的 `dna` 与 `constraint` 字段，注入三组模板 | +→与 | 符号、缺谓 |
| 8 | `### Step 2: 生成三组 config → scripts/build_3test.py` | `### Step 2: 生成三组 config，执行 scripts/build_3test.py` | →→，执行 | 符号、缺动 |
| 9 | 三组模板的 composition/lighting/scene/character 全部固定，只有 DNA+constraint 随画风变化 | 三组模板的 composition、lighting、scene、character 全部固定，只有 DNA 与 constraint 随画风变化 | /→、；+→与 | 符号 |
| 10 | `### Step 3: 批量生成 → ../pop-visual-shared/scripts/batch_test.py` | `### Step 3: 批量生成，执行 ../pop-visual-shared/scripts/batch_test.py` | →→，执行 | 符号、缺动 |
| 11 | 导出三张图的任务清单（T1_scene / T2_character / T3_comic）到 `generation_tasks.json` | 导出三张图的任务清单（T1_scene、T2_character、T3_comic）到 `generation_tasks.json` | /→、 | 符号 |
| 12 | 主 agent 读任务清单，用 `image_generate` 工具逐条生成（生图统一走 `image_generate`，不直连 API） | 主 agent 读任务清单，用 `image_generate` 工具逐条生成（生图统一使用 `image_generate`，不直连 API） | 走→使用 | 口语 |
| 13 | 自动落盘 `pe-log.json` | 自动写入 `pe-log.json` | 落盘→写入 | 口语（黑话） |
| 14 | `### Step 4: 判定 + 汇总 → 结果表` | `### Step 4: 判定与汇总，输出结果表` | +→与；→→，输出 | 符号、硬切 |
| 15 | 输出"画风通过判定表"：T1/T2/T3 各过/不过 + 偏科项标注 | 输出"画风通过判定表"：T1、T2、T3 各过或不过，并标注偏科项 | /→、；+→并标注 | 符号、缺动 |
| 16 | 不过的画风 → 标记并回媒体库（交给 `pop-visual-style` 微调 DNA） | 不过的画风，标记并回媒体库（交给 `pop-visual-style` 微调 DNA） | →→， | 符号 |

## 三、质量红线（原第 44 至 47 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 17 | ❌1 必须走固定脚本 — 禁止现场手写提示词或临时改三组模板 | ❌1 必须走固定脚本 — 不得现场手写提示词或临时修改三组模板 | 禁止→不得；临时改→临时修改 | 口语、缺谓 |
| 18 | ❌2 控制变量 — 三组模板的非画风部分（构图/光影/场景/角色）永久固定，禁止掺入画风特征 | ❌2 控制变量 — 三组模板的非画风部分（构图、光影、场景、角色）永久固定，不得掺入画风特征 | /→、；禁止→不得 | 符号 |
| 19 | ❌3 三组全测 — 每个画风必须跑 T1/T2/T3 三组，禁止只测一组 | ❌3 三组全测 — 每个画风必须执行 T1、T2、T3 三组，不得只测一组 | 跑→执行；/→、；禁止→不得 | 口语、符号 |

## 未改动

| 段落／内容 | 理由 |
|:--|:--|
| 首行 `# pop-comic-test` | 首行标题不得改；未新增 frontmatter |
| 版本句 `> 画风三组测试…v1.3.0` 的版本号部分、`## 版本` 段、`见 CHANGELOG.md` | 历史记录（版本号与变更日志指向），按须知第五节不改 |
| `pop-visual-style`、`pop-visual-shared`、`../pop-visual-style/references/文风DNA-library.json`、`scripts/build_3test.py`、`batch_test.py`、`generation_tasks.json`、`pe-log.json`、`--style-name`、`--all`、`--config`、`--out-dir`、`--seed`、`dna`、`constraint`、`styles`、`image_generate`、`TEMPLATES`、`T1_scene`、`T2_character`、`T3_comic` | 路径、脚本名、字段名、枚举值、常量名，机器可读内容 |
| `scripts/build_3test.py --style-name "X" --out config.json`、`--all --out-dir 素材/测试`、`../pop-visual-shared/scripts/batch_test.py --config ...` | 命令原文，机器可读内容 |
| `**核心价值**：…才算真正可用`、`**边界**：本 skill 只做**测试与判定**，不生产最终交付图`、Step 1 三条子弹、Step 2 第 1、3 条、Step 3 第 1 条、Step 4 第 1 条、❌4 | 无压缩、无语域降档，原样保留 |
| `## 速查表` 表头与各行（`我要`／`读/执行什么`／`什么时候用`，含 `Read/execute` 式表头） | 表格列与结构不动；单元格为路径与命令 |
| ❌4 `**固定种子** — 用 `--seed` 固定，保证可复现对比` | 已属书面陈述，无压缩 |
