# pop-critic-panel 改动清单

> **修改前**＝`skills/pop-critic-panel/SKILL.md`（原版 119 行）
> **修改后**＝`SKILL-改后.md`
> 判据三问：**谁**（主语在不在）、**做什么**（动作在不在）、**凭什么判断**（依据或正反例在不在）。
> 本轮做两件事：一、把压缩单元展开成完整句子；二、把语域收到书面。
> 病类缩写：缺主＝缺主语｜缺谓＝缺谓语｜缺动＝缺动作词｜硬切＝硬切短句｜符号＝符号串当句｜口语＝语域降档｜生造＝生造词｜指代＝指代不明｜压失＝压缩到失准｜比喻＝装饰性比喻。
> 未改动的段落登记在文末「未改动登记」，均写明理由。

## 一、前言与边界（原第 3 至 16 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 1 | 多子agent盲评：不同视野隔离评审同一靶子，单轮互不可见，主agent回收裁决。 | 本技能负责多子 agent 盲评：以相互隔离的不同视野评审同一靶子，各视野在单轮内互不可见，最终由主 agent 回收裁决。 | 补主语；中英文间加空格 | 缺主、缺谓（冒号当谓） |
| 2 | `## 这个 Skill 做什么` | `## 职责范围` | 口语式标题收为书面 | 口语 |
| 3 | **输入**：靶子（1 个或多个待审文件路径）＋可选视角点名（默认四团，用户可加自定义视角）。 | **输入**：靶子，即一个或多个待审文件的路径；可选视角点名，默认为四团，用户可增加自定义视角。 | 1 个→一个；可加→可增加 | 符号（＋）、缺"即"、缺谓 |
| 4 | **过程**：主 agent 组团 → 并行派 N 个子 agent（各自独立上下文、各自初始提示词、各自材料清单）→ 单轮盲评 → 主 agent 回收裁决。 | **过程**：主 agent 组团，并行派出 N 个子 agent，每个子 agent 各自独立上下文、各自初始提示词、各自材料清单，随后进行单轮盲评，最后由主 agent 回收裁决。 | 箭头改为叙述连接词 | 符号（→）、缺谓 |
| 5 | **产出**：盲评报告（对话呈现＋落盘 `审查/盲评-{靶子名}-{YYYYMMDD}.md`）——刺点四分：**采纳 / 误报 / 冲突 / 存疑**。 | **产出**：盲评报告，在对话中呈现并写入 `审查/盲评-{靶子名}-{YYYYMMDD}.md`。刺点分四类：**采纳、误报、冲突、存疑**。 | 落盘→写入；四分→分四类 | 符号（＋、——、/）、缺谓 |
| 6 | - 只挑刺不改文：产出止于报告，改不改、怎么改，用户拍板。 | - 只挑刺，不改文：产出止于报告；改与不改、如何改，均由用户决定。 | 拍板→决定 | 缺主、硬切 |
| 7 | - 只做单轮，不做多轮互辩：盲评的价值就在隔离——子 agent 一旦互相看到对方结论会立场趋同，多视野就废了。冲突意见原样保留给用户，不安排"对辩和解"。 | - 只做单轮，不做多轮互辩：盲评的价值在于隔离。子 agent 一旦互相看到对方结论，立场便会趋同，多视野随之失效。冲突意见原样保留给用户，不安排"对辩和解"。 | 就在→在于；就废了→随之失效 | 符号（破折号硬切）、口语 |
| 8 | - 子 agent 只读不落盘：挑刺结论回传主 agent，落盘统一归主 agent。 | - 子 agent 只读不写入：挑刺结论回传主 agent，写入统一由主 agent 执行。 | 落盘→写入 | 缺主、缺动 |

## 二、Step 1 接收靶子并组团（原第 18 至 34 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 9 | `## 怎么操作（SOP 全内联）` | `## 操作步骤（规程全文内联）` | SOP→规程 | 生造（SOP） |
| 10 | `### Step 1 接靶子·组团` | `### Step 1 接收靶子并组团` | 接→接收；·→并 | 硬切、符号（·） |
| 11 | 1. 确认靶子：用户给的文件路径列表（章纲/幕白描/卷纲/设定档/融合立项稿/正文均可）。靶子含多个文件时视为一个整体评（如"卷1全部幕白描"）。 | 1. 确认靶子：靶子为用户给出的文件路径列表，章纲、幕白描、卷纲、设定档、融合立项稿、正文均可。靶子含多个文件时，视为一个整体评审，例如"卷1全部幕白描"。 | 整体评→整体评审；如→例如 | 符号（/）、缺谓、压失 |
| 12 | 2. 确认视角团：默认下表四团；用户点名增删或自带视角（一句视野描述即可入团）。无特殊要求不追问，直接开跑。 | 2. 确认视角团：默认为下表四团；用户可点名增删，或自带视角，提供一句视野描述即可入团。无特殊要求时不追问，直接开始执行。 | 开跑→开始执行 | 缺主、口语 |
| 13 | > 视野边界是刻意的信息不对称，不是疏漏：读者代表看不到设定，挑出来的才是真读者反应；考据不看剧情节奏，挑出来的才是纯设定问题。给子 agent 的 prompt 里要写明这一点，防止它"好心"去补读。 | > 视野边界是刻意设置的信息不对称，不是疏漏：读者代表看不到设定，其挑出的问题才是真实的读者反应；考据不看剧情节奏，其挑出的问题才是纯设定问题。组装子 agent 提示词时须写明这一点，防止子 agent"好心"补读。 | prompt→提示词；要写明→须写明 | 缺主、口语、指代（它） |

## 三、Step 2 组装子 agent 提示词（原第 36 至 60 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 14 | 每个子 agent 一份 prompt，按下面骨架填充（骨架全内联，直接复制用）： | 每个子 agent 一份提示词，按下列骨架填充。骨架全文内联，可直接复制使用。 | prompt→提示词；下面→下列 | 口语（直接复制用） |
| 15 | 【身份】你是一名{视角名}。{身份一句话}。用你这把专属的尺子评审下面的材料。 | 【身份】你是一名{视角名}。{身份一句话}。按本视角的评审尺评审下列材料。 | 专属尺子→评审尺；下面的→下列 | 比喻（尺子）、缺谓 |
| 16 | 只读：{材料清单（靶子＋该视角对照材料）}。禁止读其它任何文件——这是刻意设计的视野边界，不是疏漏；越界评审反而让结论失真。 | 只读：{材料清单（靶子＋该视角对照材料）}。不得读取任何其它文件。这是刻意设置的视野边界，不是疏漏；越界评审会让结论失真。 | 禁止读→不得读取；反而→删 | 符号（破折号硬切） |
| 17 | - 只挑刺不改文：给问题定位和修改方向，不替作者写文字。 | - 只挑刺，不改文：给出问题的定位与修改方向，不替作者写文字。 | 给→给出 | 硬切、缺谓 |
| 18 | - 每条刺必须给定位（文件＋章节/段落），给不出定位的整体感想不要。 | - 每条刺必须给出定位，即文件与章节或段落；给不出定位的整体感想不予采纳。 | 不要→不予采纳 | 符号（＋、/）、口语 |
| 19 | - 材料里查不到、看不懂的，直接说"看不到"，不猜不编。 | - 材料中查不到或看不懂的，直接写明"看不到"，不得猜测，不得编造。 | 直接说→直接写明；不猜不编→不得猜测，不得编造 | 口语 |
| 20 | - 挑最痛的 ≤10 条，按严重度从高到低排，不凑数。 | - 挑出最痛的至多 10 条，按严重度从高到低排列，不得凑数。 | ≤10→至多 10 条；不凑数→不得凑数 | 缺主、符号（≤） |
| 21 | 总评：一句话——这个视野看这个靶子的整体印象。 | 总评：一句话，写明本视野看这个靶子的整体印象。 | 这个视野→本视野 | 符号（破折号硬切）、缺谓 |

## 四、Step 3 派发（原第 62 至 66 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 22 | `### Step 3 派单（隔离纪律）` | `### Step 3 派发（隔离纪律）` | 派单→派发 | 口语 |
| 23 | - 各子 agent 并行派发，互不依赖。通道适配：popwave 走 `paopao_subagent_run`（purpose=critique，只读通道天然合规）；TRAE 走 Agent 工具（子代理只读类）。 | - 各子 agent 并行派发，互不依赖。通道适配如下：popwave 使用 `paopao_subagent_run`（purpose=critique，只读通道天然合规）；TRAE 使用 Agent 工具（子代理只读类）。 | 走→使用 | 口语 |
| 24 | - **盲评铁律**：每份 prompt 里只有该视角自己的材料，禁止携带其它 agent 的任何结论、口径或提示——包括主 agent 自己的预判。主 agent 在回收前不许"预点评"。 | - **盲评铁律**：每份提示词中只有该视角自己的材料，不得携带其它 agent 的任何结论、口径或提示，包括主 agent 自己的预判。主 agent 在回收前不得"预点评"。 | prompt→提示词；不许→不得 | 符号（破折号硬切） |
| 25 | - 靶子文本直接附进 prompt 或给路径让子 agent 自读均可；对照材料给路径＋点读指引。 | - 靶子文本直接附入提示词，或给出路径由子 agent 自行读取，两种方式均可；对照材料给出路径与查阅指引。 | prompt→提示词；点读→查阅 | 符号（＋）、口语 |

## 五、Step 4 回收校验（原第 68 至 71 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 26 | - 收齐全部回传后先校格式：缺定位的刺丢弃；超 10 条的砍到 10 条（保高严重度）；总评缺失的补一句"未给出"。 | - 收齐全部回传后先校格式：缺定位的刺直接丢弃；超过 10 条的砍到 10 条，保留高严重度者；总评缺失的补一句"未给出"。 | 保→保留……者 | 缺主、口语（砍到） |
| 27 | - 格式合规的刺点全部进入裁决，不做内容预筛选——筛选是 Step 5 的裁决动作，不能提前。 | - 格式合规的刺点全部进入裁决，不做内容预筛选。筛选属于 Step 5 的裁决动作，不得提前。 | 不能→不得 | 符号（破折号硬切）、口语 |

## 六、Step 5 四分裁决与出报告（原第 73 至 106 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 28 | `### Step 5 四分裁决·出报告` | `### Step 5 四分裁决与出报告` | ·→与 | 符号（·）、硬切 |
| 29 | 1. **合并去重**：同一定位被多个视野命中 → 合并为一条，标注命中视野数（多视野同点命中=大概率真问题，排前面）。 | 1. **合并去重**：同一定位被多个视野命中时，合并为一条，标注命中视野数。多视野同点命中，大概率是真问题，排在前列。 | 排前面→排在前列 | 符号（→、=）、缺主 |
| 30 | - **误报**：视野局限导致的假问题（如读者代表挑"背景没交代"——本来就该后文揭晓的悬念）。给一句误报理由。 | - **误报**：视野局限导致的假问题，例如读者代表挑出"背景没交代"，而该背景本就应由后文揭晓。须给出一句误报理由。 | 如→例如；给→须给出 | 符号（破折号硬切）、缺主 |
| 31 | - **冲突**：不同视野意见相反（如编辑要快、考据要全）→ 原样保留双方，标"留用户拍板"，主 agent 不替用户裁决。 | - **冲突**：不同视野意见相反，例如编辑要快、考据要全。原样保留双方，标注"由用户决定"，主 agent 不替用户裁决。 | 拍板→决定 | 符号（→）、口语 |
| 32 | - **存疑**：信息不足无法判断的，列出需要补什么材料才能裁。 | - **存疑**：信息不足无法判断的，列出需要补哪些材料才能裁定。 | 才能裁→才能裁定 | 缺字、缺谓 |
| 33 | 3. **落盘** `审查/盲评-{靶子名}-{YYYYMMDD}.md`，报告模板： | 3. **写入** `审查/盲评-{靶子名}-{YYYYMMDD}.md`，报告模板： | 落盘→写入 | 口语（黑话） |
| 34 | > 视角团：{实际参团列表}｜单轮盲评·子agent互不可见 | > 视角团：{实际参团列表}｜单轮盲评，子 agent 互不可见 | 子agent→子 agent；·→， | 符号（·）、缺标点 |
| 35 | `## 采纳（按严重度＋命中视野数排）` | `## 采纳（按严重度与命中视野数排列）` | ＋→与；排→排列 | 符号（＋）、缺动 |
| 36 | `## 冲突（视野打架，留用户拍板）` | `## 冲突（视野冲突，由用户决定）` | 打架→冲突；拍板→决定 | 口语 |
| 37 | 4. **落盘后半步=消毒**：`python skills/pop-snow-pipeline/scripts/deai_gate.py 审查/盲评-{靶子名}-{YYYYMMDD}.md --profile doc --json`——WARN 定位自查（评审措辞贴事实，装腔换大白话）、机械项 `--fix`，回执一行 `消毒:盲评报告\|PASS` 或 `WARN×N`。 | 4. **写入后的第二步是去 AI 味**：`python skills/pop-snow-pipeline/scripts/deai_gate.py 审查/盲评-{靶子名}-{YYYYMMDD}.md --profile doc --json`。WARN 项按报告定位自查，评审措辞须贴合事实，装腔之词改为大白话；机械项由 `--fix` 修正；并输出一行确认：`去AI味:盲评报告\|PASS` 或 `WARN×N`。 | 落盘→写入；消毒→去 AI 味；回执→确认 | 符号（=、破折号硬切）、生造（消毒） |
| 38 | 5. 对话呈现总裁决＋采纳清单＋冲突项，其余细节指向落盘文件。结束——不追着用户问"要不要改"，改靶子的指令由用户另发。 | 5. 对话呈现总裁决、采纳清单与冲突项，其余细节指向写入的文件。至此结束，不追问用户"要不要改"；修改靶子的指令由用户另行发出。 | 落盘→写入；追着问→追问；另发→另行发出 | 符号（＋、破折号硬切）、口语 |

## 七、红线与执行模式（原第 108 至 119 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 39 | 1. **盲评隔离**：子 agent prompt 禁止携带其它 agent 的任何结论或主 agent 的预判——单轮盲评，谁都不能被谁带节奏。 | 1. **盲评隔离**：子 agent 提示词不得携带其它 agent 的任何结论或主 agent 的预判。单轮盲评，谁都不能被谁带节奏。 | prompt→提示词；禁止→不得 | 符号（破折号硬切） |
| 40 | 2. **子 agent 只读不落盘**：结论回传主 agent，落盘归主 agent。 | 2. **子 agent 只读不写入**：结论回传主 agent，写入归主 agent。 | 落盘→写入 | 口语（黑话） |
| 41 | 4. **冲突留用户拍板**：主 agent 不替用户裁决视野冲突，只呈现双方。 | 4. **冲突由用户决定**：主 agent 不替用户裁决视野冲突，只呈现双方。 | 拍板→决定 | 口语 |
| 42 | 5. **查不到明说**：材料缺失或读不到就声明，不编刺点凑数。 | 5. **查不到须明说**：材料缺失或读不到即声明，不得编造刺点凑数。 | 就→即；不编→不得编造 | 缺主、口语 |
| 43 | 6. **产出止于报告**：不改正文、不改章纲，改靶子是用户拍板后另起管线环节的事。 | 6. **产出止于报告**：不改正文、不改章纲；修改靶子是用户决定后另起管线环节的事。 | 拍板→决定；改靶子→修改靶子 | 缺主、硬切 |
| 44 | 子 agent 并行派发（互不依赖，可同时跑）；主 agent 直执组团、组装、回收裁决与落盘。 | 子 agent 并行派发，互不依赖，可同时执行；主 agent 直接执行组团、组装、回收裁决与写入。 | 跑→执行；直执→直接执行；落盘→写入 | 生造（直执）、符号（括号） |

## 未改动登记

| 段落／内容 | 理由 |
|:--|:--|
| 第 3 行版本句：`v1.0.0：初始版。完整版本历史见 [CHANGELOG.md](CHANGELOG.md)。` | 历史记录（版本号与变更日志指向），按须知第五节不改 |
| **内置视角团**表：表头四列与四行数据（视角、身份一句话、视野边界、评审尺的全部条目文字） | 评审维度条目，只清说明文字，不动维度条目本身 |
| `【回传格式】` 内的字段名（视野名、总评、刺点、定位、类型、严重度、问题、建议）、枚举值 `{理解障碍/事实矛盾/节奏塌陷/设定违背/吸引力缺失}`、`🔴致命`、占位符 `{……}`、`2. ……` | 字段名、枚举值、占位符，机器可读内容 |
| 报告模板内的 `# 盲评报告·{靶子名}·{YYYYMMDD}`、`## 总裁决`、`🔴致命 N 条｜🟡警告 N 条｜🔵建议 N 条｜多视野同点命中 N 处｜冲突 N 处`、`## 误报（视野局限导致，弃）`、`## 存疑` 及其占位说明 | 模板结构、枚举值与占位符 |
| 命令与脚本原文：`python skills/pop-snow-pipeline/scripts/deai_gate.py … --profile doc --json`、`--fix`、`paopao_subagent_run`、`purpose=critique`、`WARN×N`、文件名模板 `审查/盲评-{靶子名}-{YYYYMMDD}.md` | 命令、脚本名、字段名、路径，机器可读内容 |
| `### Step 2 组装子 agent 提示词（模板骨架）`、`### Step 4 回收校验`、`**内置视角团（默认四团）**：`、`**边界**：`、`## 红线`、`## 执行模式`、`对全部刺点做合并与分类：`、`- **采纳**：具体、可执行、不违背管线既有决策的修改方向。`、`- **存疑**`（标题）、红线第 3 条 `**每条刺必有定位**` | 无压缩、无语域降档，原样保留 |
| 首行 `# pop-critic-panel` | 不得改首行标题；未新增 frontmatter |

## 小结

- 覆盖：原文前言到执行模式，共 **44 个改动单元**，全部落在规程句与说明文字上。
- **改得最集中的三类**：一、**符号串当句**（`＋`、`→`、`＝`、`·`、`/`、`≤`、破折号硬切），原句用关系符号代替连接词，读者只能猜关系；二、**缺主语缺谓语**（"整体评""给问题定位""不猜不编""改靶子"一类压缩单元）；三、**语域降档与黑话**（落盘、消毒、回执、拍板、开跑、派单、走、跑、直执），逐处收为书面写法。
- **黑话替换**：`SOP→规程`、`落盘→写入`、`消毒→去 AI 味`、`回执→确认`、`直执→直接执行`、`prompt→提示词`、`点读→查阅`。
- **未动一处**：所有命令、脚本路径、字段名、枚举值、占位符、阈值（10 条、N）、文件名模板，一处未变；条款一条未删、未新增、未合并；表格列与段落顺序未动。
- **一处需老板复核**：第 37 条把 `消毒:盲评报告|PASS` 的档名标签改为 `去AI味:盲评报告|PASS`，以对齐须知第五节登记的枚举 `去AI味:{档名}|PASS`；该串原为 agent 手写的确认行，非脚本输出（脚本未输出此行），故未破坏机器契约。若原意要保留"消毒"作标签，告知后改回。
