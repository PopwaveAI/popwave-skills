# pop-decon 改动清单（上）· SKILL.md 到 onboarding-guide.md

> **修改前**＝`skills/pop-decon/`（7 个在改文件）
> **修改后**＝`新流程探索\第二次测试\批量文字改造\pop-decon\`（同名 + `-改后`）
> 判据三问：**谁**（主语在不在）、**做什么**（动作在不在）、**凭什么判断**（依据或正反例在不在）。
> 病类缩写：缺主＝缺主语｜缺宾＝缺宾语｜生造＝生造词｜硬切＝硬切短句｜搭配＝搭配错位｜符号＝符号串当句｜压失＝压缩到失准｜短语＝名词短语当句｜指代＝指代不明｜口语＝语域降档。
> 本轮做两件事：一、把压缩单元展开成完整句子；二、把语域收到书面。语域基准见 `pop-write改写\SKILL-改后.md`。

## 一、pop-decon/SKILL.md（26 个单元）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 1 | 拆书专家入口：下载 txt → 征询范围 → 路由 `pop-decon-dimension`（单书深度wiki拆解）→ 沉淀。不常驻调度。 | 本技能为拆书专家入口：下载 txt，征询拆解范围，路由至 `pop-decon-dimension`（单书深度wiki拆解）执行拆解，最后完成沉淀。本入口不常驻调度。 | 补主语 | 缺主、符号 |
| 2 | 用户拆书需求 + 源文件｜征询范围 + 路由到 dimension（传范围+书信息） | 用户拆书需求与源文件｜征询范围，并路由至 dimension（传入范围与书信息） | +→与；传→传入 | 符号 |
| 3 | 主 agent 直执——下载/征询/路由/沉淀均为交互编排环节；拆解整体路由给 pop-decon-dimension | 由主 agent 直接执行——下载、征询、路由、沉淀均为交互编排环节；整个拆解流程路由给 pop-decon-dimension | 直执→直接执行 | 生造、符号 |
| 4 | 路由地图：下载txt → ★征询范围★ → 路由 dimension（范围）→ L1批次拆解→L2六模块成品 → 沉淀 + 提醒少测 | 路由地图：下载 txt，★征询范围★，路由至 dimension 并传入范围，进入 L1 批次拆解，再产出 L2 六模块成品，最后完成沉淀并提醒少量测试 | 少测→少量测试 | 符号、生造 |
| 5 | ## 怎么操作（SOP全内联） | ## 怎么操作（规程全内联） | SOP→规程 | 生造 |
| 6 | execution.mode: 按需路由 \| 强保障：本 SKILL.md 每次 run 强制注入 | execution.mode: 按需路由。强保障：本 SKILL.md 每次运行时强制注入。 | run→运行时 | 符号、缺谓 |
| 7 | ### Step 0：源文件检查 + 下载 | ### Step 0：源文件检查与下载 | +→与 | 符号 |
| 8 | 用户已提供路径（TXT/EPUB/PDF）｜确认存在 → 进 Step 1 | 用户已提供路径（TXT/EPUB/PDF）｜确认文件存在后进入 Step 1 | 进→进入 | 符号、缺宾 |
| 9 | 未提供文件｜路由 `tool-download-webnovel` 下载 → 进 Step 1 | 未提供文件｜路由 `tool-download-webnovel` 下载，完成后进入 Step 1 | 进→进入 | 符号、缺谓 |
| 10 | 说"网上有"但无 URL｜搜索 `{书名} txt 下载` 找 URL 后交 tool-download-webnovel | 用户称"网上有"但未提供 URL｜搜索 `{书名} txt 下载` 取得 URL，再交由 tool-download-webnovel 处理 | 无→未提供；交→交由…处理 | 缺主、缺谓 |
| 11 | 门禁：无源文件且未完成下载不得进 Step 1（下载失败处理见红线 5） | 门禁：无源文件且未完成下载时，不得进入 Step 1（下载失败的处理见红线 5） | 进→进入 | 缺主 |
| 12 | ### Step 1：征询范围（强制，禁止默认全量跑） | ### Step 1：征询范围（强制，禁止默认全量执行） | 跑→执行 | 生造 |
| 13 | 全书｜全量拆解 → 六模块深度wiki | 全书｜全量拆解，产出六模块深度wiki | →→产出 | 符号、缺谓 |
| 14 | 某一卷 / 前N章｜指定范围拆解（产出该范围对应的模块/切片） | 某一卷 / 前N章｜按指定范围拆解（产出该范围对应的模块与切片） | /→与 | 缺谓、符号 |
| 15 | 范围确定后即按 L1批次拆解→L2成品整合推进 | 范围确定后即按 L1 批次拆解、L2 成品整合推进 | 箭头→顿号 | 符号 |
| 16 | 门禁：用户未明确选择范围 → 退回征询，不得擅自开始拆解 | 门禁：用户未明确选择范围时，退回征询，不得擅自开始拆解 | 箭头→时 | 符号、缺主 |
| 17 | 由其承载 L1 批次拆解（硬门禁）+ L2 六模块成品整合……「先样板验收再铺 / 一步到位铺全」 | 由该 skill 承载 L1 批次拆解（硬门禁）与 L2 六模块成品整合……「先样板验收再铺开，还是一步到位铺全」 | 其→该 skill；+→与；/→还是 | 指代、符号 |
| 18 | ### Step 3：沉淀 + 提醒少测 | ### Step 3：沉淀与提醒少量测试 | +→与；少测→少量测试 | 符号、生造 |
| 19 | 全部分解完成后……入口不重复处理。未指定项目时询问沉淀到哪个项目 | 全部拆解完成后……本入口不做重复处理。未指定项目时，须询问沉淀到哪个项目 | 分解→拆解；入口→本入口 | 缺主、搭配 |
| 20 | （声明为功能介绍+引导、未执行任务），补一句"报书名+想学那块就开始" | （声明为功能介绍与引导、未执行任务），补一句"报书名与想学的内容即可开始" | +→与 | 符号 |
| 21 | `tool-download-webnovel`（下载txt）/ `pop-decon-dimension`（单书深度wiki拆解·L1批次拆解+L2六模块成品） | `tool-download-webnovel`（下载 txt）、`pop-decon-dimension`（单书深度wiki拆解·L1 批次拆解与 L2 六模块成品） | /→、；+→与 | 符号 |
| 22 | 未征询就全量跑 — 必须先征询范围，只拆所选范围，禁止默认全量跑 | 未征询就全量执行 — 必须先征询范围，只拆所选范围，禁止默认全量执行 | 跑→执行 | 生造 |
| 23 | 产出物不经质量门禁直接交付 — L1 批次硬门禁不过关不得进 L2（见 dimension） | 产出物未经质量门禁即直接交付 — L1 批次硬门禁不过关时，不得进 L2（见 dimension） | 补否定与"时" | 缺主 |
| 24 | 换源全失败 → 终止告知，不得空文件硬跑 | 更换源全部失败时，终止并告知用户，不得以空文件勉强执行 | 硬跑→勉强执行 | 符号、口语、缺宾 |
| 25 | 未提醒少测即切入全书 — ……建议先用一卷/前N章少量测试，确认效果后再拓展到全书 | 未提醒少量测试即切入全书 — ……建议先用一卷或前 N 章少量测试，确认效果后再拓展到全书 | 少测→少量测试；/→或 | 生造、符号 |
| 26 | 产出落盘后消毒（doc 报告闸） — ……成品落盘后自跑……黑话/套路句式……（chXX 锚点/🔒量化值等）不在消毒范围 | 产出写入后去 AI 味（doc 报告检查） — ……成品写入后，须自行执行……黑话与套路句式……（chXX 锚点、🔒量化值等）不在去 AI 味范围 | 落盘→写入；消毒→去 AI 味；闸→检查；自跑→自行执行 | 生造、符号 |

## 二、pop-decon/references/delegation-orchestration.md（15 个单元）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 27 | 当联合锚点池 > 8 章时，使用并行委派模式分批精读 | 当联合锚点池超过 8 章时，使用并行委派模式分批精读 | >→超过 | 符号 |
| 28 | 设计原则：联合 Grep 定位 → 分批精读（每批≤8章，每章多维提取）→ 分维度笔记汇总 → 分维度产出 | 设计原则：以联合 Grep 定位，随后分批精读（每批不超过 8 章，每章按多维提取），再按维度汇总笔记，最后按维度产出 | 箭头链→完整句；≤→不超过 | 符号、缺谓 |
| 29 | 硬限制：每批≤8章。超8章强制拆批，避免子 agent 超时导致主 agent 兜底手动读取 | 硬限制：每批不超过 8 章。超过 8 章时强制拆批，避免子 agent 超时，导致主 agent 兜底手动读取 | ≤→不超过 | 符号 |
| 30 | 主 agent 拿到 Grep 命中的锚点章列表后： | 主 agent 取得 Grep 命中的锚点章列表后： | 拿到→取得 | 口语 |
| 31 | 按章节序号排序／按≤8章/批切分／为每批准备子 agent 上下文 | 按章节序号排序／按每批不超过 8 章切分／为每批准备子 agent 上下文 | ≤8章/批→每批不超过 8 章 | 符号 |
| 32 | 按多维提取清单同时提取所有选中维度的设定（每章读一次，不重复读） | 按多维提取清单，同时提取所有选中维度的设定（每章只读一次，不重复读取） | 读一次→只读一次 | 缺谓 |
| 33 | 每条抽取记录格式：`- {维度}: {设定内容} (chXXX)` —— 必须带维度标签和章节号 | 每条抽取记录的格式为：`- {维度}: {设定内容} (chXXX)` —— 必须带维度标签与章节号 | 和→与 | 缺谓、符号 |
| 34 | 某维度本章无相关内容时标注「本章无{维度}相关内容」 | 某维度在本章无相关内容时，标注「本章无{维度}相关内容」 | 补"在本章" | 缺主 |
| 35 | 输出多维提取笔记（纯文本，不落盘，返回给主 agent） | 输出多维提取笔记（纯文本，不写入磁盘，返回给主 agent） | 不落盘→不写入磁盘 | 生造 |
| 36 | 子 agent 只负责"读+多维提取+返回笔记"，不负责产出落盘文件。批次档案由主 agent 按 …… 统一落盘。 | 子 agent 只负责"读取、多维提取、返回笔记"，不负责产出文件的写入。批次档案由主 agent 按 …… 统一写入。 | 读→读取；落盘→写入 | 生造、符号 |
| 37 | ### 2.3 笔记汇总 + 产出（主 agent 执行） | ### 2.3 笔记汇总与产出（主 agent 执行） | +→与 | 符号 |
| 38 | 按维度拆分合并（去重/归并/排序） | 按维度拆分合并（去重、归并、排序） | /→、 | 符号 |
| 39 | 每批按 L1 批次档案格式落盘（……，批头+事件链表+八维+🔒+爽点钩子+关系变化） | 每批按 L1 批次档案格式写入（……，批头、事件链表、八维、🔒、爽点钩子、关系变化） | 落盘→写入；+→、 | 生造、符号 |
| 40 | 每批自检 7 项硬门禁（术语表/锚点池/schema/证据密度/量化/不编造/回溯） | 每批自检 7 项硬门禁（术语表、锚点池、schema、证据密度、量化、不编造、回溯） | /→、 | 符号 |
| 41 | 在上下文中明确写"返回抽取笔记（每条1-3句话+chXXX），禁止返回原文全文"。 | 在上下文中明确写"返回抽取笔记（每条 1-3 句话，附 chXXX），禁止返回原文全文"。 | +→附 | 符号 |

## 三、pop-decon/references/onboarding-guide.md（5 个单元）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 42 | **声明本条为功能介绍 + 引导，未执行 skill 任务**……再引导开展第一步。 | **声明本条为功能介绍与引导，未执行 skill 任务**……再引导用户开展第一步。 | +→与；补"用户" | 符号、缺宾 |
| 43 | 先声明：**这次没动用什么干活，只是带你看看** | 先声明：**本次没有执行任何任务，只是带你看看** | 没动用什么干活→没有执行任何任务 | 搭配、口语 |
| 44 | **你告诉我拆多大范围（全书/某一卷/前N章）+ 最想看哪块，我只拆那块** | **你告诉我拆多大范围（全书/某一卷/前N章）与最想看哪块，我只拆那块** | +→与 | 符号 |
| 45 | **直接把「想拆哪本书 + 想学哪块」丢给我就行。** | **直接把「想拆哪本书与想学哪块」告诉我即可。** | +→与；丢给我就行→告诉我即可 | 符号、口语 |
| 46 | 想好了就直接说，我这就开工。 | 想好了就直接说，我这就开始。 | 开工→开始 | 口语 |

## 本清单（上）小结

- 覆盖：pop-decon/SKILL.md（1-26）、delegation-orchestration.md（27-41）、onboarding-guide.md（42-46），共 **46 个改动单元**。
- 四类病最集中：**符号串当句**（`→`、`+`、`/`、`≤`、`|`）；**生造词**（直执、少测、SOP、落盘、消毒）；**缺主缺宾**（补主语、补动作）；**语域降档**（拿到、干活、丢给我、开工、硬跑）。
- 未改动见下篇末节。
