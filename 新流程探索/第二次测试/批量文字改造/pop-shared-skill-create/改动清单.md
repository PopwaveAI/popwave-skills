# pop-shared-skill-create 改动清单

> 修改前＝`skills/pop-shared-skill-create/SKILL.md`
> 修改后＝`SKILL-改后.md`
> 判据三问：**谁**（主语在不在）、**做什么**（动作在不在）、**凭什么判断**（依据或正反例在不在）。
> 病类缩写：缺主＝缺主语｜缺宾＝缺宾语｜缺谓＝缺谓语｜缺字＝缺字｜生造＝生造词｜硬切＝硬切短句｜搭配＝搭配错位｜符号＝符号串当句｜压失＝压缩到失准｜短语＝名词短语当句｜指代＝指代不明｜域降＝语域降档｜缺口＝缺动作。


## 一、文件：SKILL.md


| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 1 | > Popwave Skill 设计规范——告诉 agent skill 该写成什么样、按什么顺序写入文件。v8.0.0：全文用词升级为 Mirror 创作专家标准（行为指令/是A不是B/锚点判断/行业词直用），新增「五、用词与表达」。完整版本历史见 [CHANGELOG.md](CHANGELOG.md)。 | > Popwave Skill 设计规范：说明 agent 该把 skill 写成什么样、按什么顺序写入文件。v8.0.0：全文用词升级为 Mirror 创作专家标准（行为指令、是A不是B、锚点判断、行业词直用），新增「五、用词与表达」。完整版本历史见 [CHANGELOG.md](CHANGELOG.md)。 | ——→：；/→、 | 符号 |
| 2 | 2. **提取主干**：做"换皮测试"——抽掉具体项目名后仍能复用的流程才写进 skill；一次性案例或纯资料查询不强行 skill 化。 | 2. **提取主干**：做"换皮测试"，抽掉具体项目名后仍能复用的流程才写进 skill；一次性案例或纯资料查询不强行 skill 化。 | ——→， | 符号 |
| 3 | 3. **规划结构**：`SKILL.md` 一个文件装下完整 SOP（执行要点内联），方法论进 `references/`，可复制产物进 `templates/`，可执行代码进 `scripts/`。 | 3. **规划结构**：`SKILL.md` 一个文件装下完整流程（执行要点内联），方法论进 `references/`，可复制产物进 `templates/`，可执行代码进 `scripts/`。 | SOP→流程 | 生造 |
| 4 | 4. **动手写文件**：写 `SKILL.md` + `skill.json` + `CHANGELOG.md`，按需创建一级资源目录。改造旧 skill 时只改结构、格式、用词、检查条件，不擅自改业务方法论。 | 4. **动手写文件**：写 `SKILL.md`、`skill.json`、`CHANGELOG.md`，按需创建一级资源目录。改造旧 skill 时只改结构、格式、用词、检查条件，不擅自改业务方法论。 | +→、 | 符号 |
| 5 | **执行模式**：主 agent 直执（规范理解与写文件一体，无自然子 agent 适配点）。 | **执行模式**：由主 agent 直接执行（规范理解与写文件一体，无适合交给子 agent 的环节）。 | 直执→由…直接执行；适配点→环节 | 生造、压失 |
| 6 | \| ❌1 \| **读取 skill 文件禁止用 Read 工具** — 用 `skill_view` 或 `Get-Content -Encoding UTF8 -Raw`；Read 有行数限制会截断丢内容 \| | \| ❌1 \| **读取 skill 文件不得用 Read 工具** — 用 `skill_view` 或 `Get-Content -Encoding UTF8 -Raw`；Read 有行数限制，会截断并丢失内容 \| | 禁止用→不得用；有行数限制会截断丢内容→有行数限制，会截断并丢失内容 | 域降、缺谓 |
| 7 | \| ❌2 \| **创建必须双文件** — SKILL.md + skill.json 缺一不可，缺一个平台无法注册 \| | \| ❌2 \| **创建必须双文件** — SKILL.md 与 skill.json 缺一不可，缺一个平台就无法注册 \| | SKILL.md + skill.json→SKILL.md 与 skill.json；无法注册→就无法注册 | 符号、缺谓 |
| 8 | \| ❌3 \| **SKILL.md 必须一个文件装下完整流程** — SOP 主干+各步执行要点+红线全在 SKILL.md 内；禁止 steps 目录（新建不建，改造时合入删目录清引用）；即使 references/ 没被读到，agent 也要知道完整流程 \| | \| ❌3 \| **SKILL.md 必须一个文件装下完整流程** — 流程主干、各步执行要点、红线全在 SKILL.md 内；不得设 steps 目录（新建不建，改造时合入并删目录、清引用）；即使 references/ 没被读到，agent 也要知道完整流程 \| | SOP主干+…+红线→流程主干、…、红线；禁止 steps 目录→不得设 steps 目录；合入删目录清引用→合入并删目录、清引用 | 生造、符号、域降 |
| 9 | \| ❌4 \| **版本三处一致** — SKILL.md 正文 + skill.json + CHANGELOG.md 版本号必须一致 \| | \| ❌4 \| **版本三处一致** — SKILL.md 正文、skill.json、CHANGELOG.md 的版本号必须一致 \| | SKILL.md 正文 + skill.json + CHANGELOG.md 版本号→三者版本号 | 符号 |
| 10 | \| ❌7 \| **SKILL.md 禁止堆版本历史块** — 摘要只写当前版本一句+CHANGELOG 指针；历次变更一律进 CHANGELOG.md \| | \| ❌7 \| **SKILL.md 不得堆版本历史块** — 摘要只写当前版本一句与 CHANGELOG 指针；历次变更一律进 CHANGELOG.md \| | 禁止堆→不得堆；一句+CHANGELOG 指针→一句与 CHANGELOG 指针 | 域降、符号 |
| 11 | - **正文标题下 `> 摘要`** 只写当前版本一句 + CHANGELOG 指针。写法：`> {一句职责}。vX.Y.Z：{当前版本改动简述}。完整版本历史见 CHANGELOG.md`。description 已承担定位与触发，摘要不重复定位，也不堆历次版本变更。 | - **正文标题下 `> 摘要`** 只写当前版本一句与 CHANGELOG 指针。写法：`> {一句职责}。vX.Y.Z：{当前版本改动简述}。完整版本历史见 CHANGELOG.md`。description 已承担定位与触发，摘要不重复定位，也不堆历次版本变更。 | 一句 + CHANGELOG 指针→一句与 CHANGELOG 指针 | 符号 |
| 12 | - **description 必须含触发条件**（"当用户说…时启用"），说明做什么+何时触发+产出给谁。 | - **description 必须含触发条件**（"当用户说…时启用"），说明做什么、何时触发、产出给谁。 | 说明A+B+C→说明A、B、C | 符号 |
| 13 | ├── SKILL.md          ← 主文档：SOP主干（执行要点内联）+红线+速查表+版本 | ├── SKILL.md          ← 主文档：流程主干（执行要点内联）、红线、速查表、版本 | SOP主干（执行要点内联）+红线+速查表+版本→流程主干（执行要点内联）、红线、速查表、版本 | 生造、符号 |
| 14 | \| `references/` \| 知识层 — 读后理解 \| 阅读理解，指导操作 \| 搜索SOP、调用规范 \| | \| `references/` \| 知识层 — 读后理解 \| 阅读理解，指导操作 \| 检索流程、调用规范 \| | 搜索SOP、调用规范→检索流程、调用规范 | 生造 |
| 15 | 禁止放错：模板不放 references/；SOP 不放 templates/；可执行代码不放 references/templates 根下；不放 README.md。 | 禁止放错：模板不放 references/；流程文档不放 templates/；可执行代码不放 references/templates 根下；不放 README.md。 | SOP→流程文档 | 生造 |
| 16 | **文件拆分原则**：references/templates/scripts 越细越好——agent 只在需要时读或执行对应文件，不拆 = 每次全读浪费 token。scripts/ 只有重复性强、容易写错、需要确定性执行的逻辑才放；脚本必须有清晰入口和参数说明，新增或修改后至少跑一个代表性样例。 | **文件拆分原则**：references/templates/scripts 越细越好，agent 只在需要时读取或执行对应文件；不拆就等于每次全读，浪费 token。scripts/ 只放重复性强、容易写错、需要确定性执行的逻辑；脚本必须有清晰入口和参数说明，新增或修改后至少运行一个代表性样例。 | 越细越好——agent…；不拆 = 每次全读…；只有…才放→只放…；跑一个样例→运行一个样例 | 符号、域降、缺谓 |
| 17 | **拆分决策**：单步过长？→ 精炼压缩进 SKILL.md / 需要读后理解？→ 进 references / 需要复制填充？→ 进 templates / 需要运行代码？→ 进 scripts。 | **拆分决策**：单步过长，就精炼压缩进 SKILL.md；需要读后理解，就进 references；需要复制填充，就进 templates；需要运行代码，就进 scripts。 | 「？→ /」连锁改为分号叙述 | 符号、缺谓 |
| 18 | **内容组织顺序**：`# 标题` → `> 摘要` → `## 这个 Skill 做什么`（职责/边界/产物）→ `## 怎么操作`（SOP全内联）→ `## 红线`（第一条必须是读取协议）→ `## 速查表`（有外部文件才设）→ `## 执行模式` → `## 版本`。放不下时优先压缩红线和速查表，保住 SOP 主干。 | **内容组织顺序**：`# 标题` → `> 摘要` → `## 这个 Skill 做什么`（职责、边界、产物）→ `## 怎么操作`（流程全内联）→ `## 红线`（第一条必须是读取协议）→ `## 速查表`（有外部文件才设）→ `## 执行模式` → `## 版本`。放不下时优先压缩红线和速查表，保住流程主干。 | （职责/边界/产物）→（职责、边界、产物）；SOP全内联→流程全内联；A → B → C…→A → B → C…（保留链条但去「SOP」）；保住 SOP 主干→保住流程主干 | 生造、符号 |
| 19 | **执行要点内联写法**（单文件自包含的核心手法）：每个 SOP 步骤节直接写流程细节/检查条件/输出格式/命令模板；与主干重复的删、冗余解释压成表格、同类红线合并；检查条件/红线/输出格式/业务逻辑/脚本调用方式一条不能丢。合入后体量控制在原 SKILL.md+steps 总量的 50-65%。 | **执行要点内联写法**（单文件自包含的核心手法）：每个步骤节直接写流程细节、检查条件、输出格式、命令模板；与主干重复的删去，冗余解释压成表格，同类红线合并；检查条件、红线、输出格式、业务逻辑、脚本调用方式一条不能丢。合入后体量控制在原 SKILL.md 与 steps 总量的 50-65%。 | 细节/条件/格式/模板→细节、条件、格式、模板；与主干重复的删、→删去；SKILL.md+steps→SKILL.md 与 steps | 符号、域降 |
| 20 | **速查表设计**：全文件目录引导——每个外部文件什么时候读/执行；无外部文件则不设速查表节。 | **速查表设计**：全文件目录引导，说明每个外部文件什么时候读、什么时候执行；无外部文件则不设速查表节。 | 目录引导——每个…读/执行→目录引导，说明…读、执行 | 符号、缺谓 |
| 21 | - **红线≤7条**：只有违反后**断裂下游流程**的才算红线。是红线 ✅：读取协议（截断丢内容）/双文件结构（缺文件平台无法注册）/版本三处一致（平台注册错乱）。不是红线 ❌：frontmatter≤4行、description 含触发词、红线在第一屏（格式偏好，降级为检查清单）。 | - **红线≤7条**：只有违反后会**断裂下游流程**的才算红线。属于红线 ✅：读取协议（截断丢内容）、双文件结构（缺文件平台无法注册）、版本三处一致（平台注册错乱）。不属于红线 ❌：frontmatter≤4行、description 含触发词、红线在第一屏（格式偏好，降级为检查清单）。 | 是红线 ✅：A（…）/B（…）/C（…）→属于红线：A、B、C；不是红线 ❌→不属于红线 | 符号、缺谓 |
| 22 | - **注意力预算**：识别每份文档"最值钱的部分"，核心段占比 ≥30%；不足则辅助段该删的删、该并的并。禁止均匀分配——那是登记表不是设计文档。 | - **注意力预算**：识别每份文档"最值钱的部分"，核心段占比 ≥30%；不足则把辅助段该删的删、该并的并。不得均匀分配，那是登记表，不是设计文档。 | 禁止均匀分配——那是→不得均匀分配，那是 | 符号、域降 |
| 23 | **设计原则**：① 设计 SKILL.md 时假设外部资源可能没被读到；② 关键流程（SOP 主干+执行要点）必须在 SKILL.md 自包含；③ 实测 435 runs，3 轮对话后 skill 文件读取率降至 0-3%；④ 断裂后果严重的约束提升到 SKILL.md 红线中。 | **设计原则**：一、设计 SKILL.md 时假设外部资源可能没被读到；二、关键流程（流程主干与执行要点）必须在 SKILL.md 中自包含；三、实测 435 次运行，3 轮对话后 skill 文件读取率降至 0-3%；四、断裂后果严重的约束提升到 SKILL.md 红线中。 | ①②③④→一、二、三、四；SOP 主干+执行要点→流程主干与执行要点；435 runs→435 次运行 | 符号、生造 |
| 24 | 3. 创建 `SKILL.md`（按三、结构指引）：必须含"这个 Skill 做什么"+"怎么操作"两段且执行要点内联；红线第一条是读取协议；有外部文件则速查表为全文件目录引导 | 3. 创建 `SKILL.md`（按三、结构指引）：必须含"这个 Skill 做什么"与"怎么操作"两段，且执行要点内联；红线第一条是读取协议；有外部文件则速查表为全文件目录引导 | "A"+"B"→"A"与"B" | 符号 |
| 25 | 1. **先读全文** — 用 `Get-Content -Encoding UTF8 -Raw` 读取目标 skill 的 SKILL.md + skill.json + CHANGELOG | 1. **先读全文** — 用 `Get-Content -Encoding UTF8 -Raw` 读取目标 skill 的 SKILL.md、skill.json、CHANGELOG | SKILL.md + skill.json + CHANGELOG→SKILL.md、skill.json、CHANGELOG | 符号 |
| 26 | 2. **输出扫描报告** — 对照九、检查清单标记违规项 + 证据行号，交用户确认 | 2. **输出扫描报告** — 对照九、检查清单标记违规项与证据行号，交用户确认 | 违规项 + 证据行号→违规项与证据行号 | 符号 |
| 27 | 3. **只改结构/格式/红线位置/用词/资源归位** — 不动核心方法论和业务逻辑；发现代码散落时归入一级 `scripts/`；发现 steps 目录时把内容精炼合入 SKILL.md、删目录清引用 | 3. **只改结构、格式、红线位置、用词、资源归位** — 不动核心方法论和业务逻辑；发现代码散落时归入一级 `scripts/`；发现 steps 目录时把内容精炼合入 SKILL.md，删目录清引用 | 结构/格式/红线位置/用词/资源归位→结构、格式、红线位置、用词、资源归位；SKILL.md、删目录清引用→SKILL.md，删目录清引用 | 符号 |
| 28 | 4. **版本 +1** — SKILL.md + skill.json + CHANGELOG 三处同步 | 4. **版本 +1** — SKILL.md、skill.json、CHANGELOG 三处同步 | SKILL.md + skill.json + CHANGELOG→SKILL.md、skill.json、CHANGELOG | 符号 |
| 29 | **格式**：frontmatter ≤4 行只留 name+description｜description 含触发条件｜skill.json 七字段完整｜版本三处一致。 | **格式**：frontmatter ≤4 行只留 name 与 description｜description 含触发条件｜skill.json 七字段完整｜版本三处一致。 | name+description→name 与 description | 符号 |
| 30 | **结构**：SKILL.md 含完整 SOP 主干且执行要点已内联，不只是路由表｜"这个 Skill 做什么"说明职责边界产物｜"怎么操作"覆盖全流程动作要点产出｜红线在第一屏、第一条是读取协议、≤7条｜无 steps 目录、无 step 文件引用残留｜知识层归 references/、模板归 templates/、代码归 scripts/。 | **结构**：SKILL.md 含完整流程主干且执行要点已内联，不只是路由表｜"这个 Skill 做什么"说明职责、边界、产物｜"怎么操作"覆盖全流程动作要点与产出｜红线在第一屏、第一条是读取协议、≤7条｜无 steps 目录、无 step 文件引用残留｜知识层归 references/、模板归 templates/、代码归 scripts/。 | SOP 主干→流程主干；职责边界产物→职责、边界、产物；动作要点产出→动作要点与产出 | 生造、符号、缺定 |
| 31 | **加载检查**：速查表是全文件目录引导（列出所有外部文件及读取/执行时机）｜SKILL.md 有强弱加载保障声明｜scripts/ 新增或修改的代表性脚本已实际运行验证。 | **加载检查**：速查表是全文件目录引导（列出所有外部文件及读取、执行时机）｜SKILL.md 有强弱加载保障声明｜scripts/ 新增或修改的代表性脚本已实际运行验证。 | 读取/执行时机→读取、执行时机 | 符号 |
| 32 | **文档质量**：识别核心价值段且占比 ≥30%｜同一信息不重复出现｜摘要不堆版本历史（只当前版本一句+CHANGELOG指针）｜无深层嵌套（拍平为 3-6 步扁平流程）｜无第二人称（"You should"改为祈使句）｜**五、用词与表达六条已对照过（行为指令/是A不是B/锚点判断/行业词直用/一段一义/禁AI腔）**。 | **文档质量**：识别核心价值段且占比 ≥30%｜同一信息不重复出现｜摘要不堆版本历史（只当前版本一句加 CHANGELOG 指针）｜无深层嵌套（拍平为 3-6 步扁平流程）｜无第二人称（"You should"改为祈使句）｜**五、用词与表达六条已对照过（行为指令、是A不是B、锚点判断、行业词直用、一段一义、禁AI腔）**。 | 一句+CHANGELOG指针→一句加 CHANGELOG 指针；行为指令/是A不是B/…→行为指令、是A不是B、… | 符号 |

## 未改动

| # | 保留项 | 出处 | 理由 |
|--:|:--|:--|:--|
| 1 | 第五「用词与表达」六条正反例、装饰性比喻黑名单（含「引擎」「耦合」「闭环」「全链路」「沉淀」等示例词） | SKILL.md `## 五` | 系统级《用词规范》原文（规范条款），不改；其中禁用词为规范示例中列出的词本身，属数据 |
| 2 | 版本记录句「v8.0.0 … 完整版本历史见 CHANGELOG.md」 | 摘要行、`## 版本` | 版本记录段，按第五节不改 |
| 3 | `skill.json` 七字段清单、目录树路径名、`{ }` JSON 代码块 | SKILL.md 一、二 | 机器可读字段名与代码，按第四节不改 |
| 4 | 「平台每次运行强制注入 SKILL.md 全文」「只注入 SKILL.md 全文」中的「注入」 | SKILL.md 二、六 | 描述平台机制，保留 |
| 5 | 红线条目要求本身（读取协议、双文件、单文件自包含、版本三处一致、脚本归位、红线≤7、不堆版本块） | SKILL.md `## ❌ 质量红线` | 条款内容未动，仅改表述与语域 |
| 6 | 检查清单中「拍平为 3-6 步扁平流程」「You should」等举例 | SKILL.md 九 | 举例与数字，保留 |
