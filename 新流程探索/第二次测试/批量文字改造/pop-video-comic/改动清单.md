# pop-video-comic 改动清单

> **修改前**＝`skills/pop-video-comic/SKILL.md`（原版 204 行）
> **修改后**＝`SKILL-改后.md`
> 判据三问：**谁**（主语在不在）、**做什么**（动作在不在）、**凭什么判断**（依据或正反例在不在）。
> 本轮做两件事：一、把压缩单元展开成完整句子；二、把语域收到书面。
> 病类缩写：缺主＝缺主语｜缺谓＝缺谓语｜缺动＝缺动作词｜硬切＝硬切短句｜符号＝符号串当句｜口语＝语域降档｜生造＝生造词｜指代＝指代不明｜压失＝压缩到失准｜比喻＝装饰性比喻。

## 一、前言与管线（原第 3 至 29 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 1 | 漫画转 PPT 放映式短视频管线。…HTML+ffmpeg 合成。 | 本技能是漫画转 PPT 放映式短视频的管线。…HTML 与 ffmpeg 合成。 | 补主语；+→与 | 缺主、符号 |
| 2 | 把已成稿的漫画页（page1~N.png + 页面配置.json + OC/封面）组装成"PPT 放映式"短视频——静态图 + Ken Burns 推拉 + 字幕 + 人声口播 + BGM…**不是** AI 视频生成（Seedance 那种），是确定性渲染。 | 把已成稿的漫画页（page1~N.png、页面配置.json、OC/封面）组装成"PPT 放映式"短视频，包含静态图、Ken Burns 推拉、字幕、人声口播与 BGM…**不是** AI 视频生成（Seedance 那种），而是确定性渲染。 | +→、；破折号→逗号；补"而是" | 符号、硬切 |
| 3 | 脚本确认后才进入 Step 3 配音 + Step 4 合成。禁止跳过确认直接出片。 | 脚本确认后才进入 Step 3 配音与 Step 4 合成。不得跳过确认直接出片。 | +→与；禁止→不得 | 符号 |
| 4 | …是只读扫描/读图分析，可派子agent执行并回报（素材清单/逐页白描表），主agent落盘；…主agent直执。 | …是只读扫描与读图分析，可派子 agent 执行并回报素材清单与逐页白描表，由主 agent 写入；…由主 agent 直接执行。 | /→与；子agent→子 agent；落盘→写入；直执→直接执行 | 符号、生造 |
| 5 | 输入：漫画页图片（page1~N.png）+ `页面配置.json`（每页文字锚点）+ 可选 OC/封面。 | 输入：漫画页图片（page1~N.png）、`页面配置.json`（每页文字锚点）与可选 OC/封面。 | +→、 | 符号 |
| 6 | 输出：1 分钟级短视频 MP4（图+字幕+人声口播+Ken Burns）。 | 输出：1 分钟级短视频 MP4（图、字幕、人声口播与 Ken Burns）。 | +→、 | 符号 |
| 7 | 主 agent（或子agent）逐页 Read 图片，产出每页画面理解 | 主 agent（或子 agent）逐页 Read 图片，产出每页画面理解 | 子agent→子 agent | 口语 |
| 8 | 视频合成：Playwright 截图 + ffmpeg，复用漫画 HTML 美学，纵向滚动→横向影片 | 视频合成：Playwright 截图与 ffmpeg，复用漫画 HTML 美学，纵向滚动转为横向影片 | +→与；→→转为 | 符号 |
| 9 | 视频合成**不**走 Seedance/GenerateVideo——那是 AI 生成视频（不可控、按秒计费），本 skill 是确定性 PPT 放映式渲染 | 视频合成**不**采用 Seedance/GenerateVideo，那是 AI 生成视频（不可控、按秒计费）；本 skill 是确定性的 PPT 放映式渲染 | 走→采用；破折号→分号 | 口语、硬切 |
| 10 | `## 怎么操作（SOP 全内联）` | `## 操作步骤（规程全文内联）` | SOP→规程 | 生造（SOP） |

## 二、Step 0 与 Step 1（原第 40 至 58 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 11 | 产出 `素材清单.md` 到 `{项目}/视频/`：每页图片路径 + 尺寸 + 文字锚点 + OC/封面路径。 | 产出 `素材清单.md` 到 `{项目}/视频/`：每页图片路径、尺寸、文字锚点与 OC/封面路径。 | +→、 | 符号 |
| 12 | **缺件兜底**：缺 `页面配置.json` → 跳过文字锚点，Step 1 完全依赖读图理解（仍可走，但脚本锚点会弱）。 | **缺件兜底**：缺 `页面配置.json` 时，跳过文字锚点，Step 1 完全依赖读图理解（仍可执行，但脚本锚点会偏弱）。 | →→时；仍可走→仍可执行；弱→偏弱 | 符号、口语 |
| 13 | **完成判定**：`output/page1~N.png` 全部找到｜文字锚点已提取｜素材清单.md 已写入。 | **完成判定**：`output/page1~N.png` 全部找到；文字锚点已提取；素材清单.md 已写入。 | ｜→； | 符号 |
| 14 | 2. 单页被安全审查拦截 → 用 `页面配置.json` 该页文字锚点补全画面理解，标注该页 `[读图被拦截，配置文字兜底]`；**禁止因读图失败中断管线** | 2. 单页被安全审查拦截时，用 `页面配置.json` 该页文字锚点补全画面理解，并标注该页 `[读图被拦截，配置文字兜底]`。**不得因读图失败中断管线** | →→时；禁止→不得 | 符号 |
| 15 | **完成判定**：所有页已处理（含拦截页兜底）｜每页三要素齐全｜画面理解.md 已写入。 | **完成判定**：所有页已处理，含拦截页兜底；每页三要素齐全；画面理解.md 已写入。 | ｜→； | 符号 |

## 三、Step 2 脚本创作（原第 82 至 106 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 16 | **写作原则**：①钩子优先——开头 2 句必须有悬念或反差，不能平铺直叙"这是一个..."②每页至少 1 句，绑定 `page{N}`…③情绪线递进（如"痛醒"→"陌生房间"→…层层升温）④口播是"讲给朋友听"的口吻，自然口语化非书面朗读⑤总时长目标 ~60 秒 | **写作原则**：一、钩子优先，开头 2 句必须有悬念或反差，不得平铺直叙"这是一个……"；二、每页至少 1 句，绑定 `page{N}`…；三、情绪线递进，例如按"痛醒、陌生房间…"层层升温；四、口播用"讲给朋友听"的口吻，自然口语化，不作书面朗读；五、总时长目标约 60 秒 | ①②③→一二三；不能→不得；→→、；~→约 | 符号、口语、硬切 |
| 17 | **产出口播脚本.md**（示例格式）： | **产出 `口播脚本.md`**（示例格式）： | 文件名加代码标记 | 符号 |
| 18 | **🚪 确认闸门**：产出 `口播脚本.md` 后**停下来等老板确认**。仅当老板确认脚本后，才进入 Step 3 配音 + Step 4 合成；老板要求修改则迭代脚本后再确认。 | **🚪 确认闸门**：产出 `口播脚本.md` 后**须停下来等老板确认**。仅当老板确认脚本后，才进入 Step 3 配音与 Step 4 合成；老板要求修改时，迭代脚本后再确认。 | 补"须"；+→与；则→时 | 符号、缺谓 |
| 19 | **完成判定**：有钩子开头｜每页至少 1 句且绑定 page{N}｜情绪线递进｜总时长 ~60s｜**已等老板确认**（未确认禁止进入 Step 3）。 | **完成判定**：有钩子开头；每页至少 1 句且绑定 page{N}；情绪线递进；总时长约 60s；**已等老板确认**（未确认不得进入 Step 3）。 | ｜→；；~→约；禁止→不得 | 符号 |

## 四、Step 3 与 Step 4（原第 114 至 155 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 20 | 1. 读已确认的 `口播脚本.md`，提取所有口播句（含 seq + page 绑定）。 | 1. 读已确认的 `口播脚本.md`，提取所有口播句（含 seq 与 page 绑定）。 | +→与 | 符号 |
| 21 | **失败处理**：某句生成失败重试该句，不中断整批。 | **失败处理**：某句生成失败时重试该句，不中断整批。 | 补"时" | 缺谓 |
| 22 | **完成判定**：每句口播都有对应 mp3｜时长清单.json 含全部句的 seq/page/file/duration｜总时长 ~60s（与脚本估算偏差 <20%）。 | **完成判定**：每句口播都有对应 mp3；时长清单.json 含全部句的 seq/page/file/duration；总时长约 60s（与脚本估算的偏差小于 20%）。 | ｜→；；~→约；<→小于 | 符号 |
| 23 | 每句口播对应一帧（图片 + 字幕 + Ken Burns 起始/结束缩放位移）。 | 每句口播对应一帧（图片、字幕与 Ken Burns 的起始或结束缩放位移）。 | +→、；/→或 | 符号 |
| 24 | 画布：竖屏 1080x1920（短视频）或 16:9 1920x1080，按目标平台选 | 画布：竖屏 1080x1920（短视频）或 16:9 1920x1080，按目标平台选择 | 选→选择 | 缺谓 |
| 25 | 中文字体：`Noto Sans CJK SC` / `WenQuanYi Micro Hei`（必须显式设置，禁止默认字体） | 中文字体：`Noto Sans CJK SC` 或 `WenQuanYi Micro Hei`（必须显式设置，不得使用默认字体） | /→或；禁止→不得使用 | 符号 |
| 26 | 每帧图片按对应口播时长定格（Ken Burns 推拉）→ 拼接所有帧成视频流 → 混入人声（按 `时长清单.json` 对齐）+ 可选 BGM → 输出 `{章节名}.mp4`。 | 每帧图片按对应口播时长定格（Ken Burns 推拉），再拼接所有帧成视频流，随后混入人声（按 `时长清单.json` 对齐）与可选 BGM，输出 `{章节名}.mp4`。 | →→，再/随后；+→与 | 符号、硬切 |
| 27 | 每帧从"起始缩放/位移"缓动到"结束缩放/位移"；…缩放范围 1.0→1.15 以内 | 每帧从"起始缩放与位移"缓动到"结束缩放与位移"；…缩放范围在 1.0 至 1.15 以内 | /→与；→→至 | 符号 |
| 28 | BGM 默认无，如需加在 `scripts/render_video.py` 用 `--bgm` 参数混入（音量压低，不盖人声）；加水印/片头（OC/封面作为开场帧）。 | BGM 默认无，如需添加，在 `scripts/render_video.py` 中用 `--bgm` 参数混入（音量压低，不盖人声）；也可加水印或片头（OC/封面作为开场帧）。 | 加→添加；用→中用；/→或 | 缺谓、符号 |
| 29 | **产出**：`{项目}/视频/frames/frame{N}.png`（中间帧，过程）｜`{项目}/视频/{章节名}.mp4`（最终视频，确认后按铁律❌5 版本化命名）。 | **产出**：`{项目}/视频/frames/frame{N}.png`（中间帧，过程）；`{项目}/视频/{章节名}.mp4`（最终视频，确认后按铁律❌5 版本化命名）。 | ｜→； | 符号 |
| 30 | **完成判定**：无黑边（图填满画面，`object-fit: cover`）｜字幕字体为 CJK 字体无乱码｜每句口播与对应页面对齐｜视频可播放且已生成。 | **完成判定**：无黑边（图填满画面，`object-fit: cover`）；字幕字体为 CJK 字体、无乱码；每句口播与对应页面对齐；视频可播放且已生成。 | ｜→； | 符号 |

## 五、产出结构、铁律与前置（原第 159 至 193 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 31 | 落盘遵循 `pop-visual-pipeline/references/落盘规范.md` §3.1b §五 §六：视频成品必须版本化+`-final` | 写入遵循 `pop-visual-pipeline/references/落盘规范.md` §3.1b §五 §六：视频成品必须版本化并加 `-final` | 落盘（动词）→写入；+→并加 | 口语（黑话）、符号 |
| 32 | ❌1 …确认后才进 Step 3 配音 + Step 4 合成。禁止跳过脚本确认直接出片 | ❌1 …确认后才进 Step 3 配音与 Step 4 合成。不得跳过脚本确认直接出片 | +→与；禁止→不得 | 符号 |
| 33 | ❌2 …图片被安全审查拦截时用配置文字补全，禁止因读图失败中断管线 | ❌2 …图片被安全审查拦截时用配置文字补全，不得因读图失败中断管线 | 禁止→不得 | 口语 |
| 34 | ❌4 **视频合成走确定性渲染，不走 AI 视频** — 用 HTML+Playwright+ffmpeg 合成，禁止用 Seedance/GenerateVideo 按秒生成的 AI 视频 | ❌4 **视频合成采用确定性渲染，不采用 AI 视频** — 用 HTML、Playwright 与 ffmpeg 合成，不得使用 Seedance/GenerateVideo 按秒生成的 AI 视频 | 走→采用；+→、与；禁止用→不得使用 | 口语、符号 |
| 35 | ❌5 **视频成品版本化+final** — 确认后视频命名 `第{N}章(-配音)-v{版本}-final.mp4`，禁止裸名 `成品.mp4`；`preview/frames/audio/` 是过程不进成品态 | ❌5 **视频成品版本化并加 final** — 确认后视频命名 `第{N}章(-配音)-v{版本}-final.mp4`，不得使用裸名 `成品.mp4`；`preview/frames/audio/` 是过程，不进成品态 | +→并加；禁止裸名→不得使用裸名 | 符号、口语 |
| 36 | 1. Python 3.8+ + Pillow + edge-tts（配音） | 1. Python 3.8+、Pillow、edge-tts（配音） | +→、 | 符号 |

## 未改动

| 段落／内容 | 理由 |
|:--|:--|
| 首行 `# pop-video-comic` | 首行标题不得改；未新增 frontmatter |
| 版本句版本号 `v1.0.0`、`## 版本` 段、`> 完整版本历史见 CHANGELOG.md。` | 历史记录（版本号与变更日志指向），按须知第五节不改 |
| 核心管线行 `**Step 0 收集** → **Step 1 读图理解** → …`、`## 模型说明` 表、`## 产出文件结构` 的目录树代码块、`## 速查表` | 流程节点枚举、表格结构、目录树；箭头为节点序列记号，非句子连接符 |
| Step 2 脚本结构代码块、`口播脚本.md` 示例代码块、`时长清单.json` 示例代码块、`--bgm` 命令行代码块 | 代码块内的模板、字段、命令、路径，机器可读内容 |
| 口播字段表（`seq`/`page`/`text`/`emotion`/`est_sec`）、`os｜dialogue｜narration`、氛围标签词汇表（恐怖系／情绪系／奇观系） | 字段名、枚举值、术语枚举 |
| `落盘规范.md`（在 `pop-visual-pipeline/references/落盘规范.md` 中） | 文件名与路径，机器可读内容；文中 2 处 `落盘` 命中均为该文档名 |
| `**先脚本后出片（铁律）**`、`## 产出文件结构`、`## ❌ 铁律`、`## 速查表` 表头与各行、`edge-tts` 行、`**可选**` 前的 `**Ken Burns（推拉）规则**` 其余部分、前置条件 2 至 5 条 | 无压缩、无语域降档，原样保留 |
