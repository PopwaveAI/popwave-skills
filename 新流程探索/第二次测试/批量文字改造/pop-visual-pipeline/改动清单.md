# pop-visual-pipeline 改动清单

> 修改前＝`skills/pop-visual-pipeline/` 下各文件（原版）｜修改后＝本目录 `*-改后.*`
> 覆盖 6 个文件，逐句四列对照。

> 判据三问：**谁**（主语在不在）、**做什么**（动作在不在）、**凭什么判断**（依据或正反例在不在）。
> 病类缩写：缺主＝缺主语｜缺宾＝缺宾语｜缺谓＝缺谓语｜生造＝生造词｜硬切＝硬切短句｜搭配＝搭配错位｜符号＝符号串当句｜压失＝压缩到失准｜短语＝名词短语当句｜口语＝语域降档｜并列＝并列不当
> 说明：同文件内反复出现的「必须→须」「禁止→不得」按处数合并为一行统计，其余为逐句改动；原文列前缀 `Lx` 为原文件行号。

## 1、SKILL.md（36 个改动单元）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 1 | L3 > 小说视觉化安装器。**v4.2.0：状态源收敛 `状态.md` + 路由上移专家提示词**——pipeline 只做初始化/导入/三态迁移/状态维护；下游 asset/style/art-bible 同步改读 状态.md。完整版本历史见 [CHANGELOG.md](CHANGELOG.md)。 | > 小说视觉化安装器。**v4.2.0：状态来源收敛为 `状态.md`，路由上移至专家提示词**——pipeline 只负责初始化、导入、三态迁移与状态维护；下游 asset/style/art-bible 同步改读 状态.md。完整版本历史见 [CHANGELOG.md](CHANGELOG.md)。 | 状态源→状态来源；只做→只负责；/→、 | 生造、符号 |
| 2 | L9 pipeline 只做安装/导入/状态维护不干活——初始化或导入时生成/重建 `状态.md`（唯一机器状态源）、做三态落盘迁移、并按状态片段把 agent 指向对应子 skill；**日常视觉路由与意图闸口上移专家提示词**（两段式架构：基建层必做、派生层按需）。 | pipeline 只负责安装、导入与状态维护，不承担实际生产——初始化或导入时生成或重建 `状态.md`（唯一机器状态源）、执行三态写入迁移，并按状态片段把 agent 指向对应子 skill；**日常视觉路由与意图闸口上移专家提示词**（两段式架构：基建层必做，派生层按需）。 | 只做→只负责；不干活→不承担实际生产；落盘→写入 | 口语、生造 |
| 3 | L11 输入：项目名或当前项目目录（mode=fresh/import/resume） | 输入：本项目名称或当前项目目录（mode=fresh/import/resume） | 补量词「本」 | 短语 |
| 4 | L12 输出：标准化目录结构 + `状态.md`（唯一机器状态源）+（可选）`视觉项目总控.html` 展示面板 | 输出：标准化目录结构、`状态.md`（唯一机器状态源），以及可选的 `视觉项目总控.html` 展示面板 | 符号 + → 、/以及 | 符号 |
| 5 | L14 **核心价值**：把"**查原文+定画风+产出美术设定集=基建**"铁律固化到状态与导入流程——visual 群原是 7 个无顺序约束的散点 skill，会"没定画风就先画封面"乱序。 | **核心价值**：把"**查原文、定画风、产出美术设定集即为基建**"这条铁律固化在状态与导入流程中——visual 群原本是 7 个没有顺序约束的分散 skill，会出现"尚未确定画风就先绘制封面"的乱序问题。 | +、=→、/即为；散点→分散；会…乱序→会出现…的乱序问题 | 符号、缺主、压失 |
| 6 | L16 **日常路由不经此skill**：用户说"继续视觉/出封面/出OC/出漫画"时，主agent按**专家提示词阶段地图**调度——先过意图闸口确认本次目标（cover/oc/comic/full/asset-only，不默认推漫画）→ 读 `状态.md` 取状态片段（phase/intent/next_step/就绪态）→ 按两段式阶段地图（基建层查原文→定画风→美术设定集必做；派生层按需）+ 就绪门禁直接选 skill。各子skill按自身 SKILL.md 干活，pipeline 不参与每轮路由。 | **日常路由不经过本 skill**：用户说"继续视觉/出封面/出OC/出漫画"时，主 agent 按**专家提示词阶段地图**调度——先过意图闸口确认本次目标（cover/oc/comic/full/asset-only，不默认推漫画），再读取 `状态.md` 获取状态片段（phase/intent/next_step/就绪态），然后按两段式阶段地图（基建层查原文、定画风、美术设定集必做；派生层按需）与就绪门禁直接选择 skill。各子 skill 按自身 SKILL.md 执行，pipeline 不参与每轮路由。 | 不经→不经过；主agent→主 agent；→→先/再/然后；干活→执行；子skill→子 skill | 符号、口语 |
| 7 | L20 写入项目根，agent 每轮只读它取状态片段。模板见 `templates/状态.md`，内容： | 该文件写入项目根目录，agent 每轮只读取它获取状态片段。模板见 `templates/状态.md`，内容如下： | 补主语与「目录」「获取」 | 缺主 |
| 8 | L46 ## 怎么操作（SOP全内联） | ## 怎么操作（规程全内联） | SOP→规程 | 生造 |
| 9 | L50 只做地基：创建标准目录+生成 状态.md+自检。 | 本步只打地基：创建标准目录、生成 状态.md 并自检。 | 只做→本步只打；+→、 | 缺主、符号 |
| 10 | L52 **1. 项目空间探测**（LS 扫描项目根）：根 `状态.md` 为写作专家项目（起点/番茄）——按内容标记判定：含 `## 书目`+`立项就绪`/`世界就绪`=起点，含 `## 底牌就绪`+`## 阶段完成情况`=番茄；原文 `正文/ch*.txt`，import 走 Step 1｜`原料/小说原文/`（单个完整 txt）=独立小说项目，init｜用户指定路径=临时模式，init。判定结果写 状态.md 的 `project_type` 字段。 | **1. 项目空间探测**（用 LS 扫描项目根）：若根目录存在 `状态.md`，则为写作专家项目（起点或番茄）——按内容标记判定：含 `## 书目` 与 `立项就绪`/`世界就绪` 者为起点，含 `## 底牌就绪` 与 `## 阶段完成情况` 者为番茄；若存在原文 `正文/ch*.txt`，则 import 走 Step 1；若存在 `原料/小说原文/`（单个完整 txt），则为独立小说项目，走 init；若用户指定路径，则为临时模式，走 init。判定结果写入 状态.md 的 `project_type` 字段。 | 补条件连词与「走」；+=→与；=→为 | 符号、缺谓、压失 |
| 11 | L54 **2. 🚪 意图闸口（前置，决定基建档位）**：`AskUserQuestion` 或书面提问确认本次目标，intent 写入 状态.md 的 `intent` 字段，整条流程按档位路由——`cover` 做封面/场景图（轻量：场景资产表+视觉符号+画风，刊物角色可跳过双角度定妆）｜`oc` 做人物OC/立绘（轻量~中：角色档案+身份卡+画风，可跳过双角度定妆）｜`comic` 做漫画/连载（完整：深度角色档案+画风定标+身份卡+双角度定妆）｜`full` 全套视觉工程（完整基建）｜`asset-only` 只提取资产（最轻，只跑 asset 不进派生层）。**未明确时不默认漫画，回问用户**——只有明确说"做漫画/连载"才走 `comic` 完整档。 | **2. 🚪 意图闸口（前置步骤，决定基建档位）**：由 `AskUserQuestion` 或书面提问确认本次目标，将 intent 写入 状态.md 的 `intent` 字段，整条流程按档位路由——`cover` 制作封面或场景图（轻量档：场景资产表、视觉符号、画风；刊物角色可跳过双角度定妆）｜`oc` 制作人物 OC 或立绘（轻量至中档：角色档案、身份卡、画风；可跳过双角度定妆）｜`comic` 制作漫画或连载（完整档：深度角色档案、画风定标、身份卡、双角度定妆）｜`full` 全套视觉工程（完整基建）｜`asset-only` 只提取资产（最轻档，只执行 asset，不进入派生层）。**未明确时不默认漫画，须回问用户**——只有明确说"做漫画或连载"才走 `comic` 完整档。 | 补动词；轻量~中→轻量至中；只跑→只执行；+→、 | 缺谓、符号 |
| 12 | L69 > **禁止**再建 `素材/视觉/` 扁平目录；候选/定标/复现一律按三态落盘（见红线6）。 | > **不得**再建 `素材/视觉/` 扁平目录；候选、定标、复现一律按三态写入（见红线 6）。 | 禁止→不得；落盘→写入；/→、 | 口语、符号 |
| 13 | L71 **4. 生成 状态.md**：读 `templates/状态.md` 写入项目根，按「状态更新协议 §1」填充： | **4. 生成 状态.md**：读取 `templates/状态.md` 并写入项目根目录，按「状态更新协议 §1」填充： | 读→读取；写入项目根→写入项目根目录 | 缺谓 |
| 14 | L71 **展示面板（可选）**：仅当老板要看可视化面板时按 `templates/视觉项目总控.html` 套 状态.md 值导出 html——agent 不读 html。 | **展示面板（可选）**：仅当老板要看可视化面板时，按 `templates/视觉项目总控.html` 套用 状态.md 的值导出 html——agent 不读取 html。 | 套→套用；不读→不读取 | 缺谓 |
| 15 | L73 **5. 自检+落位 Phase 0**：9 个标准目录存在｜状态.md 生成且字段正确｜phase=init，next_step=Phase 0。通过后按专家提示词阶段地图路由到 `pop-visual-asset` 执行 Phase 0（基建层），pipeline 不参与后续每轮路由。 | **5. 自检并进入 Phase 0**：确认 9 个标准目录存在、状态.md 已生成且字段正确、phase=init 且 next_step=Phase 0。通过后按专家提示词阶段地图路由至 `pop-visual-asset` 执行 Phase 0（基建层）；pipeline 不参与后续每轮路由。 | 自检+落位→自检并进入；｜→、；路由到→路由至 | 符号、生造 |
| 16 | L77 **import 是"补跑不重做"——已有资产保留，不覆盖重建**。 | **import 遵循"补齐不重做"原则：已有资产保留，不覆盖重建。** | 补跑→补齐；破折号→冒号 | 生造、符号 |
| 17 | L79 **1. 资产清点**（扫描项目根）：`正文/ch*.txt`（小说原文）｜`素材/视觉资产/[角色名]角色档案.md`（资产提取已完成）｜`素材/风格/画风决策.md`（画风已定）｜`素材/美术设定集.md`（基建真源）｜`成品/` `测试/` `_过程/`（已有派生图）｜`漫画/`（已有漫画产出）。 | **1. 资产清点**（扫描项目根）：检查 `正文/ch*.txt`（小说原文）、`素材/视觉资产/[角色名]角色档案.md`（资产提取已完成）、`素材/风格/画风决策.md`（画风已定）、`素材/美术设定集.md`（基建真源）、`成品/` `测试/` `_过程/`（已有派生图）与 `漫画/`（已有漫画产出）。 | ｜→、；补「检查」 | 符号、缺谓 |
| 18 | L81 检测到旧结构目录时，按 `references/落盘规范.md` §七迁移表归位，**禁止留在旧位置**。核心映射：`generated-images/`（原始 UUID 生成图）→`_过程/原始图/`｜`归档/旧版产出/`（未确认旧版图）→`测试/{类型}/`｜`归档/中间产物/`+`归档/工作脚本/`→`_过程/脚本任务/`｜`素材/视觉/`内确认对外图→`成品/{类型}/`（加`-final`）、未确认候选→`测试/`｜`素材/视觉设计方案.md`→`_过程/提示词记录.md`｜`素材/风格/`未冻结定标图→`测试/画风定标/`、已冻结定标图→原位保留（基建真源）。迁移后更新 状态.md 的 `phase`/`next_step` 与就绪态为三态路径，动作落盘 `_过程/迁移记录.md`。 | 检测到旧结构目录时，按 `references/落盘规范.md` §七迁移表归位，**不得留在旧位置**。核心映射：`generated-images/`（原始 UUID 生成图）→`_过程/原始图/`；`归档/旧版产出/`（未确认旧版图）→`测试/{类型}/`；`归档/中间产物/`与`归档/工作脚本/`→`_过程/脚本任务/`；`素材/视觉/`内确认对外图→`成品/{类型}/`（加`-final`）、未确认候选→`测试/`；`素材/视觉设计方案.md`→`_过程/提示词记录.md`；`素材/风格/`未冻结定标图→`测试/画风定标/`、已冻结定标图→原位保留（基建真源）。迁移后更新 状态.md 的 `phase`/`next_step` 与就绪态为三态路径，并将本次动作写入 `_过程/迁移记录.md`。 | 禁止→不得；｜→；；+→与；动作落盘→将本次动作写入 | 符号、口语 |
| 19 | L83 **3. 生成/更新 状态.md+意图闸口**：有散落资产无状态.md→读模板 `templates/状态.md` 生成并按清点结果填充；已有状态.md→按「状态更新协议」读取更新。`intent` 空或未确认→`AskUserQuestion` 询问本次目标（见 Step 0 意图闸口）；已确认沿用不重复问。 | **3. 生成或更新 状态.md 并过意图闸口**：若有散落资产而无 状态.md，则读取模板 `templates/状态.md` 生成并按清点结果填充；若已有 状态.md，则按「状态更新协议」读取并更新。`intent` 为空或未确认时，用 `AskUserQuestion` 询问本次目标（见 Step 0 意图闸口）；已确认则沿用，不重复询问。 | /→或；+→并；→→则/时 | 符号、缺主 |
| 20 | L85 **4. 落地 Phase 决策**（按清点结果）：有正文无资产/画风/设定集→**init**（从 Phase 0 补跑）｜有资产无画风/设定集→**phase1**（补跑 Phase 1 定画风）｜有资产+画风无设定集→**phase2**（补跑 Phase 2 产美术设定集）｜有美术设定集（基建完整）→**phase3+**（基建就绪按需进派生层）｜有基建+已有派生图→按用户意图派生层路由。 | **4. 判定 Phase 决策**（按清点结果）：有正文、无资产或画风或设定集→**init**（从 Phase 0 补做）｜有资产、无画风或设定集→**phase1**（补做 Phase 1 定画风）｜有资产与画风、无设定集→**phase2**（补做 Phase 2 产美术设定集）｜有美术设定集（基建完整）→**phase3+**（基建就绪，按需进入派生层）｜有基建且有派生图→按用户意图进入派生层路由。 | 落地→判定；补跑→补做；+→与 | 口语、符号 |
| 21 | L87 **5. 基建缺口补跑**：基建层（Phase 0/1/2）有缺口必须补齐才能进派生层——缺资产→`pop-visual-asset`｜缺画风→`pop-visual-style`｜缺美术设定集→`pop-visual-art-bible`。已有资产必须归位标准目录，禁止散落。 | **5. 基建缺口补做**：基建层（Phase 0/1/2）有缺口时须补齐才能进入派生层——缺资产→`pop-visual-asset`｜缺画风→`pop-visual-style`｜缺美术设定集→`pop-visual-art-bible`。已有资产须归位到标准目录，不得散落。 | 补跑→补做；必须→须；禁止→不得；进→进入；归位→归位到 | 口语、缺宾 |
| 22 | L89 **6. 更新 状态.md**：按 `references/状态更新协议.md`（通用字段+phase 推进）更新 `phase`/`mode`/`updated_at`/`next_step` 与就绪态，已完成阶段标 `[x]`。 | **6. 更新 状态.md**：按 `references/状态更新协议.md`（通用字段与 phase 推进）更新 `phase`/`mode`/`updated_at`/`next_step` 与就绪态，已完成的阶段标注 `[x]`。 | +→与；标→标注 | 符号 |
| 23 | L93 **日常视觉路由不经此 skill**——用户说"继续视觉/出封面/出OC/出漫画"时，主 agent 按**专家提示词阶段地图**调度，pipeline 不参与每轮路由。调度口诀：**先过意图闸口确认本次目标**（cover/oc/comic/full/asset-only，不默认推漫画）→ **读 `状态.md`** 取状态片段（phase/intent/next_step/就绪态）→ 按下方「阶段地图」+ 就绪门禁直接选子 skill。pipeline 只在初始化/导入/三态迁移/状态维护时被调用。 | **日常视觉路由不经过本 skill**——用户说"继续视觉/出封面/出OC/出漫画"时，主 agent 按**专家提示词阶段地图**调度，pipeline 不参与每轮路由。调度顺序：**先过意图闸口确认本次目标**（cover/oc/comic/full/asset-only，不默认推漫画），再**读取 `状态.md`** 获取状态片段（phase/intent/next_step/就绪态），然后按下方「阶段地图」与就绪门禁直接选择子 skill。pipeline 只在初始化、导入、三态迁移与状态维护时被调用。 | 不经→不经过；口诀→顺序；→→再/然后；+→与；/→、 | 符号、生造 |
| 24 | L123 首次触发视觉专家（无任何视觉项目、非续写）时，先在回复中**直接粘贴 `references/onboarding-guide.md` 全文**建立认知（声明本次为功能介绍+引导、未执行任务），用 1-2 句补充"报书名+形态就开始"；用户已明确要开做则跳过直接干活。派生层不单独引导，统一由本 pipeline 总入口。 | 首次触发视觉专家（无任何视觉项目、非续写）时，先在回复中**直接粘贴 `references/onboarding-guide.md` 全文**以建立认知（声明本次为功能介绍与引导、未执行任务），再用一到两句话补充"报书名与形态即可开始"；用户已明确要开始制作时，跳过引导直接执行。派生层不单独引导，统一由本 pipeline 总入口负责。 | 1-2句→一到两句话；+→与；干活→执行；开做→开始制作 | 口语、符号 |
| 25 | L129 **主 agent 直执**。Step 0/1 初始化/导入均由主 agent 直接执行；子 skill 的实际干活环节由主 agent 读其 SKILL.md 后按 SOP 操作，或（派生层 phase3-6）派发子 agent 执行——子 agent 指令必须显式要求读取对应 SKILL.md，禁止凭记忆"扮演"。pipeline 自身无产出，不存在可派发的只读子任务。日常路由交专家提示词，pipeline 不做常驻循环。 | **由主 agent 直接执行**。Step 0/1 的初始化与导入均由主 agent 直接执行；子 skill 的实际生产环节由主 agent 读取其 SKILL.md 后按规程操作，或（派生层 phase3-6）派发子 agent 执行——子 agent 的指令须显式要求读取对应 SKILL.md，不得凭记忆"扮演"。pipeline 自身无产出，不存在可派发的只读子任务。日常路由交由专家提示词，pipeline 不做常驻循环。 | 直执→由…直接执行；干活→实际生产；读→读取；SOP→规程；必须→须；禁止→不得 | 生造、口语 |
| 26 | L135 1. **读取协议**：日常每轮对话第一件事读项目根 状态.md 获取当前 phase+intent→按阶段地图（专家提示词口径）调度，禁止跳过读 状态.md 直接干活。`状态.md` 是唯一机器状态源，禁止另建 project-state.md 或读 html 取状态。 | 1. **读取协议**：日常每轮对话的第一件事是读取项目根目录的 状态.md，获取当前 phase 与 intent，再按阶段地图（专家提示词口径）调度；不得跳过读取 状态.md 直接开始生产。`状态.md` 是唯一机器状态源，不得另建 project-state.md，也不得读取 html 获取状态。 | 读→读取；+→与；禁止→不得；干活→开始生产 | 口语、符号 |
| 27 | L136 2. **pipeline 只做路由不干活**——所有产出由下游 skill 生成。pipeline 不直接提取资产/选画风/产出美术设定集/画图/生成漫画。 | 2. **pipeline 只负责路由，不承担实际生产**——所有产出由下游 skill 生成。pipeline 不直接提取资产、不选画风、不产出美术设定集、不画图、不生成漫画。 | 只做→只负责；不干活→不承担实际生产；/→、 | 口语、符号 |
| 28 | L137 3. **基建依赖链+就绪门禁不可跳**——资产没就绪不进定画风，画风没就绪不进美术设定集；进派生层（Phase 3-6）前必须验证美术设定集（`素材/美术设定集.md`）存在且签核 ✅ 已认可、画风决策签核 ✅ 已认可。未签核=报错中止，提示先跑基建并完成设定集冻结。 | 3. **基建依赖链与就绪检查不可跳**——资产未就绪不得进入定画风，画风未就绪不得进入美术设定集；进入派生层（Phase 3-6）前须验证美术设定集（`素材/美术设定集.md`）存在且签核 ✅ 已认可、画风决策签核 ✅ 已认可。未签核即报错中止，并提示先执行基建、完成设定集冻结。 | +→与；门禁→检查；没→未；不进→不得进入；必须→须；先跑→先执行 | 符号、口语 |
| 29 | L138 4. **美术设定集是唯一真源**——派生层（cover/oc/comic）只消费美术设定集，禁止各自重建人物/场景/符号/画风。视觉事实改动必须回 art-bible 升级设定集版本。 | 4. **美术设定集是唯一真源**——派生层（cover/oc/comic）只消费美术设定集，不得各自重建人物、场景、符号与画风。视觉事实改动须回到 art-bible 升级设定集版本。 | 禁止→不得；/→、；必须→须；回→回到 | 符号 |
| 30 | L139 5. **意图闸口前置，不默认推漫画**——intent 为 `cover`/`oc`/`asset-only` 时不自动进漫画；未明确意图时回问用户，禁止假设用户奔漫画去。 | 5. **意图闸口前置，不默认推漫画**——intent 为 `cover`/`oc`/`asset-only` 时不自动进入漫画；未明确意图时回问用户，不得假设用户奔漫画去。 | 进→进入；禁止→不得 | 缺动 |
| 31 | L140 6. **落盘三态强制（`references/落盘规范.md`）**——所有派生产出按生命周期落盘：候选→`测试/{类型}/`，确认→`成品/{类型}/`（加 `-final`），脚本/原始图/任务清单→`_过程/`。禁止再写扁平 `素材/视觉/`、禁止 UUID dump 到 `generated-images/`、禁止 `-V1`/`--v1` 版本变体。 | 6. **三态写入强制（`references/落盘规范.md`）**——所有派生产出按生命周期写入：候选→`测试/{类型}/`，确认→`成品/{类型}/`（加 `-final`），脚本、原始图、任务清单→`_过程/`。不得再写扁平 `素材/视觉/`，不得将 UUID 命名的生成图堆放到 `generated-images/`，不得使用 `-V1`/`--v1` 版本变体。 | 落盘三态→三态写入；落盘→写入；禁止→不得；dump→堆放；/→、 | 生造、口语 |
| 32 | L148 \| `状态.md`（项目根） \| 每次对话第一件事 \| 唯一机器状态源（mode/phase/intent/next_step+就绪态+最近产出） \| | \| `状态.md`（项目根） \| 每次对话第一件事 \| 唯一机器状态源（mode/phase/intent/next_step、就绪态、最近产出） \| | +→、 | 符号 |
| 33 | L149 \| `references/状态更新协议.md` \| phase 完成后更新 状态.md 时（含 Step 0/1 落地） \| 通用字段更新+phase 推进+就绪态规范（单源） \| | \| `references/状态更新协议.md` \| phase 完成后更新 状态.md 时（含 Step 0/1 执行） \| 通用字段更新、phase 推进与就绪态规范（单源） \| | 落地→执行；+→、 | 生造、符号 |
| 34 | L150 \| `references/落盘规范.md` \| 任何落盘/迁移/命名时 \| 三态分离+统一命名+版本 final 标记+skill 映射+旧目录迁移表 \| | \| `references/落盘规范.md` \| 任何写入、迁移或命名时 \| 三态分离、统一命名、版本 final 标记、skill 映射与旧目录迁移表 \| | 落盘→写入；/→、；+→、 | 生造、符号 |
| 35 | L151 \| `templates/视觉项目总控.html` \| 仅老板要看可视化面板时 \| 由 状态.md 值套模板导出的展示面板，agent 不读 \| | \| `templates/视觉项目总控.html` \| 仅老板要看可视化面板时 \| 由 状态.md 的值套用模板导出的展示面板，agent 不读取 \| | 套→套用；不读→不读取 | 缺谓 |
| 36 | L152 \| `references/onboarding-guide.md` \| **首次触发视觉专家时输出** \| 首次对话引导语（功能全景+意图闸口+引导第一步） \| | \| `references/onboarding-guide.md` \| **首次触发视觉专家时输出** \| 首次对话引导语（功能全景、意图闸口与引导第一步） \| | +→、 | 符号 |

## 2、references/onboarding-guide.md（1 个改动单元）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 37 | L7 老板，收到。先声明：**这次没动用什么干活，只是带你看看**——你这本小说，在我手上能"长"成什么样。 | 老板，收到。先声明：**这次没有执行实际生产，只是带你看看**——你这本小说，在我手上能"长"成什么样。 | 没动用什么干活→没有执行实际生产 | 口语 |

## 3、references/状态更新协议.md（2 个改动单元）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 38 | L3 > 本文件是 `状态.md` 所有更新操作的唯一规范。初始化（Step 0）、导入重建（Step 1）、Phase 推进更新（asset/style/art-bible/cover/oc/comic 完成后 / 路由中）都读本文件，禁止在其他文件复述字段表（防多源漂移）。 | > 本文件是 `状态.md` 所有更新操作的唯一规范。初始化（Step 0）、导入重建（Step 1）、Phase 推进更新（asset/style/art-bible/cover/oc/comic 完成后或路由中）均须读取本文件，不得在其他文件中复述字段表，以免出现多个来源。 | 都读→均须读取；禁止→不得；防多源漂移→以免出现多个来源 | 生造 |
| 39 | L7 **只更新本次操作涉及的字段，不全量替换。** | **本协议只更新本次操作涉及的字段，不做全量替换。** | 补主语「本协议」 | 缺主 |

## 4、references/落盘规范.md（24 个改动单元）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 40 | L3 > 本文件是 **pop-visual-pipeline 定义、全 visual 子 skill 必须遵守** 的落盘唯一权威源。v2.0.0 | > 本文件由 **pop-visual-pipeline 定义，全 visual 子 skill 须遵守**，是文件写入的唯一权威源。v2.0.0 | 必须→须；落盘唯一权威源→文件写入的唯一权威源 | 生造 |
| 41 | L4 > 目的：根治「过程文件 / 测试文件 / 成品确认文件」不分、同图多版本难区分、跨 skill 命名不统一的乱象。 | > 目的：根治三类乱象——过程文件、测试文件与成品确认文件不分；同一张图多个版本难以区分；跨 skill 命名不统一。 | 并列项拆句，/→、 | 并列不当 |
| 42 | L5 > v2.0.0：补齐视频（pop-video-comic / pop-comic-content 出片）与内容（pop-comic-content 文本三件套）两条子管线，覆盖漫画自体系命名，全链路统一。 | > v2.0.0：补齐视频（pop-video-comic / pop-comic-content 出片）与内容（pop-comic-content 文本三件套）两条子管线，覆盖漫画自体系命名，整条流程统一。 | 全链路→整条流程 | 生造 |
| 43 | L11 视觉产出按「生命周期」分三态落盘，**全 skill 统一遵守**： | 视觉产出按「生命周期」分三态写入，**全 skill 统一遵守**： | 落盘→写入 | 生造 |
| 44 | L21 • **`测试/` 也容纳 `画册页/` 子目录**（画册页主视觉候选独立归类，避免与人物/规则候选混淆）。 | • **`测试/` 也容纳 `画册页/` 子目录**（画册页主视觉候选独立归类，避免与人物、规则候选混淆）。 | /→、 | 符号 |
| 45 | L26 ## 二、为什么（命中乱象） | ## 二、要根治的乱象 | 命中乱象→要根治的乱象 | 生造 |
| 46 | L30 1. **`素材/视觉/` 扁平大杂烩** → 封面/OC/画册页/定妆/场景全塞一个目录 → 拆为顶层 `成品/` `测试/` `_过程/` 三态。 | 1. **`素材/视觉/` 扁平大杂烩** → 封面、OC、画册页、定妆、场景全部塞进同一个目录 → 拆为顶层 `成品/`、`测试/`、`_过程/` 三态。 | /→、；全塞→全部塞进 | 符号、口语 |
| 47 | L31 2. **`generated-images/` 脱离规范** → image_generate 原始图按 UUID dump → 收敛到 `_过程/原始图/`。 | 2. **`generated-images/` 脱离规范** → image_generate 生成的原始图按 UUID 命名堆积 → 收敛到 `_过程/原始图/`。 | dump→命名堆积；补「生成的」 | 口语 |
| 48 | L32 3. **`归档/` 事后人为补救** → 旧版/脚本/任务清单被手动归档 → 规范内定义 `测试/`（旧版候选）与 `_过程/`（脚本任务）的归属。 | 3. **`归档/` 事后人为补救** → 旧版、脚本、任务清单被手动归档 → 规范内定义 `测试/`（旧版候选）与 `_过程/`（脚本任务）的归属。 | /→、 | 符号 |
| 49 | L33 4. **版本命名不统一** → `-v1/--v1/-V1` 混用、同名不同内容 → 统一 `vN` + `-final` 标记（§三）。 | 4. **版本命名不统一** → `-v1/--v1/-V1` 混用、同名不同内容 → 统一为 `vN` 加 `-final` 标记（§三）。 | 统一…+→统一为…加 | 符号 |
| 50 | L53 ### 3.1b 视频与内容命名（自体系，遵循同名版本+final 法则） | ### 3.1b 视频与内容命名（自体系，遵循同名版本与 final 法则） | +→与 | 符号 |
| 51 | L72 视觉顶层目录与小说写作管线（起点/番茄）顶层**完全不撞**： | 视觉顶层目录与小说写作管线（起点/番茄）顶层**完全不重名**： | 不撞→不重名 | 口语 |
| 52 | L79 与小说侧扁平的 `素材/*.md`（用户意图/赛道调研/文风锚定）**不重名、不混淆**。 | 与小说侧扁平的 `素材/*.md`（用户意图、赛道调研、文风基准）**不重名、不混淆**。 | /→、；文风锚定→文风基准 | 符号 |
| 53 | L86 > 基建层产出是**真源**，不属三态，落盘后冻结、只读消费。改动必须回对应 skill 升级版本。 | > 基建层产出是**真源**，不属三态，写入后冻结、只读消费。改动须回到对应 skill 升级版本。 | 落盘→写入；必须回→须回到 | 生造 |
| 54 | L90 \| `素材/视觉资产/` \| `pop-visual-asset` \| 角色档案/场景资产表/视觉符号库/IPDNA/势力地理规则档案/资产索引 \| | \| `素材/视觉资产/` \| `pop-visual-asset` \| 角色档案、场景资产表、视觉符号库、IPDNA、势力地理规则档案、资产索引 \| | /→、 | 符号 |
| 55 | L97 ## 五、派生层三态落盘映射（各 skill 遵守） | ## 五、派生层三态写入映射（各 skill 遵守） | 落盘→写入 | 生造 |
| 56 | L123 1. **三态不混落** — 未确认图进 `测试/`，确认对外图进 `成品/`，脚本/原始图/任务清单进 `_过程/`。禁止把三种文件塞同一目录。 | 1. **三态不混落** — 未确认图进 `测试/`，确认对外图进 `成品/`，脚本、原始图、任务清单进 `_过程/`。不得把三种文件塞进同一目录。 | /→、；禁止→不得；塞同一→塞进同一 | 符号、口语 |
| 57 | L128 6. **视频/内容自体系同样遵循版本+final** — 视频成品必须是 `第{N}章(-配音)-vN-final.mp4`，禁止裸名 `成品.mp4`；`preview/frames/audio/` 是过程不进成品态。<br>内容三件套确认后即在 `内容/`，是文本成品（无版本），未确认不写入 `内容/`。 | 6. **视频与内容自体系同样遵循"版本加 final"** — 视频成品必须是 `第{N}章(-配音)-vN-final.mp4`，不得使用裸名 `成品.mp4`；`preview/frames/audio/` 是过程，不进入成品态。<br>内容三件套确认后即存入 `内容/`，是文本成品（无版本），未确认不写入 `内容/`。 | /→与；+→加；禁止→不得；即在→即存入；不进→不进入 | 符号、口语 |
| 58 | 必须（同文件其余同类处，共 2 处） | 须 | 硬约束统一用「须」 | 语域 |
| 59 | 禁止（同文件其余同类处，共 4 处） | 不得 | 硬约束统一用「不得」 | 语域 |

## 5、templates/视觉项目总控.html（1 个改动单元）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 60 | L479     pop-visual-pipeline · 视觉项目总控 · 自动生成请勿手动编辑 |     pop-visual-pipeline · 视觉项目总控 · 本页面自动生成，请勿手动编辑 | 自动生成请勿手动编辑→本页面自动生成，请勿手动编辑 | 缺主 |

## 未改动（按须知第四、五节保留）

| 项 | 落点 | 理由 |
|:--|:--|:--|
| `references/落盘规范.md` 文件名与首行标题 | 文件名 / 文档标题 | 属路径与文档名，未被改；表头「落盘位置」属字段名，未改 |
| 各 SKILL.md 首段版本说明 | `> ...v4.2.0 / v2.0.0...` | 版本记录，属历史记录段 |
| `templates/状态.md` 全文 | 字段模板 | 全为字段名与占位值（mode/phase/intent/next_step/就绪态等），无规程说明文字 |
| `templates/视觉项目总控.html` 标签属性 | 全文 | 标签、class、id、内联 CSS、`<!--STATE:*-->` 占位符一律未动；仅改页脚一句人读文案 |
| 目录结构代码块与命令 | SKILL.md、落盘规范.md | 路径、命令、字段名属机器可读内容 |
| `onboarding-guide.md` 其余话术 | 全文 | 面向用户的引导话术，含「你/我/我全包」等人称，属对用户输出，非规程说明文字；仅改「没动用什么干活」一处 |
| 「门禁A/门禁B」「三态」「基建真源」「美术设定集」「二创五问」等术语名 | 全文 | 术语与枚举，按第五节保留 |
| 表格列数与表头结构 | 全文 | 未增删列、未合并章节 |

## 待裁问题（未改，供登记）

1. `templates/视觉项目总控.html` 目录树把 `视觉项目总控.html` 注为「pipeline 唯一状态文件」，与 SKILL.md「`状态.md` 是唯一机器状态源」矛盾；且 html 本应只是展示面板。属条款事实，未改。
2. `references/落盘规范.md` §四称美术设定集「八篇合一」，`SKILL.md` 与 `templates/视觉项目总控.html` 称「画风/人物/场景/符号/一致性五篇合一」；篇数不一致。属条款内容，未改。
3. `references/落盘规范.md` §三 出现「3.1」「3.1b」「3.2」混合编号。属结构，未改。
4. `references/落盘规范.md` §五 表内 skill 命名混用「pop-visual-cover」与「cover/oc」。属条款，未改。
5. `references/落盘规范.md` §七对象中「`测试/{人物/规则/势力}/…`」用花括号枚举目录名。属路径写法，未改。
