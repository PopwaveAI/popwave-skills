# pop-visual-style 改动清单

> **修改前**＝`skills/pop-visual-style/SKILL.md`（221 行）、`skills/pop-visual-style/references/lighting-composition-templates.md`（96 行）
> **修改后**＝`pop-visual-style/SKILL-改后.md`、`pop-visual-style/lighting-composition-templates-改后.md`
> 判据三问：**谁**（主语在不在）、**做什么**（动作在不在）、**凭什么判断**（依据或正反例在不在）。
> 本轮做两件事：一、把压缩单元展开成完整句子；二、把语域收到书面。
> 体例说明：原文／改后两列中，表格行的半角竖线改用全角 ｜，以免与清单表格的列分隔线冲突；路径、命令、脚本名、字段名、枚举值、数字阈值一概未动，故不复述。
> 病类缩写：缺主＝缺主语｜缺谓＝缺谓语｜缺宾＝缺宾语｜生造＝生造词｜符号＝符号串当句｜破折＝破折号硬切｜语域＝语域降档｜短语＝名词短语当句｜搭配＝搭配错位｜压失＝压缩失准。

## 一、SKILL.md · 前言与边界（原第 3—23 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 1 | > 通用文生图 + 画风DNA库。纯文生图，一键出图。当前版本 v2.1.0，完整版本历史见 [CHANGELOG.md](CHANGELOG.md)。 | > 本技能提供通用文生图能力与画风DNA库。只做纯文生图，单次操作即可生成图片。当前版本为 v2.1.0，完整版本历史见 [CHANGELOG.md](CHANGELOG.md)。 | 一键出图→单次操作即可生成图片 | 短语、符号、缺主 |
| 2 | ## 做什么 | ## 职责范围 | — | 语域 |
| 3 | 输入：画面描述（自然语言）+ 画风选择（从37种DNA库选或自定义）。 | 输入：画面描述（自然语言），以及画风选择（从 37 种 DNA 库中选择，或自定义）。 | 符号→文字 | 符号、缺谓 |
| 4 | 输出：生成图片 + 提示词记录。 | 输出：生成的图片，以及提示词记录。 | — | 符号 |
| 5 | 核心价值：**画风库是营销专家skill群的公共资产**——独立执行纯文生图，又作为art-bible/cover/oc/comic的画风层引用源。 | 核心价值：画风库是营销专家 skill 群的公共资产，既支持独立执行纯文生图，又作为 art-bible、cover、oc、comic 的画风层引用源。 | — | 破折、缺主 |
| 6 | **画风×内容解耦铁律（v1.10.0 核心）**：DNA库每条画风分两层——`dna` 是**中性技法层**（怎么画：线稿/上色/光影/比例/特征，可跨题材复用）… | **画风与内容解耦铁律（v1.10.0 核心）**：DNA 库中每条画风分两层：`dna` 是**中性技法层**，说明怎么画（线稿、上色、光影、比例、特征），可跨题材复用… | ×→与；破折号→冒号 | 符号、破折、缺谓 |
| 7 | ｜ 本skill做 ｜ 本skill不做（交给专用skill） ｜ | ｜ 本 skill 负责 ｜ 本 skill 不负责，交由专用 skill 承担 ｜ | 做→负责；交给→交由承担 | 缺谓 |
| 8 | ｜ 画风DNA库管理（37种+扩展） ｜ 美术设定集（画风/人物/场景/符号五篇合一）→ art-bible ｜ | ｜ 画风DNA库管理（37种及扩展） ｜ 美术设定集（画风、人物、场景、符号五篇合一），交由 art-bible ｜ | +→及；/→、；→→交由 | 符号 |
| 9 | ｜ 提示词结构知识（6段式/V3/高精度4块） ｜ 封面设计（视觉钩子+文字融入+构图骨架）→ cover ｜ | ｜ 提示词结构知识（6段式、V3、高精度4块） ｜ 封面设计（视觉钩子、文字融入、构图骨架），交由 cover ｜ | /、+→、；→→交由 | 符号 |
| 10 | ｜ 光照-画风兼容性矩阵 ｜ 立绘设计（六层文字+角色调研+系列冻结）→ oc ｜ | ｜ 光照与画风兼容性矩阵 ｜ 立绘设计（六层文字、角色调研、系列冻结），交由 oc ｜ | -→与；联列符→、；→→交由 | 符号 |
| 11 | ｜ 构图模板（CT1/CT2） ｜ 漫画生成（分镜+定妆图+跨章一致性）→ comic ｜ | ｜ 构图模板（CT1、CT2） ｜ 漫画生成（分镜、定妆图、跨章一致性），交由 comic ｜ | /、+→、；→→交由 | 符号 |
| 12 | ｜ 纯文生图执行 ｜ 参考图策略（参考点放权）→ cover/oc ｜ | ｜ 纯文生图执行 ｜ 参考图策略（参考点放权），交由 cover、oc ｜ | →→交由 | 符号 |
| 13 | ｜ 质量触发词+画风前置优化 ｜ 文化元素设计（定场诗/印章）→ cover/oc ｜ | ｜ 质量触发词与画风前置优化 ｜ 文化元素设计（定场诗、印章），交由 cover、oc ｜ | +→与；联列符→、 | 符号 |
| 14 | **主 agent 直执**——画风选择与定标验收是用户多轮交互门禁（选画风/看图认可/冻结签核），提示词组装与生成是 `image_generate` 工具调用，批量定标走 `batch_test.py` 固定脚本，无自然子 agent 适配点。 | **主 agent 直接执行**：画风选择与定标验收是需要用户多轮交互的检查点（选择画风、确认图片、冻结签核），提示词组装与生成通过调用 `image_generate` 工具完成，批量定标通过固定脚本 `batch_test.py` 执行，没有适合派给子 agent 的环节。 | 直执→直接执行；是工具调用→通过调用工具完成 | 生造、破折、搭配 |
| 15 | ## 怎么运作（SOP全内联） | ## 怎么运作（规程全文内联） | SOP→规程 | 生造、压失 |

## 二、SKILL.md · Step 1 画风选择（原第 43—55 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 16 | 2. **筛选推荐**：按赛道匹配 `suggested_genres` 字段、按关键词匹配 `keywords` 字段、按类别（二次元17/国漫6/韩漫3/插画概念10）→推荐1-3种画风，附推荐理由（视觉特征+代表作+适合赛道）。 | 2. **筛选推荐**：按赛道匹配 `suggested_genres` 字段，按关键词匹配 `keywords` 字段，按类别（二次元17、国漫6、韩漫3、插画概念10）推荐1-3种画风，并附推荐理由（视觉特征、代表作、适合赛道）。 | →→删；/、+→、 | 符号 |
| 17 | 3. **用户选择**：可选池中画风，或描述自定义风格。**自定义画风处理**：按DNA库格式组装 `dna`（英文画风描述≤800字符）+ `constraint`（风格保真约束）；生成后验证辨识度，未达标调整dna描述。 | 3. **用户选择**：从候选池中选择画风，或描述自定义风格。**自定义画风处理**：按 DNA 库格式组装 `dna`（英文画风描述≤800字符）与 `constraint`（风格保真约束）；生成后验证辨识度，未达标则调整 `dna` 描述。 | +→与；未达标→未达标则 | 缺谓、符号 |
| 18 | 4. **Pinterest 参考图搜索（单张锚定）**：搜索有成本（Bright Data 付费），**一次搜索、全程复用**。选定画风后搜 1 张最符合画风的参考图，作为全书风格准绳： | 4. **Pinterest 参考图搜索（单张固定）**：搜索有成本（Bright Data 付费），**一次搜索，全程复用**。选定画风后搜索 1 张最符合画风的参考图，作为全书风格准绳： | 锚定→固定；顿号→逗号 | 生造 |
| 19 | - 关键词：画风名+赛道+主要特征（如"暗黑修仙厚涂 玄幻 封面"） | - 关键词：画风名、赛道与主要特征（如"暗黑修仙厚涂 玄幻 封面"） | +→、与 | 符号 |
| 20 | - 从结果中选 **1 张最符合画风**的参考图（单张锚定），删掉其余候选，避免多图不确定性 | - 从结果中选择 **1 张最符合画风**的参考图（单张固定），删除其余候选，避免多图带来的不确定性 | 锚定→固定；删掉→删除；多图不确定性→多图带来的不确定性 | 生造、语域 |
| 21 | 6. **兼容性检查**（铁律❌3，查 `references/lighting-composition-templates.md` 兼容性矩阵）：柔美风格（少女水彩/轻小说/日系赛璐珞等）→必须用LT2，禁止LT1；平面风格（扁平矢量/波普/极简线条）→必须用LT3；暗黑风格→用LT1。 | 6. **兼容性检查**（铁律❌3，查 `references/lighting-composition-templates.md` 兼容性矩阵）：柔美风格（少女水彩、轻小说、日系赛璐珞等）必须用LT2，禁止用LT1；平面风格（扁平矢量、波普、极简线条）必须用LT3；暗黑风格用LT1。 | /→、；→→删；禁止LT1→禁止用LT1 | 符号 |
| 22 | 7. **输出记录**到 `素材/画风选择记录.md`：选定画风名+类别+keywords / dna+constraint（**纯技法层，禁止混入内容**）/ content_theme / recommended_lighting+recommended_composition / 兼容性检查结果。 | 7. **输出记录**到 `素材/画风选择记录.md`：选定画风名、类别、`keywords`、`dna` 与 `constraint`（**纯技法层，禁止混入内容**）、`content_theme`、`recommended_lighting` 与 `recommended_composition`、兼容性检查结果。 | +、/→、；字段名加反引号 | 符号 |

## 三、SKILL.md · Step 2 提示词组装（原第 68—80 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 23 | ｜ 2 画风DNA ｜ Step 1 选定的 `dna`+`constraint`（**纯技法层，禁止混入内容**；必须第2段紧跟质量触发词——画风前置符合 Seedream 注意力权重机制，执行力最强，铁律❌2） ｜ | ｜ 2 画风DNA ｜ Step 1 选定的 `dna` 与 `constraint`（**纯技法层，禁止混入内容**；必须放在第2段紧跟质量触发词，因画风前置符合 Seedream 注意力权重机制，执行力最强，铁律❌2） ｜ | +→与；破折号→逗号、因 | 符号、破折 |
| 24 | ｜ 5 场景 ｜ **内容层接入**：用户场景描述优先；跨题材复用时用户场景覆盖；无题材/要画风原生题材时用 `content_theme` 兜底 ｜ | ｜ 5 场景 ｜ **内容层接入**：用户场景描述优先；跨题材复用时用户场景覆盖；无题材或要画风原生题材时用 `content_theme` 兜底 ｜ | /→或 | 符号 |
| 25 | **内容层接入规则（铁律❌10）**：用户给了具体场景→用用户场景描述（`content_theme` 仅参考不覆盖）；跨题材复用画风（如「国漫玄幻厚涂」画现代都市）→场景段用用户场景，**技法层不变**，禁止 `content_theme` 题材元素污染；用户没给题材→`content_theme` 兜底。 | **内容层接入规则（铁律❌10）**：用户给出具体场景时，使用用户场景描述（`content_theme` 仅供参考，不覆盖）；跨题材复用画风时（如用「国漫玄幻厚涂」画现代都市），场景段使用用户场景，**技法层不变**，禁止 `content_theme` 的题材元素污染画面；用户未给出题材时，用 `content_theme` 兜底。 | 给了→给出…时；没给→未给出…时 | 符号、缺谓、语域 |
| 26 | **字数检查**：英文提示词≤600词｜人物描述≤100字｜dna字段≤800字符（DNA库已保证）｜用自然语言连贯描述，非关键词堆叠。 | **字数检查**：英文提示词≤600词，人物描述≤100字，`dna` 字段≤800字符（DNA 库已保证）；须用自然语言连贯描述，不得堆叠关键词。 | ｜→，；非→不得 | 符号、缺谓 |
| 27 | **自检**：画风DNA从DNA库取（❌1）｜画风在第2段（❌2）｜光照与画风兼容（❌3）｜**画风段只含技法无题材内容词（❌10）**——检查 `dna` 段是否误带世界观/服装/建筑/招数等｜人物≤100字｜总词数≤600。 | **自检**：画风DNA从DNA库取（❌1）；画风在第2段（❌2）；光照与画风兼容（❌3）；**画风段只含技法、不含题材内容词（❌10）**，检查 `dna` 段是否误带世界观、服装、建筑、招数等；人物≤100字；总词数≤600。 | ｜→；；无→不含；/→、 | 符号、破折 |
| 28 | **备选结构**（需文字渲染——书名/角色名等时切换）：**V3结构化公式**见 `seedream-prompt-guide.md` §二；**高精度4块结构**见同文件 §三（商业级，含HARD CONSTRAINTS）。纯文生图默认6段式，备选供cover/oc skill跨场景使用。 | **备选结构**（需要文字渲染，如书名、角色名等时切换）：**V3结构化公式**见 `seedream-prompt-guide.md` §二；**高精度4块结构**见同文件 §三（商业级，含 HARD CONSTRAINTS）。纯文生图默认使用6段式，备选结构供 cover、oc skill 跨场景使用。 | 破折号→逗号；默认→默认使用 | 破折、符号 |

## 四、SKILL.md · Step 3 执行生成（原第 96—111 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 29 | > **⚠️ 画风定标图禁止加品牌水印**：定标图是下游图生图 `ref_image` 的生产参考，加 `popwave.cn` 会被 Seedream 当画面内容带进下游成品层层污染；水印只在对外展示产出（OC 立绘/封面/漫画页）落地后叠加。 | > **⚠️ 画风定标图禁止加品牌水印**：定标图是下游图生图 `ref_image` 的生产参考，加 `popwave.cn` 会被 Seedream 当作画面内容带入下游成品，造成层层污染；水印只在对外展示的产出（OC 立绘、封面、漫画页）完成之后叠加。 | 当→当作；带进→带入；落地后→完成之后；/→、 | 语域 |
| 30 | 3. **输出目录**（落盘三态见 `../pop-visual-pipeline/references/落盘规范.md`）：定标候选统一输出 `测试/画风定标/`（不存在则创建）；**认可冻结后**复制到 `素材/风格/`（基建真源）并在 `画风决策.md` 记录冻结路径——`测试/` 内不标 final… | 3. **输出目录**（三态写入规则见 `../pop-visual-pipeline/references/落盘规范.md`）：定标候选统一输出到 `测试/画风定标/`（目录不存在则创建）；**认可冻结后**复制到 `素材/风格/`（基建真源），并在 `画风决策.md` 记录冻结路径；`测试/` 内不标 final… | 落盘三态→三态写入规则；破折号→分号 | 语域、破折 |
| 31 | 4. **回写提示词记录**（追加到项目文件）：模型/画风/光照模板/构图模板/完整提示词/尺寸/输出路径/状态✅。 | 4. **回写提示词记录**（追加到项目文件）：模型、画风、光照模板、构图模板、完整提示词、尺寸、输出路径、状态✅。 | /→、 | 符号 |
| 32 | 5. **迭代**：用户不满意时诊断问题（画风辨识度不足→强化dna描述；光照不兼容→换模板；人物变形→加约束）→调整对应维度→重新生成（文件名递增v2, v3...）→更新记录。 | 5. **迭代**：用户不满意时诊断问题（画风辨识度不足则强化 `dna` 描述；光照不兼容则更换模板；人物变形则增加约束），调整对应维度后重新生成（文件名递增为 v2、v3……），并更新记录。 | 换→更换；加→增加 | 符号 |

## 五、SKILL.md · Step 4 画风定标（原第 113—176 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 33 | ### Step 4: 画风定标（Pipeline 语境下必做；独立纯文生图跳过本步，Step 1→2→3 直接出图） | ### Step 4: 画风定标（Pipeline 语境下必做；独立纯文生图跳过本步，Step 1 至 Step 3 直接出图） | →→至 | 符号 |
| 34 | 画风是"只能靠视觉验证"的抽象资产。本步用**小说次要视觉锚点 + 固定模板**渲染定标图…未认可不冻结、不放行下游。 | 画风是"只能靠视觉验证"的抽象资产。本步用**小说次要视觉锚点与固定模板**渲染定标图…未认可则不冻结，也不放行到下游。 | +→与；不放行下游→也不放行到下游 | 符号、缺谓 |
| 35 | - **素材关联**：测试素材**从小说提取次要视觉锚点**——战斗场景/地点（`--scene`）、路人/NPC/龙套（`--side`）；和小说强相关→保留代入感，无关紧要→不承担形象验收。**禁止用主角/主要角色**——形象归 `pop-visual-art-bible`/`oc` 环节承担（铁律❌5）。 | - **素材关联**：测试素材**从小说中提取次要视觉锚点**，包括战斗场景或地点（`--scene`）、路人、NPC 或龙套（`--side`）；素材与小说强相关，保留代入感，但无关紧要，不承担形象验收。**禁止使用主角或主要角色**，其形象由 `pop-visual-art-bible` 与 `oc` 环节承担（铁律❌5）。 | 禁止用→禁止使用；破折号→逗号 | 符号、破折、缺主 |
| 36 | - **变量隔离**：画风定标的唯一变量是**画风**。测试素材**一次确定、固定使用**（注入后不换），构图固定、光照固定；**素材固定后禁止中途换素材**——那会把"画风问题"和"素材适配度问题"混为一谈。 | - **变量隔离**：画风定标的唯一变量是**画风**。测试素材**一次确定，固定使用**（传入后不再更换），构图固定，光照固定；**素材固定后禁止中途更换**，否则会把"画风问题"和"素材适配度问题"混为一谈。 | 注入→传入；那会→否则会 | 符号、破折、语域、生造 |
| 37 | - **稳定复现（工作流主线）**：不只验证"这一张对不对"，还要验证"**能不能稳定复现**"——固定 seed + 同一提示词复现对比，确认画风结果稳定而非单次运气。**只有稳定复现的画风才冻结为基线。** | - **稳定复现（工作流主线）**：不只验证"这一张对不对"，还要验证"**能不能稳定复现**"：固定 seed 与同一提示词做复现对比，确认画风结果稳定，而非出于单次运气。**只有稳定复现的画风才冻结为基线。** | 破折号→冒号；+→与 | 符号、破折 |
| 38 | ｜ `comic`/`full` ｜ **完整定标（必做）**：完整门禁 + 稳定复现验证（见下文 3），进入 character（Phase 2）前先定标 ｜ | ｜ `comic`/`full` ｜ **完整定标（必做）**：完整门禁与稳定复现验证（见下文 3），进入 character（Phase 2）前先定标 ｜ | +→与 | 符号 |
| 39 | ｜ `cover`/`oc` ｜ **agent 自检分支**：出定标图后 agent 自查辨识度/配色/光影/无文字即可，**不设强制用户门禁、不强制稳定复现**；定标图可单张，达标即标记 `✅ 已认可` 供下游作画风参考 ｜ | ｜ `cover`/`oc` ｜ **agent 自检分支**：出定标图后 agent 自查辨识度、配色、光影、无文字即可，**不设强制用户门禁，不强制稳定复现**；定标图可单张，达标即标记 `✅ 已认可` 供下游作画风参考 ｜ | /→、；顿号→逗号 | 符号 |
| 40 | - **场景类**（`--scene`）：战斗场景/地点/环境片段，能体现画风光影与氛围。示例：`abandoned ancient temple courtyard, …` | - **场景类**（`--scene`）：战斗场景、地点或环境片段，能体现画风光影与氛围。示例：`abandoned ancient temple courtyard, …` | /→、或 | 符号 |
| 41 | - **人物类**（`--side`）：路人/NPC/龙套，非主角、不需一致性、纯文生图。示例：`an old street vendor …` | - **人物类**（`--side`）：路人、NPC 或龙套，非主角，不要求一致性，纯文生图。示例：`an old street vendor …` | 不需→不要求 | 符号、缺谓 |
| 42 | - 小说暂无可提取的合适次要元素（如排期未定）时，可兜底用脚本内置中性素材（`--scene`/`--side` 不传）。**禁止传 `--character`/`--character-image` 引入主角/主要角色**。 | - 小说暂无可提取的合适次要元素（如排期未定）时，可兜底使用脚本内置的中性素材（不传 `--scene` 与 `--side`）。**禁止传 `--character` 或 `--character-image` 引入主角、主要角色**。 | 用→使用；/→或、 | 符号 |
| 43 | **2. 批量导出定标任务**（一次出多张变体；必须走固定脚本，禁止现场手写提示词、单张串行——那是"每次全新设计"，不稳定又慢）： | **2. 批量导出定标任务**（一次出多张变体；必须使用固定脚本，禁止现场手写提示词、单张串行，后者等于"每次全新设计"，既不稳定又慢）： | 走→使用；那是→后者等于；又→既…又 | 破折、语域 |
| 44 | - **`--style-names`**：从 DNA 库按画风名批量测（推荐），脚本自动取 `dna`+`constraint`+`recommended_composition`+`recommended_lighting`（默认 8 线程并发批量 + 自动 PE 日志）；画风 DNA 放第 2 段由脚本固定模板保证（铁律❌2） | - **`--style-names`**：从 DNA 库按画风名批量测（推荐），脚本自动取 `dna`、`constraint`、`recommended_composition`、`recommended_lighting`（默认 8 线程并发批量与自动 PE 日志）；画风 DNA 放在第 2 段，由脚本固定模板保证（铁律❌2） | +→、与；放→放在 | 符号、缺谓 |
| 45 | - **`--config 变体.json`**：精调变体，每个变体可单独改 `dna`/`constraint`/`lighting` | - **`--config 变体.json`**：精调变体，每个变体可单独修改 `dna`、`constraint`、`lighting` | 改→修改；/→、 | 符号 |
| 46 | - **输出**：`generation_tasks.json`（每个变体一个任务，含 prompt/size/ref_images/output_path）+ `pe-log.json`（含测试素材/模板/每个变体完整 prompt，可复现） | - **输出**：`generation_tasks.json`（每个变体一个任务，含 prompt、size、ref_images、output_path）与 `pe-log.json`（含测试素材、模板、每个变体完整 prompt，可复现） | +→与；/→、 | 符号 |
| 47 | - 从结果中选达标变体作为候选定标图；**不达标回炉只改该变体 JSON 的一个子维度，再跑同脚本，不重写调用** | - 从结果中选择达标变体作为候选定标图；**不达标回炉时只改该变体 JSON 的一个子维度，重新执行同一脚本，不重写调用** | 再跑同脚本→重新执行同一脚本 | 语域、缺谓 |
| 48 | **3. 🚪 门禁：画风定标验收 + 稳定复现验证**（comic/full 完整档必做）： | **3. 🚪 门禁：画风定标验收与稳定复现验证**（comic/full 完整档必做）： | +→与 | 符号 |
| 49 | ｜ 辨识度 ｜ 0.3 秒能否认出"来自哪个画风体系"（厚涂玄幻/赛璐珞/水彩/暗黑高对比） ｜ | ｜ 辨识度 ｜ 0.3 秒能否认出"来自哪个画风体系"（厚涂玄幻、赛璐珞、水彩、暗黑高对比） ｜ | /→、 | 符号 |
| 50 | **未达标不冻结**——回炉微调 DNA 片段（非重选风格），重新生成 v2、v3... 直到达标。 | **未达标不冻结**：回炉微调 DNA 片段（非重选风格），重新生成 v2、v3……直到达标。 | 破折号→冒号 | 破折 |
| 51 | - **稳定复现验证（核心）**：**同一提示词+同一 seed** 再跑一次固定脚本…判据——同 seed 目录下辨识度/配色/光影是否**稳定一致**…**未稳定复现不冻结**——画风漂移说明提示词对 seed 敏感，只改变体一个子维度调整，直到稳定复现。 | - **稳定复现验证（核心）**：用**同一提示词与同一 seed** 重新执行一次固定脚本…判据为：同 seed 目录下辨识度、配色、光影是否**稳定一致**…**未稳定复现不冻结**：画风漂移说明提示词对 seed 敏感，只改该变体的一个子维度进行调整，直到稳定复现。 | 再跑一次→重新执行一次；+→与；落同目录→输出到同一目录 | 符号、破折、语域 |
| 52 | **4. 认可 → 冻结基线资产**：用户认可+稳定复现通过后，画风三字段**冻结为基线**： | **4. 认可后冻结基线资产**：用户认可且稳定复现通过后，画风三字段**冻结为基线**： | →→后；+→且 | 符号 |
| 53 | - 落盘 `素材/风格/画风决策.md` 并标注 `签核状态: ✅ 已认可` | - 写入 `素材/风格/画风决策.md`，并标注 `签核状态: ✅ 已认可` | 落盘→写入 | 语域 |
| 54 | - 记录：画风名 + `dna` + `constraint` + `recommended_lighting` + `recommended_composition` + **参考图路径** + **定标 seed** + 定标图路径 | - 记录：画风名、`dna`、`constraint`、`recommended_lighting`、`recommended_composition`、**参考图路径**、**定标 seed**、定标图路径 | +→、 | 符号 |
| 55 | - **seed 必须记录**——下游用同 seed 复现保证稳定；参考图路径作为下游图生图 image 参数来源 | - **seed 必须记录**：下游用同 seed 复现保证稳定；参考图路径作为下游图生图 image 参数的来源 | 破折号→冒号 | 破折 |
| 56 | **5. 通知下游**：画风定标图已就绪，可被以下消费： | **5. 通知下游**：画风定标图已就绪，可供以下环节消费： | 可被…消费→可供…环节消费 | 缺谓 |
| 57 | ｜ `pop-visual-cover` / `pop-visual-oc` ｜ 封面/OC 的风格参考图（image 参数 + 同 seed） ｜ | ｜ `pop-visual-cover` / `pop-visual-oc` ｜ 封面与 OC 的风格参考图（image 参数与同 seed） ｜ | /→与；+→与 | 符号 |

## 六、SKILL.md · 铁律（原第 187—196 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 58 | ｜ ❌1 ｜ **画风必须从DNA库取** — 禁止凭记忆编画风描述，必须从文风DNA-library.json取dna+constraint字段 ｜ 画风辨识度不足，风格漂移 ｜ | ｜ ❌1 ｜ **画风必须从DNA库取** — 禁止凭记忆编写画风描述，必须从文风DNA-library.json取dna与constraint字段 ｜ 画风辨识度不足，风格漂移 ｜ | 编→编写；+→与 | 符号、缺谓 |
| 59 | ｜ ❌4 ｜ **提示词记录必须落盘** — 画风选择、提示词、参数写入项目文件 ｜ 无法迭代优化 ｜ | ｜ ❌4 ｜ **提示词记录必须写入** — 画风选择、提示词、参数一并写入项目文件 ｜ 无法迭代优化 ｜ | 落盘→写入 | 语域 |
| 60 | ｜ ❌5 ｜ **定标图必须用小说次要视觉锚点（变量隔离）** — 用…测画风（`--scene` 场景/`--side` 路人）…**禁止用主角/主要角色**（形象验收归 art-bible/oc 环节） ｜ 用主角→混入形象变量，分不清画风/形象问题；用无关中性素材→代入感弱 ｜ | ｜ ❌5 ｜ 同前…（`--scene` 场景、`--side` 路人）…**禁止使用主角或主要角色**（形象验收归 art-bible、oc 环节） ｜ 用主角则混入形象变量，分不清画风与形象问题；用无关中性素材则代入感弱 ｜ | 禁止用→禁止使用；→→则 | 符号 |
| 61 | ｜ ❌7 ｜ **必须验证稳定复现** — 画风定标必须用同 seed + 同提示词复现对比…冻结时记录 seed + 参考图路径 ｜ 画风依赖单次运气，下游无法复现，画风漂移 ｜ | ｜ ❌7 ｜ **必须验证稳定复现** — 画风定标必须用同 seed 与同提示词复现对比…冻结时记录 seed 与参考图路径 ｜ 画风依赖单次运气，下游无法复现，画风漂移 ｜ | +→与 | 符号 |
| 62 | ｜ ❌8 ｜ **参考图是"图资产"单张锚定** — 主路径是整图复用（image 参数）…一次搜索、全程复用，禁止重复搜索 ｜ 重复付费 + 文字提炼还原不了参考图，风格失真 ｜ | ｜ ❌8 ｜ **参考图是"图资产"，单张固定** — 主路径是整图复用（image 参数）…一次搜索，全程复用，禁止重复搜索 ｜ 重复付费，且文字提炼还原不了参考图，风格失真 ｜ | 锚定→固定；+→且 | 生造、符号 |
| 63 | ｜ ❌9 ｜ **画风定标走固定脚本 `batch_test.py`** — 定标必须用 `../pop-visual-shared/scripts/batch_test.py`（固定素材+固定6段式模板+并发批量+自动PE日志）… ｜ 每次测试变量不隔离、不稳定、慢 ｜ | ｜ ❌9 ｜ 同前…（固定素材、固定6段式模板、并发批量、自动PE日志）… ｜ 每次测试变量不隔离、不稳定、慢 ｜ | +→、 | 符号 |
| 64 | ｜ ❌10 ｜ **画风与内容解耦 — DNA 必须是纯技法层** — `dna` 只描述怎么画（线稿/上色/光影/比例/特征）…（世界观/服装/建筑/道具/招数斗气等）…不同画风耦合方式各异，逐条剥离… ｜ … ｜ | ｜ ❌10 ｜ 同前…（线稿、上色、光影、比例、特征）…（世界观、服装、建筑、道具、招数斗气等）…不同画风的耦合方式各异，须逐条剥离… ｜ … ｜ | /→、；逐条→须逐条 | 符号、缺谓 |

## 七、SKILL.md · 速查表与跨 skill 引用（原第 200—221 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 65 | ｜ 我要 ｜ 读什么 ｜ 什么时候 ｜ | ｜ 需求 ｜ 读取内容 ｜ 使用时机 ｜ | 我要→需求；读什么→读取内容 | 语域 |
| 66 | ｜ 提示词结构+写法 ｜ `../pop-visual-shared/references/seedream-prompt-guide.md` ｜ Step 2 ｜ | ｜ 提示词结构与写法 ｜ 同上 ｜ Step 2 ｜ | +→与 | 符号 |
| 67 | ｜ 构图模板+光照模板+兼容性矩阵 ｜ `references/lighting-composition-templates.md` ｜ Step 1 兼容性检查 / Step 2 ｜ | ｜ 构图模板、光照模板与兼容性矩阵 ｜ 同上 ｜ Step 1 兼容性检查 / Step 2 ｜ | +→、与 | 符号 |
| 68 | > **环境**：Python 3.8+ + requests。 | > **环境**：Python 3.8+ 及 requests。 | +→及 | 符号 |
| 69 | art-bible/cover/oc/comic skill引用本skill画风层时： | art-bible、cover、oc、comic 各 skill 引用本 skill 画风层时： | /→、；补"各" | 符号、缺主 |
| 70 | 1. **画风描述**：从 `references/文风DNA-library.json` 取 `dna` + `constraint` 字段（**纯技法层**）；取 `content_theme` 作为该画风原生题材的默认内容（跨题材时用各skill的场景描述覆盖） | 1. **画风描述**：从 `references/文风DNA-library.json` 取 `dna` 与 `constraint` 字段（**纯技法层**）；取 `content_theme` 作为该画风原生题材的默认内容（跨题材时由各 skill 的场景描述覆盖） | +→与；用→由 | 符号 |
| 71 | 4. **提示词组装**：各skill用自己的结构层（V3/4块/三字段），画风段从DNA库取 `dna`+`constraint`（纯技法），内容层独立于画风段… | 4. **提示词组装**：各 skill 使用自己的结构层（V3、4块、三字段），画风段从 DNA 库取 `dna` 与 `constraint`（纯技法），内容层独立于画风段… | 用→使用；/、+→、与 | 符号 |
| 72 | 6. **画风基线资产+稳定复现**：…下游只消费冻结的画风三字段+定标图+冻结 seed+参考图…**按 intent**：`comic`/`full` 完整定标门禁+稳定复现，`cover`/`oc` 走 agent 自检分支 | 6. **画风基线资产与稳定复现**：…下游只消费冻结的画风三字段、定标图、冻结 seed、参考图…**按 intent 分流**：`comic`/`full` 走完整定标门禁与稳定复现，`cover`/`oc` 走 agent 自检分支 | +→、与；补"分流""走" | 符号、缺谓 |
| 73 | 7. **art-bible 是第一消费方**：画风基线首先被 `pop-visual-art-bible` 消费（画风篇引用+定全宇宙色彩基调），再由美术设定集下发给 oc/cover/comic… | 7. **art-bible 是第一消费方**：画风基线首先被 `pop-visual-art-bible` 消费（引用画风篇，定全宇宙色彩基调），再由美术设定集下发给 oc、cover、comic… | +→，；/→、 | 符号 |

## 八、lighting-composition-templates.md（原第 3—95 行）

| # | 原文 | 改后 | 语域修正 | 病类 |
|--:|:--|:--|:--|:--|
| 74 | > 本文件定义3种光照模板（LT1/LT2/LT3）和2种构图模板（CT1/CT2），以及画风-光照-构图兼容性矩阵。数据源：`references/文风DNA-library.json`。 | > 本文件定义3种光照模板（LT1/LT2/LT3）和2种构图模板（CT1/CT2），以及画风、光照、构图兼容性矩阵。数据源：同前。 | -→、 | 符号 |
| 75 | > **Phase 7验证**：成功拯救水彩风格(D→B)，轻小说插画风获A档。 | > **Phase 7验证**：成功将水彩风格从 D 档提升至 B 档，轻小说插画风获 A 档。 | →→文字 | 符号、缺主 |
| 76 | cover/oc/comic skill引用本文件时： | cover、oc、comic 各 skill 引用本文件时： | /→、；补"各" | 符号、缺主 |
| 77 | - **cover skill**：光源设计表仍由叙事驱动（道具自发光/逆光/顶光等），本文件的光照模板作为画风兼容性参考——选定画风后检查recommended_lighting，避免使用不兼容的光照 | - **cover skill**：光源设计表仍由叙事驱动（道具自发光、逆光、顶光等），本文件的光照模板作为画风兼容性参考；选定画风后检查 recommended_lighting，避免使用不兼容的光照 | 破折号→分号；/→、 | 破折、符号 |
| 78 | - **oc skill**：同cover，立绘的光源以角色表现为优先，但需检查画风兼容性 | - **oc skill**：与 cover 相同，立绘的光源以角色表现优先，但需检查画风兼容性 | 同cover→与 cover 相同 | 缺谓 |

## 未改动（按须知第四、五节保留）

| 项 | 保留内容 | 理由 |
|--:|:--|:--|
| 1 | `../pop-visual-pipeline/references/落盘规范.md` | 文件路径属机器可读内容，第 4 节保留（清单第 30 条命中） |
| 2 | 代码块全部内容，含 `# 从 DNA 库按画风名批量测（传入小说次要素材：战斗场景 + 路人）→ 导出 generation_tasks.json` 与 `# 精调变体（…）→ 用于"只改一个子维度"的回炉迭代` | 第 5 节：代码块内的命令、路径、字段名、阈值不碰 |
| 3 | `蒸汽朋克黄铜`（LT1 兼容画风表内） | 画风库条目名，第 4、5 节保留 |
| 4 | 全部画风名、光照/构图模板编号（LT1/LT2/LT3、CT1/CT2）、英文提示词描述 | 库条目与模板数据，第 4、5 节保留 |
| 5 | 尺寸表（1125x1500 等）、像素阈值 236 万、`watermark=`false``、`ARK_API_KEY`、`image_generate` 参数 | 数字阈值与字段名，第 4 节保留 |
| 6 | `Phase 6发现`、`Phase 7验证`、`Phase 0验证` 等带 Phase 标记的验证结论 | 验证记录，按第 5 节历史记录性质保留（仅第 75 条的旧写法按符号串处理） |
| 7 | `门禁`、`铁律`、`锚点`（视觉锚点）等本包既有术语 | 跨文件通用术语，不在须知第六节语域纪律表内，改则牵动全包，故留待统一裁定 |
| 8 | `| 本 skill 负责 |` 之后的边界表分隔行 `|:---------|:---------------------------|` | 表格结构行，第 4 节不改表格列 |
