# 条漫版本研究方案

> 为 pop-visual-comic 增加「条漫」版本的研究与改造方案。2026-08-05
> 结论先行：**现有管线已具备条漫的 HTML 形态（长条滚动），缺的是「生成单位」与「分格节奏」的条漫化改造，推荐在 comic 内新增条漫模式，而非新建独立 skill。**

---

## 一、现状诊断：comic 现在是什么

| 维度 | 当前 pop-visual-comic（页漫） |
|:-----|:------------------------------|
| **生成单位** | 「页」—— Seedream 单张直出**整页多格**漫画图（含分格线，YZ-1~8 八种模板） |
| **排版基准** | layout-baseline 元尊 YZ-1~8（页级分格结构） |
| **HTML 形态** | 已是最原始的「长条滚动」——整页图从上到下竖排，文字叠加 |
| **阅读节奏** | 以「页」为节奏单元，一屏一页 |
| **手机适配** | 容器 max-width 820px，整页图等比缩放，手机上偏小 |

**关键发现**：HTML 层已经是竖排滚动，但**生成层仍是「页漫」逻辑**——每张图是一页多格，不是一条纵向串流。这是条漫与现在的本质分界。

---

## 二、条漫 vs 页漫：核心差异（条漫方法论）

来自对网络漫画平台（小明太极/快看等）条漫分镜方法论的研究：

### 2.1 阅读机制
- **手机滑动 = 翻页**：手机屏幕单次呈现的内容 = 「一页」，每次下拉 = 翻一次页。
- 读者是**剧情的参与者**，下拉动作 = 摇镜头，天然增强沉浸感。

### 2.2 生成单位：格，不是页
条漫以「格」为基本单位纵向串流，一格相当独立成图。格宽通常固定（全宽），**格高可变**：
- 普通格：近景/特写/中景，常见高度
- 大画格：远景/全景，凸显人物与空间关系
- **跨屏超长画格（条漫独有）**：超过一屏，必须滑动才能窥全貌，用于一镜到底、战斗冲击、压迫感强化

### 2.3 分格与节奏铁律
- 中小格按 **S 型排布**保证视觉平衡（单列偏多时为上下节奏起伏）
- **避免连续多个跨屏长画格**，否则给读者阅读压力（张弛有度）
- 情绪节奏：普通格（快）→ 长画格（慢/沉浸）交错，控制下滑节奏
- **格高 = 节奏变量**：格越高，读者停留越久，情绪越沉淀

### 2.4 文字定位
条漫文本随「格」定位，气泡/旁白位置更自由，可精确落在某格内，不像页漫绑在整页上。

---

## 三、改造方案（推荐）

### 3.1 总体思路
在 pop-visual-comic 内新增**双模式**：现有「页漫模式」保留，新增「条漫模式」。入口由用户指定（或项目快照里记录）。

### 3.2 需要改的环节

| 环节 | 页漫（现状） | 条漫模式（新增） |
|:-----|:------------|:----------------|
| **1. 排版基准库** | 新增 `layout-baseline-webtoon.md`：条漫专属「格高体系」——普通格/大格/跨屏超长格 + S型排布 + 节奏计算公式（长格间隔 ≤N 格） |
| **2. Step1 场景拆分** | 以「页」拆场景 | 以「镜/格」拆场景，标注每格高度档位 + 一屏几格节奏组 |
| **3. Step2 导演卡** | 页面设计表（页级） | 镜表（格级）：格号/内容/高度档/镜语L/C/F/台词气泡/节奏位 |
| **4. 生成脚本** | 每页一条任务（多格页） | **每格一条生成任务**（或每节奏组一条长格），generate_comic_page.py 增加 webtoon 模式 |
| **5. HTML/叠加** | 整页图堆叠 | **逐格纵向排列**，格间留白可控，气泡/旁白精确锚定到格；容器限宽约 480px（手机标准） |
| **6. 分镜提示词** | 页内排多个 Panel | 单格/长格独立构图，长格用「纵向延伸构图」描述 |

### 3.3 提示词层差异（关键）
- **页漫**：`A vertical manga comic page with 4 panels arranged in a 2x2 grid...`
- **条漫普通格**：`A single vertical webtoon panel, full width, [构图/镜头]，fills the frame`
- **条漫超长格**：`A single ultra-tall vertical webtoon panel extending beyond one screen, [一镜到底/纵深构图]，continuous vertical action`

复用现有 L/C/F 镜语库、三重锁定、文字控制负面词——这些与页漫完全通用，无需重做。

### 3.4 复用清单（不重造轮子）
- 画风基准库 art-style-baseline ✅ 通用
- 内容层 L/C/F 镜语库 ✅ 通用
- 文字控制负面词 ✅ 通用
- 角色三重锁定/定妆图 ✅ 通用
- 记忆机制（角色库/快照/状态/沉淀）✅ 通用
- 三个 step 文件流程骨架 ✅ 复用，仅产出表格式不同

---

## 四、两个可选决策

**决策 A：模式组织方式**
1. **（推荐）comic 内双模式**：SKILL.md 加模式切换说明，Step 文件按模式分支。改动可控，复用度高。
2. **新建独立 skill** `pop-visual-webtoon`：彻底隔离，但大量复用代码重复，维护成本高。

**决策 B：条漫生成粒度**
1. **（推荐）每格一图**：单格独立生成，节奏/构图最可控，角色一致性好（每格可用定妆图参考），但章图数量多（一屏=1~2格）。
2. **每节奏组一图**：2-3 格拼一张长图，减少生成次数，但构图控制弱（类似页漫，没吃到条漫红利）。

---

## 五、建议下一步

1. 确认决策 A（推荐 comic 内双模式）
2. 先做一条**样章**验证：用现有「我的诡异人生」或新章节，手工按条漫镜表生成 3-5 格长条，验证`image_generate`在「单格全宽 + 超长格」两种提示词下的出图质量与节奏
3. 样章通过后，再落库排版基准（layout-baseline-webtoon）+ 改脚本/Step

> 样章先行，避免一次性大改后返工。这是沿用 popwave 一贯的「先实测验证再入库」铁律。